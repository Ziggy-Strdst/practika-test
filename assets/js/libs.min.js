!(function (t, e) {
  'use strict';
  'object' == typeof module && 'object' == typeof module.exports
    ? (module.exports = t.document
        ? e(t, !0)
        : function (t) {
            if (!t.document)
              throw new Error('jQuery requires a window with a document');
            return e(t);
          })
    : e(t);
})('undefined' != typeof window ? window : this, function (t, e) {
  'use strict';
  function n(t, e, n) {
    var i,
      o = (e = e || st).createElement('script');
    if (((o.text = t), n)) for (i in xt) n[i] && (o[i] = n[i]);
    e.head.appendChild(o).parentNode.removeChild(o);
  }
  function i(t) {
    return null == t
      ? t + ''
      : 'object' == typeof t || 'function' == typeof t
      ? pt[ht.call(t)] || 'object'
      : typeof t;
  }
  function o(t) {
    var e = !!t && 'length' in t && t.length,
      n = i(t);
    return (
      !yt(t) &&
      !bt(t) &&
      ('array' === n ||
        0 === e ||
        ('number' == typeof e && e > 0 && e - 1 in t))
    );
  }
  function r(t, e) {
    return t.nodeName && t.nodeName.toLowerCase() === e.toLowerCase();
  }
  function s(t, e, n) {
    return yt(e)
      ? wt.grep(t, function (t, i) {
          return !!e.call(t, i, t) !== n;
        })
      : e.nodeType
      ? wt.grep(t, function (t) {
          return (t === e) !== n;
        })
      : 'string' != typeof e
      ? wt.grep(t, function (t) {
          return dt.call(e, t) > -1 !== n;
        })
      : wt.filter(e, t, n);
  }
  function a(t, e) {
    for (; (t = t[e]) && 1 !== t.nodeType; );
    return t;
  }
  function l(t) {
    var e = {};
    return (
      wt.each(t.match(Mt) || [], function (t, n) {
        e[n] = !0;
      }),
      e
    );
  }
  function c(t) {
    return t;
  }
  function u(t) {
    throw t;
  }
  function d(t, e, n, i) {
    var o;
    try {
      t && yt((o = t.promise))
        ? o.call(t).done(e).fail(n)
        : t && yt((o = t.then))
        ? o.call(t, e, n)
        : e.apply(void 0, [t].slice(i));
    } catch (t) {
      n.apply(void 0, [t]);
    }
  }
  function p() {
    st.removeEventListener('DOMContentLoaded', p),
      t.removeEventListener('load', p),
      wt.ready();
  }
  function h(t, e) {
    return e.toUpperCase();
  }
  function f(t) {
    return t.replace(zt, 'ms-').replace(Ot, h);
  }
  function g() {
    this.expando = wt.expando + g.uid++;
  }
  function m(t) {
    return (
      'true' === t ||
      ('false' !== t &&
        ('null' === t
          ? null
          : t === +t + ''
          ? +t
          : qt.test(t)
          ? JSON.parse(t)
          : t))
    );
  }
  function v(t, e, n) {
    var i;
    if (void 0 === n && 1 === t.nodeType)
      if (
        ((i = 'data-' + e.replace(Rt, '-$&').toLowerCase()),
        'string' == typeof (n = t.getAttribute(i)))
      ) {
        try {
          n = m(n);
        } catch (t) {}
        Ft.set(t, e, n);
      } else n = void 0;
    return n;
  }
  function y(t, e, n, i) {
    var o,
      r,
      s = 20,
      a = i
        ? function () {
            return i.cur();
          }
        : function () {
            return wt.css(t, e, '');
          },
      l = a(),
      c = (n && n[3]) || (wt.cssNumber[e] ? '' : 'px'),
      u = (wt.cssNumber[e] || ('px' !== c && +l)) && Wt.exec(wt.css(t, e));
    if (u && u[3] !== c) {
      for (l /= 2, c = c || u[3], u = +l || 1; s--; )
        wt.style(t, e, u + c),
          (1 - r) * (1 - (r = a() / l || 0.5)) <= 0 && (s = 0),
          (u /= r);
      (u *= 2), wt.style(t, e, u + c), (n = n || []);
    }
    return (
      n &&
        ((u = +u || +l || 0),
        (o = n[1] ? u + (n[1] + 1) * n[2] : +n[2]),
        i && ((i.unit = c), (i.start = u), (i.end = o))),
      o
    );
  }
  function b(t) {
    var e,
      n = t.ownerDocument,
      i = t.nodeName,
      o = Ut[i];
    return (
      o ||
      ((e = n.body.appendChild(n.createElement(i))),
      (o = wt.css(e, 'display')),
      e.parentNode.removeChild(e),
      'none' === o && (o = 'block'),
      (Ut[i] = o),
      o)
    );
  }
  function x(t, e) {
    for (var n, i, o = [], r = 0, s = t.length; r < s; r++)
      (i = t[r]).style &&
        ((n = i.style.display),
        e
          ? ('none' === n &&
              ((o[r] = Nt.get(i, 'display') || null),
              o[r] || (i.style.display = '')),
            '' === i.style.display && Vt(i) && (o[r] = b(i)))
          : 'none' !== n && ((o[r] = 'none'), Nt.set(i, 'display', n)));
    for (r = 0; r < s; r++) null != o[r] && (t[r].style.display = o[r]);
    return t;
  }
  function w(t, e) {
    var n;
    return (
      (n =
        void 0 !== t.getElementsByTagName
          ? t.getElementsByTagName(e || '*')
          : void 0 !== t.querySelectorAll
          ? t.querySelectorAll(e || '*')
          : []),
      void 0 === e || (e && r(t, e)) ? wt.merge([t], n) : n
    );
  }
  function C(t, e) {
    for (var n = 0, i = t.length; n < i; n++)
      Nt.set(t[n], 'globalEval', !e || Nt.get(e[n], 'globalEval'));
  }
  function _(t, e, n, o, r) {
    for (
      var s,
        a,
        l,
        c,
        u,
        d,
        p = e.createDocumentFragment(),
        h = [],
        f = 0,
        g = t.length;
      f < g;
      f++
    )
      if ((s = t[f]) || 0 === s)
        if ('object' === i(s)) wt.merge(h, s.nodeType ? [s] : s);
        else if (Jt.test(s)) {
          for (
            a = a || p.appendChild(e.createElement('div')),
              l = (Qt.exec(s) || ['', ''])[1].toLowerCase(),
              c = Kt[l] || Kt._default,
              a.innerHTML = c[1] + wt.htmlPrefilter(s) + c[2],
              d = c[0];
            d--;

          )
            a = a.lastChild;
          wt.merge(h, a.childNodes), ((a = p.firstChild).textContent = '');
        } else h.push(e.createTextNode(s));
    for (p.textContent = '', f = 0; (s = h[f++]); )
      if (o && wt.inArray(s, o) > -1) r && r.push(s);
      else if (
        ((u = wt.contains(s.ownerDocument, s)),
        (a = w(p.appendChild(s), 'script')),
        u && C(a),
        n)
      )
        for (d = 0; (s = a[d++]); ) Gt.test(s.type || '') && n.push(s);
    return p;
  }
  function T() {
    return !0;
  }
  function k() {
    return !1;
  }
  function S() {
    try {
      return st.activeElement;
    } catch (t) {}
  }
  function $(t, e, n, i, o, r) {
    var s, a;
    if ('object' == typeof e) {
      'string' != typeof n && ((i = i || n), (n = void 0));
      for (a in e) $(t, a, n, i, e[a], r);
      return t;
    }
    if (
      (null == i && null == o
        ? ((o = n), (i = n = void 0))
        : null == o &&
          ('string' == typeof n
            ? ((o = i), (i = void 0))
            : ((o = i), (i = n), (n = void 0))),
      !1 === o)
    )
      o = k;
    else if (!o) return t;
    return (
      1 === r &&
        ((s = o),
        ((o = function (t) {
          return wt().off(t), s.apply(this, arguments);
        }).guid = s.guid || (s.guid = wt.guid++))),
      t.each(function () {
        wt.event.add(this, e, o, i, n);
      })
    );
  }
  function E(t, e) {
    return r(t, 'table') && r(11 !== e.nodeType ? e : e.firstChild, 'tr')
      ? wt(t).children('tbody')[0] || t
      : t;
  }
  function P(t) {
    return (t.type = (null !== t.getAttribute('type')) + '/' + t.type), t;
  }
  function A(t) {
    return (
      'true/' === (t.type || '').slice(0, 5)
        ? (t.type = t.type.slice(5))
        : t.removeAttribute('type'),
      t
    );
  }
  function I(t, e) {
    var n, i, o, r, s, a, l, c;
    if (1 === e.nodeType) {
      if (
        Nt.hasData(t) &&
        ((r = Nt.access(t)), (s = Nt.set(e, r)), (c = r.events))
      ) {
        delete s.handle, (s.events = {});
        for (o in c)
          for (n = 0, i = c[o].length; n < i; n++) wt.event.add(e, o, c[o][n]);
      }
      Ft.hasData(t) &&
        ((a = Ft.access(t)), (l = wt.extend({}, a)), Ft.set(e, l));
    }
  }
  function M(t, e) {
    var n = e.nodeName.toLowerCase();
    'input' === n && Zt.test(t.type)
      ? (e.checked = t.checked)
      : ('input' !== n && 'textarea' !== n) ||
        (e.defaultValue = t.defaultValue);
  }
  function L(t, e, i, o) {
    e = ct.apply([], e);
    var r,
      s,
      a,
      l,
      c,
      u,
      d = 0,
      p = t.length,
      h = p - 1,
      f = e[0],
      g = yt(f);
    if (g || (p > 1 && 'string' == typeof f && !vt.checkClone && se.test(f)))
      return t.each(function (n) {
        var r = t.eq(n);
        g && (e[0] = f.call(this, n, r.html())), L(r, e, i, o);
      });
    if (
      p &&
      ((r = _(e, t[0].ownerDocument, !1, t, o)),
      (s = r.firstChild),
      1 === r.childNodes.length && (r = s),
      s || o)
    ) {
      for (l = (a = wt.map(w(r, 'script'), P)).length; d < p; d++)
        (c = r),
          d !== h &&
            ((c = wt.clone(c, !0, !0)), l && wt.merge(a, w(c, 'script'))),
          i.call(t[d], c, d);
      if (l)
        for (u = a[a.length - 1].ownerDocument, wt.map(a, A), d = 0; d < l; d++)
          (c = a[d]),
            Gt.test(c.type || '') &&
              !Nt.access(c, 'globalEval') &&
              wt.contains(u, c) &&
              (c.src && 'module' !== (c.type || '').toLowerCase()
                ? wt._evalUrl && wt._evalUrl(c.src)
                : n(c.textContent.replace(ae, ''), u, c));
    }
    return t;
  }
  function D(t, e, n) {
    for (var i, o = e ? wt.filter(e, t) : t, r = 0; null != (i = o[r]); r++)
      n || 1 !== i.nodeType || wt.cleanData(w(i)),
        i.parentNode &&
          (n && wt.contains(i.ownerDocument, i) && C(w(i, 'script')),
          i.parentNode.removeChild(i));
    return t;
  }
  function j(t, e, n) {
    var i,
      o,
      r,
      s,
      a = t.style;
    return (
      (n = n || ce(t)) &&
        ('' !== (s = n.getPropertyValue(e) || n[e]) ||
          wt.contains(t.ownerDocument, t) ||
          (s = wt.style(t, e)),
        !vt.pixelBoxStyles() &&
          le.test(s) &&
          ue.test(e) &&
          ((i = a.width),
          (o = a.minWidth),
          (r = a.maxWidth),
          (a.minWidth = a.maxWidth = a.width = s),
          (s = n.width),
          (a.width = i),
          (a.minWidth = o),
          (a.maxWidth = r))),
      void 0 !== s ? s + '' : s
    );
  }
  function z(t, e) {
    return {
      get: function () {
        if (!t()) return (this.get = e).apply(this, arguments);
        delete this.get;
      },
    };
  }
  function O(t) {
    if (t in me) return t;
    for (var e = t[0].toUpperCase() + t.slice(1), n = ge.length; n--; )
      if ((t = ge[n] + e) in me) return t;
  }
  function H(t) {
    var e = wt.cssProps[t];
    return e || (e = wt.cssProps[t] = O(t) || t), e;
  }
  function N(t, e, n) {
    var i = Wt.exec(e);
    return i ? Math.max(0, i[2] - (n || 0)) + (i[3] || 'px') : e;
  }
  function F(t, e, n, i, o, r) {
    var s = 'width' === e ? 1 : 0,
      a = 0,
      l = 0;
    if (n === (i ? 'border' : 'content')) return 0;
    for (; s < 4; s += 2)
      'margin' === n && (l += wt.css(t, n + Xt[s], !0, o)),
        i
          ? ('content' === n && (l -= wt.css(t, 'padding' + Xt[s], !0, o)),
            'margin' !== n &&
              (l -= wt.css(t, 'border' + Xt[s] + 'Width', !0, o)))
          : ((l += wt.css(t, 'padding' + Xt[s], !0, o)),
            'padding' !== n
              ? (l += wt.css(t, 'border' + Xt[s] + 'Width', !0, o))
              : (a += wt.css(t, 'border' + Xt[s] + 'Width', !0, o)));
    return (
      !i &&
        r >= 0 &&
        (l += Math.max(
          0,
          Math.ceil(
            t['offset' + e[0].toUpperCase() + e.slice(1)] - r - l - a - 0.5
          )
        )),
      l
    );
  }
  function q(t, e, n) {
    var i = ce(t),
      o = j(t, e, i),
      r = 'border-box' === wt.css(t, 'boxSizing', !1, i),
      s = r;
    if (le.test(o)) {
      if (!n) return o;
      o = 'auto';
    }
    return (
      (s = s && (vt.boxSizingReliable() || o === t.style[e])),
      ('auto' === o ||
        (!parseFloat(o) && 'inline' === wt.css(t, 'display', !1, i))) &&
        ((o = t['offset' + e[0].toUpperCase() + e.slice(1)]), (s = !0)),
      (o = parseFloat(o) || 0) +
        F(t, e, n || (r ? 'border' : 'content'), s, i, o) +
        'px'
    );
  }
  function R(t, e, n, i, o) {
    return new R.prototype.init(t, e, n, i, o);
  }
  function B() {
    ye &&
      (!1 === st.hidden && t.requestAnimationFrame
        ? t.requestAnimationFrame(B)
        : t.setTimeout(B, wt.fx.interval),
      wt.fx.tick());
  }
  function W() {
    return (
      t.setTimeout(function () {
        ve = void 0;
      }),
      (ve = Date.now())
    );
  }
  function X(t, e) {
    var n,
      i = 0,
      o = { height: t };
    for (e = e ? 1 : 0; i < 4; i += 2 - e)
      o['margin' + (n = Xt[i])] = o['padding' + n] = t;
    return e && (o.opacity = o.width = t), o;
  }
  function V(t, e, n) {
    for (
      var i,
        o = (Z.tweeners[e] || []).concat(Z.tweeners['*']),
        r = 0,
        s = o.length;
      r < s;
      r++
    )
      if ((i = o[r].call(n, e, t))) return i;
  }
  function Y(t, e, n) {
    var i,
      o,
      r,
      s,
      a,
      l,
      c,
      u,
      d = 'width' in e || 'height' in e,
      p = this,
      h = {},
      f = t.style,
      g = t.nodeType && Vt(t),
      m = Nt.get(t, 'fxshow');
    n.queue ||
      (null == (s = wt._queueHooks(t, 'fx')).unqueued &&
        ((s.unqueued = 0),
        (a = s.empty.fire),
        (s.empty.fire = function () {
          s.unqueued || a();
        })),
      s.unqueued++,
      p.always(function () {
        p.always(function () {
          s.unqueued--, wt.queue(t, 'fx').length || s.empty.fire();
        });
      }));
    for (i in e)
      if (((o = e[i]), be.test(o))) {
        if (
          (delete e[i], (r = r || 'toggle' === o), o === (g ? 'hide' : 'show'))
        ) {
          if ('show' !== o || !m || void 0 === m[i]) continue;
          g = !0;
        }
        h[i] = (m && m[i]) || wt.style(t, i);
      }
    if ((l = !wt.isEmptyObject(e)) || !wt.isEmptyObject(h)) {
      d &&
        1 === t.nodeType &&
        ((n.overflow = [f.overflow, f.overflowX, f.overflowY]),
        null == (c = m && m.display) && (c = Nt.get(t, 'display')),
        'none' === (u = wt.css(t, 'display')) &&
          (c
            ? (u = c)
            : (x([t], !0),
              (c = t.style.display || c),
              (u = wt.css(t, 'display')),
              x([t]))),
        ('inline' === u || ('inline-block' === u && null != c)) &&
          'none' === wt.css(t, 'float') &&
          (l ||
            (p.done(function () {
              f.display = c;
            }),
            null == c && ((u = f.display), (c = 'none' === u ? '' : u))),
          (f.display = 'inline-block'))),
        n.overflow &&
          ((f.overflow = 'hidden'),
          p.always(function () {
            (f.overflow = n.overflow[0]),
              (f.overflowX = n.overflow[1]),
              (f.overflowY = n.overflow[2]);
          })),
        (l = !1);
      for (i in h)
        l ||
          (m
            ? 'hidden' in m && (g = m.hidden)
            : (m = Nt.access(t, 'fxshow', { display: c })),
          r && (m.hidden = !g),
          g && x([t], !0),
          p.done(function () {
            g || x([t]), Nt.remove(t, 'fxshow');
            for (i in h) wt.style(t, i, h[i]);
          })),
          (l = V(g ? m[i] : 0, i, p)),
          i in m || ((m[i] = l.start), g && ((l.end = l.start), (l.start = 0)));
    }
  }
  function U(t, e) {
    var n, i, o, r, s;
    for (n in t)
      if (
        ((i = f(n)),
        (o = e[i]),
        (r = t[n]),
        Array.isArray(r) && ((o = r[1]), (r = t[n] = r[0])),
        n !== i && ((t[i] = r), delete t[n]),
        (s = wt.cssHooks[i]) && 'expand' in s)
      ) {
        (r = s.expand(r)), delete t[i];
        for (n in r) n in t || ((t[n] = r[n]), (e[n] = o));
      } else e[i] = o;
  }
  function Z(t, e, n) {
    var i,
      o,
      r = 0,
      s = Z.prefilters.length,
      a = wt.Deferred().always(function () {
        delete l.elem;
      }),
      l = function () {
        if (o) return !1;
        for (
          var e = ve || W(),
            n = Math.max(0, c.startTime + c.duration - e),
            i = 1 - (n / c.duration || 0),
            r = 0,
            s = c.tweens.length;
          r < s;
          r++
        )
          c.tweens[r].run(i);
        return (
          a.notifyWith(t, [c, i, n]),
          i < 1 && s
            ? n
            : (s || a.notifyWith(t, [c, 1, 0]), a.resolveWith(t, [c]), !1)
        );
      },
      c = a.promise({
        elem: t,
        props: wt.extend({}, e),
        opts: wt.extend(
          !0,
          { specialEasing: {}, easing: wt.easing._default },
          n
        ),
        originalProperties: e,
        originalOptions: n,
        startTime: ve || W(),
        duration: n.duration,
        tweens: [],
        createTween: function (e, n) {
          var i = wt.Tween(
            t,
            c.opts,
            e,
            n,
            c.opts.specialEasing[e] || c.opts.easing
          );
          return c.tweens.push(i), i;
        },
        stop: function (e) {
          var n = 0,
            i = e ? c.tweens.length : 0;
          if (o) return this;
          for (o = !0; n < i; n++) c.tweens[n].run(1);
          return (
            e
              ? (a.notifyWith(t, [c, 1, 0]), a.resolveWith(t, [c, e]))
              : a.rejectWith(t, [c, e]),
            this
          );
        },
      }),
      u = c.props;
    for (U(u, c.opts.specialEasing); r < s; r++)
      if ((i = Z.prefilters[r].call(c, t, u, c.opts)))
        return (
          yt(i.stop) &&
            (wt._queueHooks(c.elem, c.opts.queue).stop = i.stop.bind(i)),
          i
        );
    return (
      wt.map(u, V, c),
      yt(c.opts.start) && c.opts.start.call(t, c),
      c
        .progress(c.opts.progress)
        .done(c.opts.done, c.opts.complete)
        .fail(c.opts.fail)
        .always(c.opts.always),
      wt.fx.timer(wt.extend(l, { elem: t, anim: c, queue: c.opts.queue })),
      c
    );
  }
  function Q(t) {
    return (t.match(Mt) || []).join(' ');
  }
  function G(t) {
    return (t.getAttribute && t.getAttribute('class')) || '';
  }
  function K(t) {
    return Array.isArray(t) ? t : 'string' == typeof t ? t.match(Mt) || [] : [];
  }
  function J(t, e, n, o) {
    var r;
    if (Array.isArray(e))
      wt.each(e, function (e, i) {
        n || Ie.test(t)
          ? o(t, i)
          : J(
              t + '[' + ('object' == typeof i && null != i ? e : '') + ']',
              i,
              n,
              o
            );
      });
    else if (n || 'object' !== i(e)) o(t, e);
    else for (r in e) J(t + '[' + r + ']', e[r], n, o);
  }
  function tt(t) {
    return function (e, n) {
      'string' != typeof e && ((n = e), (e = '*'));
      var i,
        o = 0,
        r = e.toLowerCase().match(Mt) || [];
      if (yt(n))
        for (; (i = r[o++]); )
          '+' === i[0]
            ? ((i = i.slice(1) || '*'), (t[i] = t[i] || []).unshift(n))
            : (t[i] = t[i] || []).push(n);
    };
  }
  function et(t, e, n, i) {
    function o(a) {
      var l;
      return (
        (r[a] = !0),
        wt.each(t[a] || [], function (t, a) {
          var c = a(e, n, i);
          return 'string' != typeof c || s || r[c]
            ? s
              ? !(l = c)
              : void 0
            : (e.dataTypes.unshift(c), o(c), !1);
        }),
        l
      );
    }
    var r = {},
      s = t === Be;
    return o(e.dataTypes[0]) || (!r['*'] && o('*'));
  }
  function nt(t, e) {
    var n,
      i,
      o = wt.ajaxSettings.flatOptions || {};
    for (n in e) void 0 !== e[n] && ((o[n] ? t : i || (i = {}))[n] = e[n]);
    return i && wt.extend(!0, t, i), t;
  }
  function it(t, e, n) {
    for (var i, o, r, s, a = t.contents, l = t.dataTypes; '*' === l[0]; )
      l.shift(),
        void 0 === i && (i = t.mimeType || e.getResponseHeader('Content-Type'));
    if (i)
      for (o in a)
        if (a[o] && a[o].test(i)) {
          l.unshift(o);
          break;
        }
    if (l[0] in n) r = l[0];
    else {
      for (o in n) {
        if (!l[0] || t.converters[o + ' ' + l[0]]) {
          r = o;
          break;
        }
        s || (s = o);
      }
      r = r || s;
    }
    if (r) return r !== l[0] && l.unshift(r), n[r];
  }
  function ot(t, e, n, i) {
    var o,
      r,
      s,
      a,
      l,
      c = {},
      u = t.dataTypes.slice();
    if (u[1]) for (s in t.converters) c[s.toLowerCase()] = t.converters[s];
    for (r = u.shift(); r; )
      if (
        (t.responseFields[r] && (n[t.responseFields[r]] = e),
        !l && i && t.dataFilter && (e = t.dataFilter(e, t.dataType)),
        (l = r),
        (r = u.shift()))
      )
        if ('*' === r) r = l;
        else if ('*' !== l && l !== r) {
          if (!(s = c[l + ' ' + r] || c['* ' + r]))
            for (o in c)
              if (
                (a = o.split(' '))[1] === r &&
                (s = c[l + ' ' + a[0]] || c['* ' + a[0]])
              ) {
                !0 === s
                  ? (s = c[o])
                  : !0 !== c[o] && ((r = a[0]), u.unshift(a[1]));
                break;
              }
          if (!0 !== s)
            if (s && t.throws) e = s(e);
            else
              try {
                e = s(e);
              } catch (t) {
                return {
                  state: 'parsererror',
                  error: s ? t : 'No conversion from ' + l + ' to ' + r,
                };
              }
        }
    return { state: 'success', data: e };
  }
  var rt = [],
    st = t.document,
    at = Object.getPrototypeOf,
    lt = rt.slice,
    ct = rt.concat,
    ut = rt.push,
    dt = rt.indexOf,
    pt = {},
    ht = pt.toString,
    ft = pt.hasOwnProperty,
    gt = ft.toString,
    mt = gt.call(Object),
    vt = {},
    yt = function (t) {
      return 'function' == typeof t && 'number' != typeof t.nodeType;
    },
    bt = function (t) {
      return null != t && t === t.window;
    },
    xt = { type: !0, src: !0, noModule: !0 },
    wt = function (t, e) {
      return new wt.fn.init(t, e);
    },
    Ct = /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g;
  (wt.fn = wt.prototype =
    {
      jquery: '3.3.1',
      constructor: wt,
      length: 0,
      toArray: function () {
        return lt.call(this);
      },
      get: function (t) {
        return null == t
          ? lt.call(this)
          : t < 0
          ? this[t + this.length]
          : this[t];
      },
      pushStack: function (t) {
        var e = wt.merge(this.constructor(), t);
        return (e.prevObject = this), e;
      },
      each: function (t) {
        return wt.each(this, t);
      },
      map: function (t) {
        return this.pushStack(
          wt.map(this, function (e, n) {
            return t.call(e, n, e);
          })
        );
      },
      slice: function () {
        return this.pushStack(lt.apply(this, arguments));
      },
      first: function () {
        return this.eq(0);
      },
      last: function () {
        return this.eq(-1);
      },
      eq: function (t) {
        var e = this.length,
          n = +t + (t < 0 ? e : 0);
        return this.pushStack(n >= 0 && n < e ? [this[n]] : []);
      },
      end: function () {
        return this.prevObject || this.constructor();
      },
      push: ut,
      sort: rt.sort,
      splice: rt.splice,
    }),
    (wt.extend = wt.fn.extend =
      function () {
        var t,
          e,
          n,
          i,
          o,
          r,
          s = arguments[0] || {},
          a = 1,
          l = arguments.length,
          c = !1;
        for (
          'boolean' == typeof s && ((c = s), (s = arguments[a] || {}), a++),
            'object' == typeof s || yt(s) || (s = {}),
            a === l && ((s = this), a--);
          a < l;
          a++
        )
          if (null != (t = arguments[a]))
            for (e in t)
              (n = s[e]),
                s !== (i = t[e]) &&
                  (c && i && (wt.isPlainObject(i) || (o = Array.isArray(i)))
                    ? (o
                        ? ((o = !1), (r = n && Array.isArray(n) ? n : []))
                        : (r = n && wt.isPlainObject(n) ? n : {}),
                      (s[e] = wt.extend(c, r, i)))
                    : void 0 !== i && (s[e] = i));
        return s;
      }),
    wt.extend({
      expando: 'jQuery' + ('3.3.1' + Math.random()).replace(/\D/g, ''),
      isReady: !0,
      error: function (t) {
        throw new Error(t);
      },
      noop: function () {},
      isPlainObject: function (t) {
        var e, n;
        return !(
          !t ||
          '[object Object]' !== ht.call(t) ||
          ((e = at(t)) &&
            ('function' !=
              typeof (n = ft.call(e, 'constructor') && e.constructor) ||
              gt.call(n) !== mt))
        );
      },
      isEmptyObject: function (t) {
        var e;
        for (e in t) return !1;
        return !0;
      },
      globalEval: function (t) {
        n(t);
      },
      each: function (t, e) {
        var n,
          i = 0;
        if (o(t))
          for (n = t.length; i < n && !1 !== e.call(t[i], i, t[i]); i++);
        else for (i in t) if (!1 === e.call(t[i], i, t[i])) break;
        return t;
      },
      trim: function (t) {
        return null == t ? '' : (t + '').replace(Ct, '');
      },
      makeArray: function (t, e) {
        var n = e || [];
        return (
          null != t &&
            (o(Object(t))
              ? wt.merge(n, 'string' == typeof t ? [t] : t)
              : ut.call(n, t)),
          n
        );
      },
      inArray: function (t, e, n) {
        return null == e ? -1 : dt.call(e, t, n);
      },
      merge: function (t, e) {
        for (var n = +e.length, i = 0, o = t.length; i < n; i++) t[o++] = e[i];
        return (t.length = o), t;
      },
      grep: function (t, e, n) {
        for (var i = [], o = 0, r = t.length, s = !n; o < r; o++)
          !e(t[o], o) !== s && i.push(t[o]);
        return i;
      },
      map: function (t, e, n) {
        var i,
          r,
          s = 0,
          a = [];
        if (o(t))
          for (i = t.length; s < i; s++)
            null != (r = e(t[s], s, n)) && a.push(r);
        else for (s in t) null != (r = e(t[s], s, n)) && a.push(r);
        return ct.apply([], a);
      },
      guid: 1,
      support: vt,
    }),
    'function' == typeof Symbol &&
      (wt.fn[Symbol.iterator] = rt[Symbol.iterator]),
    wt.each(
      'Boolean Number String Function Array Date RegExp Object Error Symbol'.split(
        ' '
      ),
      function (t, e) {
        pt['[object ' + e + ']'] = e.toLowerCase();
      }
    );
  var _t = (function (t) {
    function e(t, e, n, i) {
      var o,
        r,
        s,
        a,
        l,
        c,
        u,
        p = e && e.ownerDocument,
        f = e ? e.nodeType : 9;
      if (
        ((n = n || []),
        'string' != typeof t || !t || (1 !== f && 9 !== f && 11 !== f))
      )
        return n;
      if (
        !i &&
        ((e ? e.ownerDocument || e : q) !== L && M(e), (e = e || L), j)
      ) {
        if (11 !== f && (l = vt.exec(t)))
          if ((o = l[1])) {
            if (9 === f) {
              if (!(s = e.getElementById(o))) return n;
              if (s.id === o) return n.push(s), n;
            } else if (p && (s = p.getElementById(o)) && N(e, s) && s.id === o)
              return n.push(s), n;
          } else {
            if (l[2]) return K.apply(n, e.getElementsByTagName(t)), n;
            if (
              (o = l[3]) &&
              C.getElementsByClassName &&
              e.getElementsByClassName
            )
              return K.apply(n, e.getElementsByClassName(o)), n;
          }
        if (C.qsa && !V[t + ' '] && (!z || !z.test(t))) {
          if (1 !== f) (p = e), (u = t);
          else if ('object' !== e.nodeName.toLowerCase()) {
            for (
              (a = e.getAttribute('id'))
                ? (a = a.replace(wt, Ct))
                : e.setAttribute('id', (a = F)),
                r = (c = S(t)).length;
              r--;

            )
              c[r] = '#' + a + ' ' + h(c[r]);
            (u = c.join(',')), (p = (yt.test(t) && d(e.parentNode)) || e);
          }
          if (u)
            try {
              return K.apply(n, p.querySelectorAll(u)), n;
            } catch (t) {
            } finally {
              a === F && e.removeAttribute('id');
            }
        }
      }
      return E(t.replace(at, '$1'), e, n, i);
    }
    function n() {
      function t(n, i) {
        return (
          e.push(n + ' ') > _.cacheLength && delete t[e.shift()],
          (t[n + ' '] = i)
        );
      }
      var e = [];
      return t;
    }
    function i(t) {
      return (t[F] = !0), t;
    }
    function o(t) {
      var e = L.createElement('fieldset');
      try {
        return !!t(e);
      } catch (t) {
        return !1;
      } finally {
        e.parentNode && e.parentNode.removeChild(e), (e = null);
      }
    }
    function r(t, e) {
      for (var n = t.split('|'), i = n.length; i--; ) _.attrHandle[n[i]] = e;
    }
    function s(t, e) {
      var n = e && t,
        i =
          n &&
          1 === t.nodeType &&
          1 === e.nodeType &&
          t.sourceIndex - e.sourceIndex;
      if (i) return i;
      if (n) for (; (n = n.nextSibling); ) if (n === e) return -1;
      return t ? 1 : -1;
    }
    function a(t) {
      return function (e) {
        return 'input' === e.nodeName.toLowerCase() && e.type === t;
      };
    }
    function l(t) {
      return function (e) {
        var n = e.nodeName.toLowerCase();
        return ('input' === n || 'button' === n) && e.type === t;
      };
    }
    function c(t) {
      return function (e) {
        return 'form' in e
          ? e.parentNode && !1 === e.disabled
            ? 'label' in e
              ? 'label' in e.parentNode
                ? e.parentNode.disabled === t
                : e.disabled === t
              : e.isDisabled === t || (e.isDisabled !== !t && Tt(e) === t)
            : e.disabled === t
          : 'label' in e && e.disabled === t;
      };
    }
    function u(t) {
      return i(function (e) {
        return (
          (e = +e),
          i(function (n, i) {
            for (var o, r = t([], n.length, e), s = r.length; s--; )
              n[(o = r[s])] && (n[o] = !(i[o] = n[o]));
          })
        );
      });
    }
    function d(t) {
      return t && void 0 !== t.getElementsByTagName && t;
    }
    function p() {}
    function h(t) {
      for (var e = 0, n = t.length, i = ''; e < n; e++) i += t[e].value;
      return i;
    }
    function f(t, e, n) {
      var i = e.dir,
        o = e.next,
        r = o || i,
        s = n && 'parentNode' === r,
        a = B++;
      return e.first
        ? function (e, n, o) {
            for (; (e = e[i]); ) if (1 === e.nodeType || s) return t(e, n, o);
            return !1;
          }
        : function (e, n, l) {
            var c,
              u,
              d,
              p = [R, a];
            if (l) {
              for (; (e = e[i]); )
                if ((1 === e.nodeType || s) && t(e, n, l)) return !0;
            } else
              for (; (e = e[i]); )
                if (1 === e.nodeType || s)
                  if (
                    ((d = e[F] || (e[F] = {})),
                    (u = d[e.uniqueID] || (d[e.uniqueID] = {})),
                    o && o === e.nodeName.toLowerCase())
                  )
                    e = e[i] || e;
                  else {
                    if ((c = u[r]) && c[0] === R && c[1] === a)
                      return (p[2] = c[2]);
                    if (((u[r] = p), (p[2] = t(e, n, l)))) return !0;
                  }
            return !1;
          };
    }
    function g(t) {
      return t.length > 1
        ? function (e, n, i) {
            for (var o = t.length; o--; ) if (!t[o](e, n, i)) return !1;
            return !0;
          }
        : t[0];
    }
    function m(t, n, i) {
      for (var o = 0, r = n.length; o < r; o++) e(t, n[o], i);
      return i;
    }
    function v(t, e, n, i, o) {
      for (var r, s = [], a = 0, l = t.length, c = null != e; a < l; a++)
        (r = t[a]) && ((n && !n(r, i, o)) || (s.push(r), c && e.push(a)));
      return s;
    }
    function y(t, e, n, o, r, s) {
      return (
        o && !o[F] && (o = y(o)),
        r && !r[F] && (r = y(r, s)),
        i(function (i, s, a, l) {
          var c,
            u,
            d,
            p = [],
            h = [],
            f = s.length,
            g = i || m(e || '*', a.nodeType ? [a] : a, []),
            y = !t || (!i && e) ? g : v(g, p, t, a, l),
            b = n ? (r || (i ? t : f || o) ? [] : s) : y;
          if ((n && n(y, b, a, l), o))
            for (c = v(b, h), o(c, [], a, l), u = c.length; u--; )
              (d = c[u]) && (b[h[u]] = !(y[h[u]] = d));
          if (i) {
            if (r || t) {
              if (r) {
                for (c = [], u = b.length; u--; )
                  (d = b[u]) && c.push((y[u] = d));
                r(null, (b = []), c, l);
              }
              for (u = b.length; u--; )
                (d = b[u]) &&
                  (c = r ? tt(i, d) : p[u]) > -1 &&
                  (i[c] = !(s[c] = d));
            }
          } else (b = v(b === s ? b.splice(f, b.length) : b)), r ? r(null, s, b, l) : K.apply(s, b);
        })
      );
    }
    function b(t) {
      for (
        var e,
          n,
          i,
          o = t.length,
          r = _.relative[t[0].type],
          s = r || _.relative[' '],
          a = r ? 1 : 0,
          l = f(
            function (t) {
              return t === e;
            },
            s,
            !0
          ),
          c = f(
            function (t) {
              return tt(e, t) > -1;
            },
            s,
            !0
          ),
          u = [
            function (t, n, i) {
              var o =
                (!r && (i || n !== P)) ||
                ((e = n).nodeType ? l(t, n, i) : c(t, n, i));
              return (e = null), o;
            },
          ];
        a < o;
        a++
      )
        if ((n = _.relative[t[a].type])) u = [f(g(u), n)];
        else {
          if ((n = _.filter[t[a].type].apply(null, t[a].matches))[F]) {
            for (i = ++a; i < o && !_.relative[t[i].type]; i++);
            return y(
              a > 1 && g(u),
              a > 1 &&
                h(
                  t
                    .slice(0, a - 1)
                    .concat({ value: ' ' === t[a - 2].type ? '*' : '' })
                ).replace(at, '$1'),
              n,
              a < i && b(t.slice(a, i)),
              i < o && b((t = t.slice(i))),
              i < o && h(t)
            );
          }
          u.push(n);
        }
      return g(u);
    }
    function x(t, n) {
      var o = n.length > 0,
        r = t.length > 0,
        s = function (i, s, a, l, c) {
          var u,
            d,
            p,
            h = 0,
            f = '0',
            g = i && [],
            m = [],
            y = P,
            b = i || (r && _.find.TAG('*', c)),
            x = (R += null == y ? 1 : Math.random() || 0.1),
            w = b.length;
          for (
            c && (P = s === L || s || c);
            f !== w && null != (u = b[f]);
            f++
          ) {
            if (r && u) {
              for (
                d = 0, s || u.ownerDocument === L || (M(u), (a = !j));
                (p = t[d++]);

              )
                if (p(u, s || L, a)) {
                  l.push(u);
                  break;
                }
              c && (R = x);
            }
            o && ((u = !p && u) && h--, i && g.push(u));
          }
          if (((h += f), o && f !== h)) {
            for (d = 0; (p = n[d++]); ) p(g, m, s, a);
            if (i) {
              if (h > 0) for (; f--; ) g[f] || m[f] || (m[f] = Q.call(l));
              m = v(m);
            }
            K.apply(l, m),
              c && !i && m.length > 0 && h + n.length > 1 && e.uniqueSort(l);
          }
          return c && ((R = x), (P = y)), g;
        };
      return o ? i(s) : s;
    }
    var w,
      C,
      _,
      T,
      k,
      S,
      $,
      E,
      P,
      A,
      I,
      M,
      L,
      D,
      j,
      z,
      O,
      H,
      N,
      F = 'sizzle' + 1 * new Date(),
      q = t.document,
      R = 0,
      B = 0,
      W = n(),
      X = n(),
      V = n(),
      Y = function (t, e) {
        return t === e && (I = !0), 0;
      },
      U = {}.hasOwnProperty,
      Z = [],
      Q = Z.pop,
      G = Z.push,
      K = Z.push,
      J = Z.slice,
      tt = function (t, e) {
        for (var n = 0, i = t.length; n < i; n++) if (t[n] === e) return n;
        return -1;
      },
      et =
        'checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped',
      nt = '[\\x20\\t\\r\\n\\f]',
      it = '(?:\\\\.|[\\w-]|[^\0-\\xa0])+',
      ot =
        '\\[' +
        nt +
        '*(' +
        it +
        ')(?:' +
        nt +
        '*([*^$|!~]?=)' +
        nt +
        '*(?:\'((?:\\\\.|[^\\\\\'])*)\'|"((?:\\\\.|[^\\\\"])*)"|(' +
        it +
        '))|)' +
        nt +
        '*\\]',
      rt =
        ':(' +
        it +
        ')(?:\\(((\'((?:\\\\.|[^\\\\\'])*)\'|"((?:\\\\.|[^\\\\"])*)")|((?:\\\\.|[^\\\\()[\\]]|' +
        ot +
        ')*)|.*)\\)|)',
      st = new RegExp(nt + '+', 'g'),
      at = new RegExp(
        '^' + nt + '+|((?:^|[^\\\\])(?:\\\\.)*)' + nt + '+$',
        'g'
      ),
      lt = new RegExp('^' + nt + '*,' + nt + '*'),
      ct = new RegExp('^' + nt + '*([>+~]|' + nt + ')' + nt + '*'),
      ut = new RegExp('=' + nt + '*([^\\]\'"]*?)' + nt + '*\\]', 'g'),
      dt = new RegExp(rt),
      pt = new RegExp('^' + it + '$'),
      ht = {
        ID: new RegExp('^#(' + it + ')'),
        CLASS: new RegExp('^\\.(' + it + ')'),
        TAG: new RegExp('^(' + it + '|[*])'),
        ATTR: new RegExp('^' + ot),
        PSEUDO: new RegExp('^' + rt),
        CHILD: new RegExp(
          '^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(' +
            nt +
            '*(even|odd|(([+-]|)(\\d*)n|)' +
            nt +
            '*(?:([+-]|)' +
            nt +
            '*(\\d+)|))' +
            nt +
            '*\\)|)',
          'i'
        ),
        bool: new RegExp('^(?:' + et + ')$', 'i'),
        needsContext: new RegExp(
          '^' +
            nt +
            '*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(' +
            nt +
            '*((?:-\\d)?\\d*)' +
            nt +
            '*\\)|)(?=[^-]|$)',
          'i'
        ),
      },
      ft = /^(?:input|select|textarea|button)$/i,
      gt = /^h\d$/i,
      mt = /^[^{]+\{\s*\[native \w/,
      vt = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/,
      yt = /[+~]/,
      bt = new RegExp('\\\\([\\da-f]{1,6}' + nt + '?|(' + nt + ')|.)', 'ig'),
      xt = function (t, e, n) {
        var i = '0x' + e - 65536;
        return i !== i || n
          ? e
          : i < 0
          ? String.fromCharCode(i + 65536)
          : String.fromCharCode((i >> 10) | 55296, (1023 & i) | 56320);
      },
      wt = /([\0-\x1f\x7f]|^-?\d)|^-$|[^\0-\x1f\x7f-\uFFFF\w-]/g,
      Ct = function (t, e) {
        return e
          ? '\0' === t
            ? '�'
            : t.slice(0, -1) +
              '\\' +
              t.charCodeAt(t.length - 1).toString(16) +
              ' '
          : '\\' + t;
      },
      _t = function () {
        M();
      },
      Tt = f(
        function (t) {
          return !0 === t.disabled && ('form' in t || 'label' in t);
        },
        { dir: 'parentNode', next: 'legend' }
      );
    try {
      K.apply((Z = J.call(q.childNodes)), q.childNodes),
        Z[q.childNodes.length].nodeType;
    } catch (t) {
      K = {
        apply: Z.length
          ? function (t, e) {
              G.apply(t, J.call(e));
            }
          : function (t, e) {
              for (var n = t.length, i = 0; (t[n++] = e[i++]); );
              t.length = n - 1;
            },
      };
    }
    (C = e.support = {}),
      (k = e.isXML =
        function (t) {
          var e = t && (t.ownerDocument || t).documentElement;
          return !!e && 'HTML' !== e.nodeName;
        }),
      (M = e.setDocument =
        function (t) {
          var e,
            n,
            i = t ? t.ownerDocument || t : q;
          return i !== L && 9 === i.nodeType && i.documentElement
            ? ((L = i),
              (D = L.documentElement),
              (j = !k(L)),
              q !== L &&
                (n = L.defaultView) &&
                n.top !== n &&
                (n.addEventListener
                  ? n.addEventListener('unload', _t, !1)
                  : n.attachEvent && n.attachEvent('onunload', _t)),
              (C.attributes = o(function (t) {
                return (t.className = 'i'), !t.getAttribute('className');
              })),
              (C.getElementsByTagName = o(function (t) {
                return (
                  t.appendChild(L.createComment('')),
                  !t.getElementsByTagName('*').length
                );
              })),
              (C.getElementsByClassName = mt.test(L.getElementsByClassName)),
              (C.getById = o(function (t) {
                return (
                  (D.appendChild(t).id = F),
                  !L.getElementsByName || !L.getElementsByName(F).length
                );
              })),
              C.getById
                ? ((_.filter.ID = function (t) {
                    var e = t.replace(bt, xt);
                    return function (t) {
                      return t.getAttribute('id') === e;
                    };
                  }),
                  (_.find.ID = function (t, e) {
                    if (void 0 !== e.getElementById && j) {
                      var n = e.getElementById(t);
                      return n ? [n] : [];
                    }
                  }))
                : ((_.filter.ID = function (t) {
                    var e = t.replace(bt, xt);
                    return function (t) {
                      var n =
                        void 0 !== t.getAttributeNode &&
                        t.getAttributeNode('id');
                      return n && n.value === e;
                    };
                  }),
                  (_.find.ID = function (t, e) {
                    if (void 0 !== e.getElementById && j) {
                      var n,
                        i,
                        o,
                        r = e.getElementById(t);
                      if (r) {
                        if ((n = r.getAttributeNode('id')) && n.value === t)
                          return [r];
                        for (o = e.getElementsByName(t), i = 0; (r = o[i++]); )
                          if ((n = r.getAttributeNode('id')) && n.value === t)
                            return [r];
                      }
                      return [];
                    }
                  })),
              (_.find.TAG = C.getElementsByTagName
                ? function (t, e) {
                    return void 0 !== e.getElementsByTagName
                      ? e.getElementsByTagName(t)
                      : C.qsa
                      ? e.querySelectorAll(t)
                      : void 0;
                  }
                : function (t, e) {
                    var n,
                      i = [],
                      o = 0,
                      r = e.getElementsByTagName(t);
                    if ('*' === t) {
                      for (; (n = r[o++]); ) 1 === n.nodeType && i.push(n);
                      return i;
                    }
                    return r;
                  }),
              (_.find.CLASS =
                C.getElementsByClassName &&
                function (t, e) {
                  if (void 0 !== e.getElementsByClassName && j)
                    return e.getElementsByClassName(t);
                }),
              (O = []),
              (z = []),
              (C.qsa = mt.test(L.querySelectorAll)) &&
                (o(function (t) {
                  (D.appendChild(t).innerHTML =
                    "<a id='" +
                    F +
                    "'></a><select id='" +
                    F +
                    "-\r\\' msallowcapture=''><option selected=''></option></select>"),
                    t.querySelectorAll("[msallowcapture^='']").length &&
                      z.push('[*^$]=' + nt + '*(?:\'\'|"")'),
                    t.querySelectorAll('[selected]').length ||
                      z.push('\\[' + nt + '*(?:value|' + et + ')'),
                    t.querySelectorAll('[id~=' + F + '-]').length ||
                      z.push('~='),
                    t.querySelectorAll(':checked').length || z.push(':checked'),
                    t.querySelectorAll('a#' + F + '+*').length ||
                      z.push('.#.+[+~]');
                }),
                o(function (t) {
                  t.innerHTML =
                    "<a href='' disabled='disabled'></a><select disabled='disabled'><option/></select>";
                  var e = L.createElement('input');
                  e.setAttribute('type', 'hidden'),
                    t.appendChild(e).setAttribute('name', 'D'),
                    t.querySelectorAll('[name=d]').length &&
                      z.push('name' + nt + '*[*^$|!~]?='),
                    2 !== t.querySelectorAll(':enabled').length &&
                      z.push(':enabled', ':disabled'),
                    (D.appendChild(t).disabled = !0),
                    2 !== t.querySelectorAll(':disabled').length &&
                      z.push(':enabled', ':disabled'),
                    t.querySelectorAll('*,:x'),
                    z.push(',.*:');
                })),
              (C.matchesSelector = mt.test(
                (H =
                  D.matches ||
                  D.webkitMatchesSelector ||
                  D.mozMatchesSelector ||
                  D.oMatchesSelector ||
                  D.msMatchesSelector)
              )) &&
                o(function (t) {
                  (C.disconnectedMatch = H.call(t, '*')),
                    H.call(t, "[s!='']:x"),
                    O.push('!=', rt);
                }),
              (z = z.length && new RegExp(z.join('|'))),
              (O = O.length && new RegExp(O.join('|'))),
              (e = mt.test(D.compareDocumentPosition)),
              (N =
                e || mt.test(D.contains)
                  ? function (t, e) {
                      var n = 9 === t.nodeType ? t.documentElement : t,
                        i = e && e.parentNode;
                      return (
                        t === i ||
                        !(
                          !i ||
                          1 !== i.nodeType ||
                          !(n.contains
                            ? n.contains(i)
                            : t.compareDocumentPosition &&
                              16 & t.compareDocumentPosition(i))
                        )
                      );
                    }
                  : function (t, e) {
                      if (e)
                        for (; (e = e.parentNode); ) if (e === t) return !0;
                      return !1;
                    }),
              (Y = e
                ? function (t, e) {
                    if (t === e) return (I = !0), 0;
                    var n =
                      !t.compareDocumentPosition - !e.compareDocumentPosition;
                    return (
                      n ||
                      (1 &
                        (n =
                          (t.ownerDocument || t) === (e.ownerDocument || e)
                            ? t.compareDocumentPosition(e)
                            : 1) ||
                      (!C.sortDetached && e.compareDocumentPosition(t) === n)
                        ? t === L || (t.ownerDocument === q && N(q, t))
                          ? -1
                          : e === L || (e.ownerDocument === q && N(q, e))
                          ? 1
                          : A
                          ? tt(A, t) - tt(A, e)
                          : 0
                        : 4 & n
                        ? -1
                        : 1)
                    );
                  }
                : function (t, e) {
                    if (t === e) return (I = !0), 0;
                    var n,
                      i = 0,
                      o = t.parentNode,
                      r = e.parentNode,
                      a = [t],
                      l = [e];
                    if (!o || !r)
                      return t === L
                        ? -1
                        : e === L
                        ? 1
                        : o
                        ? -1
                        : r
                        ? 1
                        : A
                        ? tt(A, t) - tt(A, e)
                        : 0;
                    if (o === r) return s(t, e);
                    for (n = t; (n = n.parentNode); ) a.unshift(n);
                    for (n = e; (n = n.parentNode); ) l.unshift(n);
                    for (; a[i] === l[i]; ) i++;
                    return i
                      ? s(a[i], l[i])
                      : a[i] === q
                      ? -1
                      : l[i] === q
                      ? 1
                      : 0;
                  }),
              L)
            : L;
        }),
      (e.matches = function (t, n) {
        return e(t, null, null, n);
      }),
      (e.matchesSelector = function (t, n) {
        if (
          ((t.ownerDocument || t) !== L && M(t),
          (n = n.replace(ut, "='$1']")),
          C.matchesSelector &&
            j &&
            !V[n + ' '] &&
            (!O || !O.test(n)) &&
            (!z || !z.test(n)))
        )
          try {
            var i = H.call(t, n);
            if (
              i ||
              C.disconnectedMatch ||
              (t.document && 11 !== t.document.nodeType)
            )
              return i;
          } catch (t) {}
        return e(n, L, null, [t]).length > 0;
      }),
      (e.contains = function (t, e) {
        return (t.ownerDocument || t) !== L && M(t), N(t, e);
      }),
      (e.attr = function (t, e) {
        (t.ownerDocument || t) !== L && M(t);
        var n = _.attrHandle[e.toLowerCase()],
          i = n && U.call(_.attrHandle, e.toLowerCase()) ? n(t, e, !j) : void 0;
        return void 0 !== i
          ? i
          : C.attributes || !j
          ? t.getAttribute(e)
          : (i = t.getAttributeNode(e)) && i.specified
          ? i.value
          : null;
      }),
      (e.escape = function (t) {
        return (t + '').replace(wt, Ct);
      }),
      (e.error = function (t) {
        throw new Error('Syntax error, unrecognized expression: ' + t);
      }),
      (e.uniqueSort = function (t) {
        var e,
          n = [],
          i = 0,
          o = 0;
        if (
          ((I = !C.detectDuplicates),
          (A = !C.sortStable && t.slice(0)),
          t.sort(Y),
          I)
        ) {
          for (; (e = t[o++]); ) e === t[o] && (i = n.push(o));
          for (; i--; ) t.splice(n[i], 1);
        }
        return (A = null), t;
      }),
      (T = e.getText =
        function (t) {
          var e,
            n = '',
            i = 0,
            o = t.nodeType;
          if (o) {
            if (1 === o || 9 === o || 11 === o) {
              if ('string' == typeof t.textContent) return t.textContent;
              for (t = t.firstChild; t; t = t.nextSibling) n += T(t);
            } else if (3 === o || 4 === o) return t.nodeValue;
          } else for (; (e = t[i++]); ) n += T(e);
          return n;
        }),
      ((_ = e.selectors =
        {
          cacheLength: 50,
          createPseudo: i,
          match: ht,
          attrHandle: {},
          find: {},
          relative: {
            '>': { dir: 'parentNode', first: !0 },
            ' ': { dir: 'parentNode' },
            '+': { dir: 'previousSibling', first: !0 },
            '~': { dir: 'previousSibling' },
          },
          preFilter: {
            ATTR: function (t) {
              return (
                (t[1] = t[1].replace(bt, xt)),
                (t[3] = (t[3] || t[4] || t[5] || '').replace(bt, xt)),
                '~=' === t[2] && (t[3] = ' ' + t[3] + ' '),
                t.slice(0, 4)
              );
            },
            CHILD: function (t) {
              return (
                (t[1] = t[1].toLowerCase()),
                'nth' === t[1].slice(0, 3)
                  ? (t[3] || e.error(t[0]),
                    (t[4] = +(t[4]
                      ? t[5] + (t[6] || 1)
                      : 2 * ('even' === t[3] || 'odd' === t[3]))),
                    (t[5] = +(t[7] + t[8] || 'odd' === t[3])))
                  : t[3] && e.error(t[0]),
                t
              );
            },
            PSEUDO: function (t) {
              var e,
                n = !t[6] && t[2];
              return ht.CHILD.test(t[0])
                ? null
                : (t[3]
                    ? (t[2] = t[4] || t[5] || '')
                    : n &&
                      dt.test(n) &&
                      (e = S(n, !0)) &&
                      (e = n.indexOf(')', n.length - e) - n.length) &&
                      ((t[0] = t[0].slice(0, e)), (t[2] = n.slice(0, e))),
                  t.slice(0, 3));
            },
          },
          filter: {
            TAG: function (t) {
              var e = t.replace(bt, xt).toLowerCase();
              return '*' === t
                ? function () {
                    return !0;
                  }
                : function (t) {
                    return t.nodeName && t.nodeName.toLowerCase() === e;
                  };
            },
            CLASS: function (t) {
              var e = W[t + ' '];
              return (
                e ||
                ((e = new RegExp('(^|' + nt + ')' + t + '(' + nt + '|$)')) &&
                  W(t, function (t) {
                    return e.test(
                      ('string' == typeof t.className && t.className) ||
                        (void 0 !== t.getAttribute &&
                          t.getAttribute('class')) ||
                        ''
                    );
                  }))
              );
            },
            ATTR: function (t, n, i) {
              return function (o) {
                var r = e.attr(o, t);
                return null == r
                  ? '!=' === n
                  : !n ||
                      ((r += ''),
                      '=' === n
                        ? r === i
                        : '!=' === n
                        ? r !== i
                        : '^=' === n
                        ? i && 0 === r.indexOf(i)
                        : '*=' === n
                        ? i && r.indexOf(i) > -1
                        : '$=' === n
                        ? i && r.slice(-i.length) === i
                        : '~=' === n
                        ? (' ' + r.replace(st, ' ') + ' ').indexOf(i) > -1
                        : '|=' === n &&
                          (r === i || r.slice(0, i.length + 1) === i + '-'));
              };
            },
            CHILD: function (t, e, n, i, o) {
              var r = 'nth' !== t.slice(0, 3),
                s = 'last' !== t.slice(-4),
                a = 'of-type' === e;
              return 1 === i && 0 === o
                ? function (t) {
                    return !!t.parentNode;
                  }
                : function (e, n, l) {
                    var c,
                      u,
                      d,
                      p,
                      h,
                      f,
                      g = r !== s ? 'nextSibling' : 'previousSibling',
                      m = e.parentNode,
                      v = a && e.nodeName.toLowerCase(),
                      y = !l && !a,
                      b = !1;
                    if (m) {
                      if (r) {
                        for (; g; ) {
                          for (p = e; (p = p[g]); )
                            if (
                              a
                                ? p.nodeName.toLowerCase() === v
                                : 1 === p.nodeType
                            )
                              return !1;
                          f = g = 'only' === t && !f && 'nextSibling';
                        }
                        return !0;
                      }
                      if (((f = [s ? m.firstChild : m.lastChild]), s && y)) {
                        for (
                          b =
                            (h =
                              (c =
                                (u =
                                  (d = (p = m)[F] || (p[F] = {}))[p.uniqueID] ||
                                  (d[p.uniqueID] = {}))[t] || [])[0] === R &&
                              c[1]) && c[2],
                            p = h && m.childNodes[h];
                          (p = (++h && p && p[g]) || (b = h = 0) || f.pop());

                        )
                          if (1 === p.nodeType && ++b && p === e) {
                            u[t] = [R, h, b];
                            break;
                          }
                      } else if (
                        (y &&
                          (b = h =
                            (c =
                              (u =
                                (d = (p = e)[F] || (p[F] = {}))[p.uniqueID] ||
                                (d[p.uniqueID] = {}))[t] || [])[0] === R &&
                            c[1]),
                        !1 === b)
                      )
                        for (
                          ;
                          (p = (++h && p && p[g]) || (b = h = 0) || f.pop()) &&
                          ((a
                            ? p.nodeName.toLowerCase() !== v
                            : 1 !== p.nodeType) ||
                            !++b ||
                            (y &&
                              ((u =
                                (d = p[F] || (p[F] = {}))[p.uniqueID] ||
                                (d[p.uniqueID] = {}))[t] = [R, b]),
                            p !== e));

                        );
                      return (b -= o) === i || (b % i == 0 && b / i >= 0);
                    }
                  };
            },
            PSEUDO: function (t, n) {
              var o,
                r =
                  _.pseudos[t] ||
                  _.setFilters[t.toLowerCase()] ||
                  e.error('unsupported pseudo: ' + t);
              return r[F]
                ? r(n)
                : r.length > 1
                ? ((o = [t, t, '', n]),
                  _.setFilters.hasOwnProperty(t.toLowerCase())
                    ? i(function (t, e) {
                        for (var i, o = r(t, n), s = o.length; s--; )
                          t[(i = tt(t, o[s]))] = !(e[i] = o[s]);
                      })
                    : function (t) {
                        return r(t, 0, o);
                      })
                : r;
            },
          },
          pseudos: {
            not: i(function (t) {
              var e = [],
                n = [],
                o = $(t.replace(at, '$1'));
              return o[F]
                ? i(function (t, e, n, i) {
                    for (var r, s = o(t, null, i, []), a = t.length; a--; )
                      (r = s[a]) && (t[a] = !(e[a] = r));
                  })
                : function (t, i, r) {
                    return (
                      (e[0] = t), o(e, null, r, n), (e[0] = null), !n.pop()
                    );
                  };
            }),
            has: i(function (t) {
              return function (n) {
                return e(t, n).length > 0;
              };
            }),
            contains: i(function (t) {
              return (
                (t = t.replace(bt, xt)),
                function (e) {
                  return (e.textContent || e.innerText || T(e)).indexOf(t) > -1;
                }
              );
            }),
            lang: i(function (t) {
              return (
                pt.test(t || '') || e.error('unsupported lang: ' + t),
                (t = t.replace(bt, xt).toLowerCase()),
                function (e) {
                  var n;
                  do {
                    if (
                      (n = j
                        ? e.lang
                        : e.getAttribute('xml:lang') || e.getAttribute('lang'))
                    )
                      return (
                        (n = n.toLowerCase()) === t || 0 === n.indexOf(t + '-')
                      );
                  } while ((e = e.parentNode) && 1 === e.nodeType);
                  return !1;
                }
              );
            }),
            target: function (e) {
              var n = t.location && t.location.hash;
              return n && n.slice(1) === e.id;
            },
            root: function (t) {
              return t === D;
            },
            focus: function (t) {
              return (
                t === L.activeElement &&
                (!L.hasFocus || L.hasFocus()) &&
                !!(t.type || t.href || ~t.tabIndex)
              );
            },
            enabled: c(!1),
            disabled: c(!0),
            checked: function (t) {
              var e = t.nodeName.toLowerCase();
              return (
                ('input' === e && !!t.checked) ||
                ('option' === e && !!t.selected)
              );
            },
            selected: function (t) {
              return (
                t.parentNode && t.parentNode.selectedIndex, !0 === t.selected
              );
            },
            empty: function (t) {
              for (t = t.firstChild; t; t = t.nextSibling)
                if (t.nodeType < 6) return !1;
              return !0;
            },
            parent: function (t) {
              return !_.pseudos.empty(t);
            },
            header: function (t) {
              return gt.test(t.nodeName);
            },
            input: function (t) {
              return ft.test(t.nodeName);
            },
            button: function (t) {
              var e = t.nodeName.toLowerCase();
              return ('input' === e && 'button' === t.type) || 'button' === e;
            },
            text: function (t) {
              var e;
              return (
                'input' === t.nodeName.toLowerCase() &&
                'text' === t.type &&
                (null == (e = t.getAttribute('type')) ||
                  'text' === e.toLowerCase())
              );
            },
            first: u(function () {
              return [0];
            }),
            last: u(function (t, e) {
              return [e - 1];
            }),
            eq: u(function (t, e, n) {
              return [n < 0 ? n + e : n];
            }),
            even: u(function (t, e) {
              for (var n = 0; n < e; n += 2) t.push(n);
              return t;
            }),
            odd: u(function (t, e) {
              for (var n = 1; n < e; n += 2) t.push(n);
              return t;
            }),
            lt: u(function (t, e, n) {
              for (var i = n < 0 ? n + e : n; --i >= 0; ) t.push(i);
              return t;
            }),
            gt: u(function (t, e, n) {
              for (var i = n < 0 ? n + e : n; ++i < e; ) t.push(i);
              return t;
            }),
          },
        }).pseudos.nth = _.pseudos.eq);
    for (w in { radio: !0, checkbox: !0, file: !0, password: !0, image: !0 })
      _.pseudos[w] = a(w);
    for (w in { submit: !0, reset: !0 }) _.pseudos[w] = l(w);
    return (
      (p.prototype = _.filters = _.pseudos),
      (_.setFilters = new p()),
      (S = e.tokenize =
        function (t, n) {
          var i,
            o,
            r,
            s,
            a,
            l,
            c,
            u = X[t + ' '];
          if (u) return n ? 0 : u.slice(0);
          for (a = t, l = [], c = _.preFilter; a; ) {
            (i && !(o = lt.exec(a))) ||
              (o && (a = a.slice(o[0].length) || a), l.push((r = []))),
              (i = !1),
              (o = ct.exec(a)) &&
                ((i = o.shift()),
                r.push({ value: i, type: o[0].replace(at, ' ') }),
                (a = a.slice(i.length)));
            for (s in _.filter)
              !(o = ht[s].exec(a)) ||
                (c[s] && !(o = c[s](o))) ||
                ((i = o.shift()),
                r.push({ value: i, type: s, matches: o }),
                (a = a.slice(i.length)));
            if (!i) break;
          }
          return n ? a.length : a ? e.error(t) : X(t, l).slice(0);
        }),
      ($ = e.compile =
        function (t, e) {
          var n,
            i = [],
            o = [],
            r = V[t + ' '];
          if (!r) {
            for (e || (e = S(t)), n = e.length; n--; )
              (r = b(e[n]))[F] ? i.push(r) : o.push(r);
            (r = V(t, x(o, i))).selector = t;
          }
          return r;
        }),
      (E = e.select =
        function (t, e, n, i) {
          var o,
            r,
            s,
            a,
            l,
            c = 'function' == typeof t && t,
            u = !i && S((t = c.selector || t));
          if (((n = n || []), 1 === u.length)) {
            if (
              (r = u[0] = u[0].slice(0)).length > 2 &&
              'ID' === (s = r[0]).type &&
              9 === e.nodeType &&
              j &&
              _.relative[r[1].type]
            ) {
              if (!(e = (_.find.ID(s.matches[0].replace(bt, xt), e) || [])[0]))
                return n;
              c && (e = e.parentNode), (t = t.slice(r.shift().value.length));
            }
            for (
              o = ht.needsContext.test(t) ? 0 : r.length;
              o-- && ((s = r[o]), !_.relative[(a = s.type)]);

            )
              if (
                (l = _.find[a]) &&
                (i = l(
                  s.matches[0].replace(bt, xt),
                  (yt.test(r[0].type) && d(e.parentNode)) || e
                ))
              ) {
                if ((r.splice(o, 1), !(t = i.length && h(r))))
                  return K.apply(n, i), n;
                break;
              }
          }
          return (
            (c || $(t, u))(
              i,
              e,
              !j,
              n,
              !e || (yt.test(t) && d(e.parentNode)) || e
            ),
            n
          );
        }),
      (C.sortStable = F.split('').sort(Y).join('') === F),
      (C.detectDuplicates = !!I),
      M(),
      (C.sortDetached = o(function (t) {
        return 1 & t.compareDocumentPosition(L.createElement('fieldset'));
      })),
      o(function (t) {
        return (
          (t.innerHTML = "<a href='#'></a>"),
          '#' === t.firstChild.getAttribute('href')
        );
      }) ||
        r('type|href|height|width', function (t, e, n) {
          if (!n) return t.getAttribute(e, 'type' === e.toLowerCase() ? 1 : 2);
        }),
      (C.attributes &&
        o(function (t) {
          return (
            (t.innerHTML = '<input/>'),
            t.firstChild.setAttribute('value', ''),
            '' === t.firstChild.getAttribute('value')
          );
        })) ||
        r('value', function (t, e, n) {
          if (!n && 'input' === t.nodeName.toLowerCase()) return t.defaultValue;
        }),
      o(function (t) {
        return null == t.getAttribute('disabled');
      }) ||
        r(et, function (t, e, n) {
          var i;
          if (!n)
            return !0 === t[e]
              ? e.toLowerCase()
              : (i = t.getAttributeNode(e)) && i.specified
              ? i.value
              : null;
        }),
      e
    );
  })(t);
  (wt.find = _t),
    (wt.expr = _t.selectors),
    (wt.expr[':'] = wt.expr.pseudos),
    (wt.uniqueSort = wt.unique = _t.uniqueSort),
    (wt.text = _t.getText),
    (wt.isXMLDoc = _t.isXML),
    (wt.contains = _t.contains),
    (wt.escapeSelector = _t.escape);
  var Tt = function (t, e, n) {
      for (var i = [], o = void 0 !== n; (t = t[e]) && 9 !== t.nodeType; )
        if (1 === t.nodeType) {
          if (o && wt(t).is(n)) break;
          i.push(t);
        }
      return i;
    },
    kt = function (t, e) {
      for (var n = []; t; t = t.nextSibling)
        1 === t.nodeType && t !== e && n.push(t);
      return n;
    },
    St = wt.expr.match.needsContext,
    $t = /^<([a-z][^\/\0>:\x20\t\r\n\f]*)[\x20\t\r\n\f]*\/?>(?:<\/\1>|)$/i;
  (wt.filter = function (t, e, n) {
    var i = e[0];
    return (
      n && (t = ':not(' + t + ')'),
      1 === e.length && 1 === i.nodeType
        ? wt.find.matchesSelector(i, t)
          ? [i]
          : []
        : wt.find.matches(
            t,
            wt.grep(e, function (t) {
              return 1 === t.nodeType;
            })
          )
    );
  }),
    wt.fn.extend({
      find: function (t) {
        var e,
          n,
          i = this.length,
          o = this;
        if ('string' != typeof t)
          return this.pushStack(
            wt(t).filter(function () {
              for (e = 0; e < i; e++) if (wt.contains(o[e], this)) return !0;
            })
          );
        for (n = this.pushStack([]), e = 0; e < i; e++) wt.find(t, o[e], n);
        return i > 1 ? wt.uniqueSort(n) : n;
      },
      filter: function (t) {
        return this.pushStack(s(this, t || [], !1));
      },
      not: function (t) {
        return this.pushStack(s(this, t || [], !0));
      },
      is: function (t) {
        return !!s(
          this,
          'string' == typeof t && St.test(t) ? wt(t) : t || [],
          !1
        ).length;
      },
    });
  var Et,
    Pt = /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]+))$/;
  ((wt.fn.init = function (t, e, n) {
    var i, o;
    if (!t) return this;
    if (((n = n || Et), 'string' == typeof t)) {
      if (
        !(i =
          '<' === t[0] && '>' === t[t.length - 1] && t.length >= 3
            ? [null, t, null]
            : Pt.exec(t)) ||
        (!i[1] && e)
      )
        return !e || e.jquery ? (e || n).find(t) : this.constructor(e).find(t);
      if (i[1]) {
        if (
          ((e = e instanceof wt ? e[0] : e),
          wt.merge(
            this,
            wt.parseHTML(i[1], e && e.nodeType ? e.ownerDocument || e : st, !0)
          ),
          $t.test(i[1]) && wt.isPlainObject(e))
        )
          for (i in e) yt(this[i]) ? this[i](e[i]) : this.attr(i, e[i]);
        return this;
      }
      return (
        (o = st.getElementById(i[2])) && ((this[0] = o), (this.length = 1)),
        this
      );
    }
    return t.nodeType
      ? ((this[0] = t), (this.length = 1), this)
      : yt(t)
      ? void 0 !== n.ready
        ? n.ready(t)
        : t(wt)
      : wt.makeArray(t, this);
  }).prototype = wt.fn),
    (Et = wt(st));
  var At = /^(?:parents|prev(?:Until|All))/,
    It = { children: !0, contents: !0, next: !0, prev: !0 };
  wt.fn.extend({
    has: function (t) {
      var e = wt(t, this),
        n = e.length;
      return this.filter(function () {
        for (var t = 0; t < n; t++) if (wt.contains(this, e[t])) return !0;
      });
    },
    closest: function (t, e) {
      var n,
        i = 0,
        o = this.length,
        r = [],
        s = 'string' != typeof t && wt(t);
      if (!St.test(t))
        for (; i < o; i++)
          for (n = this[i]; n && n !== e; n = n.parentNode)
            if (
              n.nodeType < 11 &&
              (s
                ? s.index(n) > -1
                : 1 === n.nodeType && wt.find.matchesSelector(n, t))
            ) {
              r.push(n);
              break;
            }
      return this.pushStack(r.length > 1 ? wt.uniqueSort(r) : r);
    },
    index: function (t) {
      return t
        ? 'string' == typeof t
          ? dt.call(wt(t), this[0])
          : dt.call(this, t.jquery ? t[0] : t)
        : this[0] && this[0].parentNode
        ? this.first().prevAll().length
        : -1;
    },
    add: function (t, e) {
      return this.pushStack(wt.uniqueSort(wt.merge(this.get(), wt(t, e))));
    },
    addBack: function (t) {
      return this.add(null == t ? this.prevObject : this.prevObject.filter(t));
    },
  }),
    wt.each(
      {
        parent: function (t) {
          var e = t.parentNode;
          return e && 11 !== e.nodeType ? e : null;
        },
        parents: function (t) {
          return Tt(t, 'parentNode');
        },
        parentsUntil: function (t, e, n) {
          return Tt(t, 'parentNode', n);
        },
        next: function (t) {
          return a(t, 'nextSibling');
        },
        prev: function (t) {
          return a(t, 'previousSibling');
        },
        nextAll: function (t) {
          return Tt(t, 'nextSibling');
        },
        prevAll: function (t) {
          return Tt(t, 'previousSibling');
        },
        nextUntil: function (t, e, n) {
          return Tt(t, 'nextSibling', n);
        },
        prevUntil: function (t, e, n) {
          return Tt(t, 'previousSibling', n);
        },
        siblings: function (t) {
          return kt((t.parentNode || {}).firstChild, t);
        },
        children: function (t) {
          return kt(t.firstChild);
        },
        contents: function (t) {
          return r(t, 'iframe')
            ? t.contentDocument
            : (r(t, 'template') && (t = t.content || t),
              wt.merge([], t.childNodes));
        },
      },
      function (t, e) {
        wt.fn[t] = function (n, i) {
          var o = wt.map(this, e, n);
          return (
            'Until' !== t.slice(-5) && (i = n),
            i && 'string' == typeof i && (o = wt.filter(i, o)),
            this.length > 1 &&
              (It[t] || wt.uniqueSort(o), At.test(t) && o.reverse()),
            this.pushStack(o)
          );
        };
      }
    );
  var Mt = /[^\x20\t\r\n\f]+/g;
  (wt.Callbacks = function (t) {
    t = 'string' == typeof t ? l(t) : wt.extend({}, t);
    var e,
      n,
      o,
      r,
      s = [],
      a = [],
      c = -1,
      u = function () {
        for (r = r || t.once, o = e = !0; a.length; c = -1)
          for (n = a.shift(); ++c < s.length; )
            !1 === s[c].apply(n[0], n[1]) &&
              t.stopOnFalse &&
              ((c = s.length), (n = !1));
        t.memory || (n = !1), (e = !1), r && (s = n ? [] : '');
      },
      d = {
        add: function () {
          return (
            s &&
              (n && !e && ((c = s.length - 1), a.push(n)),
              (function e(n) {
                wt.each(n, function (n, o) {
                  yt(o)
                    ? (t.unique && d.has(o)) || s.push(o)
                    : o && o.length && 'string' !== i(o) && e(o);
                });
              })(arguments),
              n && !e && u()),
            this
          );
        },
        remove: function () {
          return (
            wt.each(arguments, function (t, e) {
              for (var n; (n = wt.inArray(e, s, n)) > -1; )
                s.splice(n, 1), n <= c && c--;
            }),
            this
          );
        },
        has: function (t) {
          return t ? wt.inArray(t, s) > -1 : s.length > 0;
        },
        empty: function () {
          return s && (s = []), this;
        },
        disable: function () {
          return (r = a = []), (s = n = ''), this;
        },
        disabled: function () {
          return !s;
        },
        lock: function () {
          return (r = a = []), n || e || (s = n = ''), this;
        },
        locked: function () {
          return !!r;
        },
        fireWith: function (t, n) {
          return (
            r ||
              ((n = [t, (n = n || []).slice ? n.slice() : n]),
              a.push(n),
              e || u()),
            this
          );
        },
        fire: function () {
          return d.fireWith(this, arguments), this;
        },
        fired: function () {
          return !!o;
        },
      };
    return d;
  }),
    wt.extend({
      Deferred: function (e) {
        var n = [
            [
              'notify',
              'progress',
              wt.Callbacks('memory'),
              wt.Callbacks('memory'),
              2,
            ],
            [
              'resolve',
              'done',
              wt.Callbacks('once memory'),
              wt.Callbacks('once memory'),
              0,
              'resolved',
            ],
            [
              'reject',
              'fail',
              wt.Callbacks('once memory'),
              wt.Callbacks('once memory'),
              1,
              'rejected',
            ],
          ],
          i = 'pending',
          o = {
            state: function () {
              return i;
            },
            always: function () {
              return r.done(arguments).fail(arguments), this;
            },
            catch: function (t) {
              return o.then(null, t);
            },
            pipe: function () {
              var t = arguments;
              return wt
                .Deferred(function (e) {
                  wt.each(n, function (n, i) {
                    var o = yt(t[i[4]]) && t[i[4]];
                    r[i[1]](function () {
                      var t = o && o.apply(this, arguments);
                      t && yt(t.promise)
                        ? t
                            .promise()
                            .progress(e.notify)
                            .done(e.resolve)
                            .fail(e.reject)
                        : e[i[0] + 'With'](this, o ? [t] : arguments);
                    });
                  }),
                    (t = null);
                })
                .promise();
            },
            then: function (e, i, o) {
              function r(e, n, i, o) {
                return function () {
                  var a = this,
                    l = arguments,
                    d = function () {
                      var t, d;
                      if (!(e < s)) {
                        if ((t = i.apply(a, l)) === n.promise())
                          throw new TypeError('Thenable self-resolution');
                        (d =
                          t &&
                          ('object' == typeof t || 'function' == typeof t) &&
                          t.then),
                          yt(d)
                            ? o
                              ? d.call(t, r(s, n, c, o), r(s, n, u, o))
                              : (s++,
                                d.call(
                                  t,
                                  r(s, n, c, o),
                                  r(s, n, u, o),
                                  r(s, n, c, n.notifyWith)
                                ))
                            : (i !== c && ((a = void 0), (l = [t])),
                              (o || n.resolveWith)(a, l));
                      }
                    },
                    p = o
                      ? d
                      : function () {
                          try {
                            d();
                          } catch (t) {
                            wt.Deferred.exceptionHook &&
                              wt.Deferred.exceptionHook(t, p.stackTrace),
                              e + 1 >= s &&
                                (i !== u && ((a = void 0), (l = [t])),
                                n.rejectWith(a, l));
                          }
                        };
                  e
                    ? p()
                    : (wt.Deferred.getStackHook &&
                        (p.stackTrace = wt.Deferred.getStackHook()),
                      t.setTimeout(p));
                };
              }
              var s = 0;
              return wt
                .Deferred(function (t) {
                  n[0][3].add(r(0, t, yt(o) ? o : c, t.notifyWith)),
                    n[1][3].add(r(0, t, yt(e) ? e : c)),
                    n[2][3].add(r(0, t, yt(i) ? i : u));
                })
                .promise();
            },
            promise: function (t) {
              return null != t ? wt.extend(t, o) : o;
            },
          },
          r = {};
        return (
          wt.each(n, function (t, e) {
            var s = e[2],
              a = e[5];
            (o[e[1]] = s.add),
              a &&
                s.add(
                  function () {
                    i = a;
                  },
                  n[3 - t][2].disable,
                  n[3 - t][3].disable,
                  n[0][2].lock,
                  n[0][3].lock
                ),
              s.add(e[3].fire),
              (r[e[0]] = function () {
                return (
                  r[e[0] + 'With'](this === r ? void 0 : this, arguments), this
                );
              }),
              (r[e[0] + 'With'] = s.fireWith);
          }),
          o.promise(r),
          e && e.call(r, r),
          r
        );
      },
      when: function (t) {
        var e = arguments.length,
          n = e,
          i = Array(n),
          o = lt.call(arguments),
          r = wt.Deferred(),
          s = function (t) {
            return function (n) {
              (i[t] = this),
                (o[t] = arguments.length > 1 ? lt.call(arguments) : n),
                --e || r.resolveWith(i, o);
            };
          };
        if (
          e <= 1 &&
          (d(t, r.done(s(n)).resolve, r.reject, !e),
          'pending' === r.state() || yt(o[n] && o[n].then))
        )
          return r.then();
        for (; n--; ) d(o[n], s(n), r.reject);
        return r.promise();
      },
    });
  var Lt = /^(Eval|Internal|Range|Reference|Syntax|Type|URI)Error$/;
  (wt.Deferred.exceptionHook = function (e, n) {
    t.console &&
      t.console.warn &&
      e &&
      Lt.test(e.name) &&
      t.console.warn('jQuery.Deferred exception: ' + e.message, e.stack, n);
  }),
    (wt.readyException = function (e) {
      t.setTimeout(function () {
        throw e;
      });
    });
  var Dt = wt.Deferred();
  (wt.fn.ready = function (t) {
    return (
      Dt.then(t).catch(function (t) {
        wt.readyException(t);
      }),
      this
    );
  }),
    wt.extend({
      isReady: !1,
      readyWait: 1,
      ready: function (t) {
        (!0 === t ? --wt.readyWait : wt.isReady) ||
          ((wt.isReady = !0),
          (!0 !== t && --wt.readyWait > 0) || Dt.resolveWith(st, [wt]));
      },
    }),
    (wt.ready.then = Dt.then),
    'complete' === st.readyState ||
    ('loading' !== st.readyState && !st.documentElement.doScroll)
      ? t.setTimeout(wt.ready)
      : (st.addEventListener('DOMContentLoaded', p),
        t.addEventListener('load', p));
  var jt = function (t, e, n, o, r, s, a) {
      var l = 0,
        c = t.length,
        u = null == n;
      if ('object' === i(n)) {
        r = !0;
        for (l in n) jt(t, e, l, n[l], !0, s, a);
      } else if (
        void 0 !== o &&
        ((r = !0),
        yt(o) || (a = !0),
        u &&
          (a
            ? (e.call(t, o), (e = null))
            : ((u = e),
              (e = function (t, e, n) {
                return u.call(wt(t), n);
              }))),
        e)
      )
        for (; l < c; l++) e(t[l], n, a ? o : o.call(t[l], l, e(t[l], n)));
      return r ? t : u ? e.call(t) : c ? e(t[0], n) : s;
    },
    zt = /^-ms-/,
    Ot = /-([a-z])/g,
    Ht = function (t) {
      return 1 === t.nodeType || 9 === t.nodeType || !+t.nodeType;
    };
  (g.uid = 1),
    (g.prototype = {
      cache: function (t) {
        var e = t[this.expando];
        return (
          e ||
            ((e = {}),
            Ht(t) &&
              (t.nodeType
                ? (t[this.expando] = e)
                : Object.defineProperty(t, this.expando, {
                    value: e,
                    configurable: !0,
                  }))),
          e
        );
      },
      set: function (t, e, n) {
        var i,
          o = this.cache(t);
        if ('string' == typeof e) o[f(e)] = n;
        else for (i in e) o[f(i)] = e[i];
        return o;
      },
      get: function (t, e) {
        return void 0 === e
          ? this.cache(t)
          : t[this.expando] && t[this.expando][f(e)];
      },
      access: function (t, e, n) {
        return void 0 === e || (e && 'string' == typeof e && void 0 === n)
          ? this.get(t, e)
          : (this.set(t, e, n), void 0 !== n ? n : e);
      },
      remove: function (t, e) {
        var n,
          i = t[this.expando];
        if (void 0 !== i) {
          if (void 0 !== e) {
            n = (e = Array.isArray(e)
              ? e.map(f)
              : (e = f(e)) in i
              ? [e]
              : e.match(Mt) || []).length;
            for (; n--; ) delete i[e[n]];
          }
          (void 0 === e || wt.isEmptyObject(i)) &&
            (t.nodeType ? (t[this.expando] = void 0) : delete t[this.expando]);
        }
      },
      hasData: function (t) {
        var e = t[this.expando];
        return void 0 !== e && !wt.isEmptyObject(e);
      },
    });
  var Nt = new g(),
    Ft = new g(),
    qt = /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/,
    Rt = /[A-Z]/g;
  wt.extend({
    hasData: function (t) {
      return Ft.hasData(t) || Nt.hasData(t);
    },
    data: function (t, e, n) {
      return Ft.access(t, e, n);
    },
    removeData: function (t, e) {
      Ft.remove(t, e);
    },
    _data: function (t, e, n) {
      return Nt.access(t, e, n);
    },
    _removeData: function (t, e) {
      Nt.remove(t, e);
    },
  }),
    wt.fn.extend({
      data: function (t, e) {
        var n,
          i,
          o,
          r = this[0],
          s = r && r.attributes;
        if (void 0 === t) {
          if (
            this.length &&
            ((o = Ft.get(r)), 1 === r.nodeType && !Nt.get(r, 'hasDataAttrs'))
          ) {
            for (n = s.length; n--; )
              s[n] &&
                0 === (i = s[n].name).indexOf('data-') &&
                ((i = f(i.slice(5))), v(r, i, o[i]));
            Nt.set(r, 'hasDataAttrs', !0);
          }
          return o;
        }
        return 'object' == typeof t
          ? this.each(function () {
              Ft.set(this, t);
            })
          : jt(
              this,
              function (e) {
                var n;
                if (r && void 0 === e) {
                  if (void 0 !== (n = Ft.get(r, t))) return n;
                  if (void 0 !== (n = v(r, t))) return n;
                } else
                  this.each(function () {
                    Ft.set(this, t, e);
                  });
              },
              null,
              e,
              arguments.length > 1,
              null,
              !0
            );
      },
      removeData: function (t) {
        return this.each(function () {
          Ft.remove(this, t);
        });
      },
    }),
    wt.extend({
      queue: function (t, e, n) {
        var i;
        if (t)
          return (
            (e = (e || 'fx') + 'queue'),
            (i = Nt.get(t, e)),
            n &&
              (!i || Array.isArray(n)
                ? (i = Nt.access(t, e, wt.makeArray(n)))
                : i.push(n)),
            i || []
          );
      },
      dequeue: function (t, e) {
        e = e || 'fx';
        var n = wt.queue(t, e),
          i = n.length,
          o = n.shift(),
          r = wt._queueHooks(t, e),
          s = function () {
            wt.dequeue(t, e);
          };
        'inprogress' === o && ((o = n.shift()), i--),
          o &&
            ('fx' === e && n.unshift('inprogress'),
            delete r.stop,
            o.call(t, s, r)),
          !i && r && r.empty.fire();
      },
      _queueHooks: function (t, e) {
        var n = e + 'queueHooks';
        return (
          Nt.get(t, n) ||
          Nt.access(t, n, {
            empty: wt.Callbacks('once memory').add(function () {
              Nt.remove(t, [e + 'queue', n]);
            }),
          })
        );
      },
    }),
    wt.fn.extend({
      queue: function (t, e) {
        var n = 2;
        return (
          'string' != typeof t && ((e = t), (t = 'fx'), n--),
          arguments.length < n
            ? wt.queue(this[0], t)
            : void 0 === e
            ? this
            : this.each(function () {
                var n = wt.queue(this, t, e);
                wt._queueHooks(this, t),
                  'fx' === t && 'inprogress' !== n[0] && wt.dequeue(this, t);
              })
        );
      },
      dequeue: function (t) {
        return this.each(function () {
          wt.dequeue(this, t);
        });
      },
      clearQueue: function (t) {
        return this.queue(t || 'fx', []);
      },
      promise: function (t, e) {
        var n,
          i = 1,
          o = wt.Deferred(),
          r = this,
          s = this.length,
          a = function () {
            --i || o.resolveWith(r, [r]);
          };
        for (
          'string' != typeof t && ((e = t), (t = void 0)), t = t || 'fx';
          s--;

        )
          (n = Nt.get(r[s], t + 'queueHooks')) &&
            n.empty &&
            (i++, n.empty.add(a));
        return a(), o.promise(e);
      },
    });
  var Bt = /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/.source,
    Wt = new RegExp('^(?:([+-])=|)(' + Bt + ')([a-z%]*)$', 'i'),
    Xt = ['Top', 'Right', 'Bottom', 'Left'],
    Vt = function (t, e) {
      return (
        'none' === (t = e || t).style.display ||
        ('' === t.style.display &&
          wt.contains(t.ownerDocument, t) &&
          'none' === wt.css(t, 'display'))
      );
    },
    Yt = function (t, e, n, i) {
      var o,
        r,
        s = {};
      for (r in e) (s[r] = t.style[r]), (t.style[r] = e[r]);
      o = n.apply(t, i || []);
      for (r in e) t.style[r] = s[r];
      return o;
    },
    Ut = {};
  wt.fn.extend({
    show: function () {
      return x(this, !0);
    },
    hide: function () {
      return x(this);
    },
    toggle: function (t) {
      return 'boolean' == typeof t
        ? t
          ? this.show()
          : this.hide()
        : this.each(function () {
            Vt(this) ? wt(this).show() : wt(this).hide();
          });
    },
  });
  var Zt = /^(?:checkbox|radio)$/i,
    Qt = /<([a-z][^\/\0>\x20\t\r\n\f]+)/i,
    Gt = /^$|^module$|\/(?:java|ecma)script/i,
    Kt = {
      option: [1, "<select multiple='multiple'>", '</select>'],
      thead: [1, '<table>', '</table>'],
      col: [2, '<table><colgroup>', '</colgroup></table>'],
      tr: [2, '<table><tbody>', '</tbody></table>'],
      td: [3, '<table><tbody><tr>', '</tr></tbody></table>'],
      _default: [0, '', ''],
    };
  (Kt.optgroup = Kt.option),
    (Kt.tbody = Kt.tfoot = Kt.colgroup = Kt.caption = Kt.thead),
    (Kt.th = Kt.td);
  var Jt = /<|&#?\w+;/;
  !(function () {
    var t = st.createDocumentFragment().appendChild(st.createElement('div')),
      e = st.createElement('input');
    e.setAttribute('type', 'radio'),
      e.setAttribute('checked', 'checked'),
      e.setAttribute('name', 't'),
      t.appendChild(e),
      (vt.checkClone = t.cloneNode(!0).cloneNode(!0).lastChild.checked),
      (t.innerHTML = '<textarea>x</textarea>'),
      (vt.noCloneChecked = !!t.cloneNode(!0).lastChild.defaultValue);
  })();
  var te = st.documentElement,
    ee = /^key/,
    ne = /^(?:mouse|pointer|contextmenu|drag|drop)|click/,
    ie = /^([^.]*)(?:\.(.+)|)/;
  (wt.event = {
    global: {},
    add: function (t, e, n, i, o) {
      var r,
        s,
        a,
        l,
        c,
        u,
        d,
        p,
        h,
        f,
        g,
        m = Nt.get(t);
      if (m)
        for (
          n.handler && ((n = (r = n).handler), (o = r.selector)),
            o && wt.find.matchesSelector(te, o),
            n.guid || (n.guid = wt.guid++),
            (l = m.events) || (l = m.events = {}),
            (s = m.handle) ||
              (s = m.handle =
                function (e) {
                  return void 0 !== wt && wt.event.triggered !== e.type
                    ? wt.event.dispatch.apply(t, arguments)
                    : void 0;
                }),
            c = (e = (e || '').match(Mt) || ['']).length;
          c--;

        )
          (h = g = (a = ie.exec(e[c]) || [])[1]),
            (f = (a[2] || '').split('.').sort()),
            h &&
              ((d = wt.event.special[h] || {}),
              (h = (o ? d.delegateType : d.bindType) || h),
              (d = wt.event.special[h] || {}),
              (u = wt.extend(
                {
                  type: h,
                  origType: g,
                  data: i,
                  handler: n,
                  guid: n.guid,
                  selector: o,
                  needsContext: o && wt.expr.match.needsContext.test(o),
                  namespace: f.join('.'),
                },
                r
              )),
              (p = l[h]) ||
                (((p = l[h] = []).delegateCount = 0),
                (d.setup && !1 !== d.setup.call(t, i, f, s)) ||
                  (t.addEventListener && t.addEventListener(h, s))),
              d.add &&
                (d.add.call(t, u), u.handler.guid || (u.handler.guid = n.guid)),
              o ? p.splice(p.delegateCount++, 0, u) : p.push(u),
              (wt.event.global[h] = !0));
    },
    remove: function (t, e, n, i, o) {
      var r,
        s,
        a,
        l,
        c,
        u,
        d,
        p,
        h,
        f,
        g,
        m = Nt.hasData(t) && Nt.get(t);
      if (m && (l = m.events)) {
        for (c = (e = (e || '').match(Mt) || ['']).length; c--; )
          if (
            ((a = ie.exec(e[c]) || []),
            (h = g = a[1]),
            (f = (a[2] || '').split('.').sort()),
            h)
          ) {
            for (
              d = wt.event.special[h] || {},
                p = l[(h = (i ? d.delegateType : d.bindType) || h)] || [],
                a =
                  a[2] &&
                  new RegExp('(^|\\.)' + f.join('\\.(?:.*\\.|)') + '(\\.|$)'),
                s = r = p.length;
              r--;

            )
              (u = p[r]),
                (!o && g !== u.origType) ||
                  (n && n.guid !== u.guid) ||
                  (a && !a.test(u.namespace)) ||
                  (i && i !== u.selector && ('**' !== i || !u.selector)) ||
                  (p.splice(r, 1),
                  u.selector && p.delegateCount--,
                  d.remove && d.remove.call(t, u));
            s &&
              !p.length &&
              ((d.teardown && !1 !== d.teardown.call(t, f, m.handle)) ||
                wt.removeEvent(t, h, m.handle),
              delete l[h]);
          } else for (h in l) wt.event.remove(t, h + e[c], n, i, !0);
        wt.isEmptyObject(l) && Nt.remove(t, 'handle events');
      }
    },
    dispatch: function (t) {
      var e,
        n,
        i,
        o,
        r,
        s,
        a = wt.event.fix(t),
        l = new Array(arguments.length),
        c = (Nt.get(this, 'events') || {})[a.type] || [],
        u = wt.event.special[a.type] || {};
      for (l[0] = a, e = 1; e < arguments.length; e++) l[e] = arguments[e];
      if (
        ((a.delegateTarget = this),
        !u.preDispatch || !1 !== u.preDispatch.call(this, a))
      ) {
        for (
          s = wt.event.handlers.call(this, a, c), e = 0;
          (o = s[e++]) && !a.isPropagationStopped();

        )
          for (
            a.currentTarget = o.elem, n = 0;
            (r = o.handlers[n++]) && !a.isImmediatePropagationStopped();

          )
            (a.rnamespace && !a.rnamespace.test(r.namespace)) ||
              ((a.handleObj = r),
              (a.data = r.data),
              void 0 !==
                (i = (
                  (wt.event.special[r.origType] || {}).handle || r.handler
                ).apply(o.elem, l)) &&
                !1 === (a.result = i) &&
                (a.preventDefault(), a.stopPropagation()));
        return u.postDispatch && u.postDispatch.call(this, a), a.result;
      }
    },
    handlers: function (t, e) {
      var n,
        i,
        o,
        r,
        s,
        a = [],
        l = e.delegateCount,
        c = t.target;
      if (l && c.nodeType && !('click' === t.type && t.button >= 1))
        for (; c !== this; c = c.parentNode || this)
          if (1 === c.nodeType && ('click' !== t.type || !0 !== c.disabled)) {
            for (r = [], s = {}, n = 0; n < l; n++)
              void 0 === s[(o = (i = e[n]).selector + ' ')] &&
                (s[o] = i.needsContext
                  ? wt(o, this).index(c) > -1
                  : wt.find(o, this, null, [c]).length),
                s[o] && r.push(i);
            r.length && a.push({ elem: c, handlers: r });
          }
      return (
        (c = this), l < e.length && a.push({ elem: c, handlers: e.slice(l) }), a
      );
    },
    addProp: function (t, e) {
      Object.defineProperty(wt.Event.prototype, t, {
        enumerable: !0,
        configurable: !0,
        get: yt(e)
          ? function () {
              if (this.originalEvent) return e(this.originalEvent);
            }
          : function () {
              if (this.originalEvent) return this.originalEvent[t];
            },
        set: function (e) {
          Object.defineProperty(this, t, {
            enumerable: !0,
            configurable: !0,
            writable: !0,
            value: e,
          });
        },
      });
    },
    fix: function (t) {
      return t[wt.expando] ? t : new wt.Event(t);
    },
    special: {
      load: { noBubble: !0 },
      focus: {
        trigger: function () {
          if (this !== S() && this.focus) return this.focus(), !1;
        },
        delegateType: 'focusin',
      },
      blur: {
        trigger: function () {
          if (this === S() && this.blur) return this.blur(), !1;
        },
        delegateType: 'focusout',
      },
      click: {
        trigger: function () {
          if ('checkbox' === this.type && this.click && r(this, 'input'))
            return this.click(), !1;
        },
        _default: function (t) {
          return r(t.target, 'a');
        },
      },
      beforeunload: {
        postDispatch: function (t) {
          void 0 !== t.result &&
            t.originalEvent &&
            (t.originalEvent.returnValue = t.result);
        },
      },
    },
  }),
    (wt.removeEvent = function (t, e, n) {
      t.removeEventListener && t.removeEventListener(e, n);
    }),
    (wt.Event = function (t, e) {
      if (!(this instanceof wt.Event)) return new wt.Event(t, e);
      t && t.type
        ? ((this.originalEvent = t),
          (this.type = t.type),
          (this.isDefaultPrevented =
            t.defaultPrevented ||
            (void 0 === t.defaultPrevented && !1 === t.returnValue)
              ? T
              : k),
          (this.target =
            t.target && 3 === t.target.nodeType
              ? t.target.parentNode
              : t.target),
          (this.currentTarget = t.currentTarget),
          (this.relatedTarget = t.relatedTarget))
        : (this.type = t),
        e && wt.extend(this, e),
        (this.timeStamp = (t && t.timeStamp) || Date.now()),
        (this[wt.expando] = !0);
    }),
    (wt.Event.prototype = {
      constructor: wt.Event,
      isDefaultPrevented: k,
      isPropagationStopped: k,
      isImmediatePropagationStopped: k,
      isSimulated: !1,
      preventDefault: function () {
        var t = this.originalEvent;
        (this.isDefaultPrevented = T),
          t && !this.isSimulated && t.preventDefault();
      },
      stopPropagation: function () {
        var t = this.originalEvent;
        (this.isPropagationStopped = T),
          t && !this.isSimulated && t.stopPropagation();
      },
      stopImmediatePropagation: function () {
        var t = this.originalEvent;
        (this.isImmediatePropagationStopped = T),
          t && !this.isSimulated && t.stopImmediatePropagation(),
          this.stopPropagation();
      },
    }),
    wt.each(
      {
        altKey: !0,
        bubbles: !0,
        cancelable: !0,
        changedTouches: !0,
        ctrlKey: !0,
        detail: !0,
        eventPhase: !0,
        metaKey: !0,
        pageX: !0,
        pageY: !0,
        shiftKey: !0,
        view: !0,
        char: !0,
        charCode: !0,
        key: !0,
        keyCode: !0,
        button: !0,
        buttons: !0,
        clientX: !0,
        clientY: !0,
        offsetX: !0,
        offsetY: !0,
        pointerId: !0,
        pointerType: !0,
        screenX: !0,
        screenY: !0,
        targetTouches: !0,
        toElement: !0,
        touches: !0,
        which: function (t) {
          var e = t.button;
          return null == t.which && ee.test(t.type)
            ? null != t.charCode
              ? t.charCode
              : t.keyCode
            : !t.which && void 0 !== e && ne.test(t.type)
            ? 1 & e
              ? 1
              : 2 & e
              ? 3
              : 4 & e
              ? 2
              : 0
            : t.which;
        },
      },
      wt.event.addProp
    ),
    wt.each(
      {
        mouseenter: 'mouseover',
        mouseleave: 'mouseout',
        pointerenter: 'pointerover',
        pointerleave: 'pointerout',
      },
      function (t, e) {
        wt.event.special[t] = {
          delegateType: e,
          bindType: e,
          handle: function (t) {
            var n,
              i = this,
              o = t.relatedTarget,
              r = t.handleObj;
            return (
              (o && (o === i || wt.contains(i, o))) ||
                ((t.type = r.origType),
                (n = r.handler.apply(this, arguments)),
                (t.type = e)),
              n
            );
          },
        };
      }
    ),
    wt.fn.extend({
      on: function (t, e, n, i) {
        return $(this, t, e, n, i);
      },
      one: function (t, e, n, i) {
        return $(this, t, e, n, i, 1);
      },
      off: function (t, e, n) {
        var i, o;
        if (t && t.preventDefault && t.handleObj)
          return (
            (i = t.handleObj),
            wt(t.delegateTarget).off(
              i.namespace ? i.origType + '.' + i.namespace : i.origType,
              i.selector,
              i.handler
            ),
            this
          );
        if ('object' == typeof t) {
          for (o in t) this.off(o, e, t[o]);
          return this;
        }
        return (
          (!1 !== e && 'function' != typeof e) || ((n = e), (e = void 0)),
          !1 === n && (n = k),
          this.each(function () {
            wt.event.remove(this, t, n, e);
          })
        );
      },
    });
  var oe =
      /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([a-z][^\/\0>\x20\t\r\n\f]*)[^>]*)\/>/gi,
    re = /<script|<style|<link/i,
    se = /checked\s*(?:[^=]|=\s*.checked.)/i,
    ae = /^\s*<!(?:\[CDATA\[|--)|(?:\]\]|--)>\s*$/g;
  wt.extend({
    htmlPrefilter: function (t) {
      return t.replace(oe, '<$1></$2>');
    },
    clone: function (t, e, n) {
      var i,
        o,
        r,
        s,
        a = t.cloneNode(!0),
        l = wt.contains(t.ownerDocument, t);
      if (
        !(
          vt.noCloneChecked ||
          (1 !== t.nodeType && 11 !== t.nodeType) ||
          wt.isXMLDoc(t)
        )
      )
        for (s = w(a), i = 0, o = (r = w(t)).length; i < o; i++) M(r[i], s[i]);
      if (e)
        if (n)
          for (r = r || w(t), s = s || w(a), i = 0, o = r.length; i < o; i++)
            I(r[i], s[i]);
        else I(t, a);
      return (s = w(a, 'script')).length > 0 && C(s, !l && w(t, 'script')), a;
    },
    cleanData: function (t) {
      for (var e, n, i, o = wt.event.special, r = 0; void 0 !== (n = t[r]); r++)
        if (Ht(n)) {
          if ((e = n[Nt.expando])) {
            if (e.events)
              for (i in e.events)
                o[i] ? wt.event.remove(n, i) : wt.removeEvent(n, i, e.handle);
            n[Nt.expando] = void 0;
          }
          n[Ft.expando] && (n[Ft.expando] = void 0);
        }
    },
  }),
    wt.fn.extend({
      detach: function (t) {
        return D(this, t, !0);
      },
      remove: function (t) {
        return D(this, t);
      },
      text: function (t) {
        return jt(
          this,
          function (t) {
            return void 0 === t
              ? wt.text(this)
              : this.empty().each(function () {
                  (1 !== this.nodeType &&
                    11 !== this.nodeType &&
                    9 !== this.nodeType) ||
                    (this.textContent = t);
                });
          },
          null,
          t,
          arguments.length
        );
      },
      append: function () {
        return L(this, arguments, function (t) {
          (1 !== this.nodeType &&
            11 !== this.nodeType &&
            9 !== this.nodeType) ||
            E(this, t).appendChild(t);
        });
      },
      prepend: function () {
        return L(this, arguments, function (t) {
          if (
            1 === this.nodeType ||
            11 === this.nodeType ||
            9 === this.nodeType
          ) {
            var e = E(this, t);
            e.insertBefore(t, e.firstChild);
          }
        });
      },
      before: function () {
        return L(this, arguments, function (t) {
          this.parentNode && this.parentNode.insertBefore(t, this);
        });
      },
      after: function () {
        return L(this, arguments, function (t) {
          this.parentNode && this.parentNode.insertBefore(t, this.nextSibling);
        });
      },
      empty: function () {
        for (var t, e = 0; null != (t = this[e]); e++)
          1 === t.nodeType && (wt.cleanData(w(t, !1)), (t.textContent = ''));
        return this;
      },
      clone: function (t, e) {
        return (
          (t = null != t && t),
          (e = null == e ? t : e),
          this.map(function () {
            return wt.clone(this, t, e);
          })
        );
      },
      html: function (t) {
        return jt(
          this,
          function (t) {
            var e = this[0] || {},
              n = 0,
              i = this.length;
            if (void 0 === t && 1 === e.nodeType) return e.innerHTML;
            if (
              'string' == typeof t &&
              !re.test(t) &&
              !Kt[(Qt.exec(t) || ['', ''])[1].toLowerCase()]
            ) {
              t = wt.htmlPrefilter(t);
              try {
                for (; n < i; n++)
                  1 === (e = this[n] || {}).nodeType &&
                    (wt.cleanData(w(e, !1)), (e.innerHTML = t));
                e = 0;
              } catch (t) {}
            }
            e && this.empty().append(t);
          },
          null,
          t,
          arguments.length
        );
      },
      replaceWith: function () {
        var t = [];
        return L(
          this,
          arguments,
          function (e) {
            var n = this.parentNode;
            wt.inArray(this, t) < 0 &&
              (wt.cleanData(w(this)), n && n.replaceChild(e, this));
          },
          t
        );
      },
    }),
    wt.each(
      {
        appendTo: 'append',
        prependTo: 'prepend',
        insertBefore: 'before',
        insertAfter: 'after',
        replaceAll: 'replaceWith',
      },
      function (t, e) {
        wt.fn[t] = function (t) {
          for (var n, i = [], o = wt(t), r = o.length - 1, s = 0; s <= r; s++)
            (n = s === r ? this : this.clone(!0)),
              wt(o[s])[e](n),
              ut.apply(i, n.get());
          return this.pushStack(i);
        };
      }
    );
  var le = new RegExp('^(' + Bt + ')(?!px)[a-z%]+$', 'i'),
    ce = function (e) {
      var n = e.ownerDocument.defaultView;
      return (n && n.opener) || (n = t), n.getComputedStyle(e);
    },
    ue = new RegExp(Xt.join('|'), 'i');
  !(function () {
    function e() {
      if (c) {
        (l.style.cssText =
          'position:absolute;left:-11111px;width:60px;margin-top:1px;padding:0;border:0'),
          (c.style.cssText =
            'position:relative;display:block;box-sizing:border-box;overflow:scroll;margin:auto;border:1px;padding:1px;width:60%;top:1%'),
          te.appendChild(l).appendChild(c);
        var e = t.getComputedStyle(c);
        (i = '1%' !== e.top),
          (a = 12 === n(e.marginLeft)),
          (c.style.right = '60%'),
          (s = 36 === n(e.right)),
          (o = 36 === n(e.width)),
          (c.style.position = 'absolute'),
          (r = 36 === c.offsetWidth || 'absolute'),
          te.removeChild(l),
          (c = null);
      }
    }
    function n(t) {
      return Math.round(parseFloat(t));
    }
    var i,
      o,
      r,
      s,
      a,
      l = st.createElement('div'),
      c = st.createElement('div');
    c.style &&
      ((c.style.backgroundClip = 'content-box'),
      (c.cloneNode(!0).style.backgroundClip = ''),
      (vt.clearCloneStyle = 'content-box' === c.style.backgroundClip),
      wt.extend(vt, {
        boxSizingReliable: function () {
          return e(), o;
        },
        pixelBoxStyles: function () {
          return e(), s;
        },
        pixelPosition: function () {
          return e(), i;
        },
        reliableMarginLeft: function () {
          return e(), a;
        },
        scrollboxSize: function () {
          return e(), r;
        },
      }));
  })();
  var de = /^(none|table(?!-c[ea]).+)/,
    pe = /^--/,
    he = { position: 'absolute', visibility: 'hidden', display: 'block' },
    fe = { letterSpacing: '0', fontWeight: '400' },
    ge = ['Webkit', 'Moz', 'ms'],
    me = st.createElement('div').style;
  wt.extend({
    cssHooks: {
      opacity: {
        get: function (t, e) {
          if (e) {
            var n = j(t, 'opacity');
            return '' === n ? '1' : n;
          }
        },
      },
    },
    cssNumber: {
      animationIterationCount: !0,
      columnCount: !0,
      fillOpacity: !0,
      flexGrow: !0,
      flexShrink: !0,
      fontWeight: !0,
      lineHeight: !0,
      opacity: !0,
      order: !0,
      orphans: !0,
      widows: !0,
      zIndex: !0,
      zoom: !0,
    },
    cssProps: {},
    style: function (t, e, n, i) {
      if (t && 3 !== t.nodeType && 8 !== t.nodeType && t.style) {
        var o,
          r,
          s,
          a = f(e),
          l = pe.test(e),
          c = t.style;
        if (
          (l || (e = H(a)),
          (s = wt.cssHooks[e] || wt.cssHooks[a]),
          void 0 === n)
        )
          return s && 'get' in s && void 0 !== (o = s.get(t, !1, i)) ? o : c[e];
        'string' == (r = typeof n) &&
          (o = Wt.exec(n)) &&
          o[1] &&
          ((n = y(t, e, o)), (r = 'number')),
          null != n &&
            n === n &&
            ('number' === r &&
              (n += (o && o[3]) || (wt.cssNumber[a] ? '' : 'px')),
            vt.clearCloneStyle ||
              '' !== n ||
              0 !== e.indexOf('background') ||
              (c[e] = 'inherit'),
            (s && 'set' in s && void 0 === (n = s.set(t, n, i))) ||
              (l ? c.setProperty(e, n) : (c[e] = n)));
      }
    },
    css: function (t, e, n, i) {
      var o,
        r,
        s,
        a = f(e);
      return (
        pe.test(e) || (e = H(a)),
        (s = wt.cssHooks[e] || wt.cssHooks[a]) &&
          'get' in s &&
          (o = s.get(t, !0, n)),
        void 0 === o && (o = j(t, e, i)),
        'normal' === o && e in fe && (o = fe[e]),
        '' === n || n
          ? ((r = parseFloat(o)), !0 === n || isFinite(r) ? r || 0 : o)
          : o
      );
    },
  }),
    wt.each(['height', 'width'], function (t, e) {
      wt.cssHooks[e] = {
        get: function (t, n, i) {
          if (n)
            return !de.test(wt.css(t, 'display')) ||
              (t.getClientRects().length && t.getBoundingClientRect().width)
              ? q(t, e, i)
              : Yt(t, he, function () {
                  return q(t, e, i);
                });
        },
        set: function (t, n, i) {
          var o,
            r = ce(t),
            s = 'border-box' === wt.css(t, 'boxSizing', !1, r),
            a = i && F(t, e, i, s, r);
          return (
            s &&
              vt.scrollboxSize() === r.position &&
              (a -= Math.ceil(
                t['offset' + e[0].toUpperCase() + e.slice(1)] -
                  parseFloat(r[e]) -
                  F(t, e, 'border', !1, r) -
                  0.5
              )),
            a &&
              (o = Wt.exec(n)) &&
              'px' !== (o[3] || 'px') &&
              ((t.style[e] = n), (n = wt.css(t, e))),
            N(t, n, a)
          );
        },
      };
    }),
    (wt.cssHooks.marginLeft = z(vt.reliableMarginLeft, function (t, e) {
      if (e)
        return (
          (parseFloat(j(t, 'marginLeft')) ||
            t.getBoundingClientRect().left -
              Yt(t, { marginLeft: 0 }, function () {
                return t.getBoundingClientRect().left;
              })) + 'px'
        );
    })),
    wt.each({ margin: '', padding: '', border: 'Width' }, function (t, e) {
      (wt.cssHooks[t + e] = {
        expand: function (n) {
          for (
            var i = 0, o = {}, r = 'string' == typeof n ? n.split(' ') : [n];
            i < 4;
            i++
          )
            o[t + Xt[i] + e] = r[i] || r[i - 2] || r[0];
          return o;
        },
      }),
        'margin' !== t && (wt.cssHooks[t + e].set = N);
    }),
    wt.fn.extend({
      css: function (t, e) {
        return jt(
          this,
          function (t, e, n) {
            var i,
              o,
              r = {},
              s = 0;
            if (Array.isArray(e)) {
              for (i = ce(t), o = e.length; s < o; s++)
                r[e[s]] = wt.css(t, e[s], !1, i);
              return r;
            }
            return void 0 !== n ? wt.style(t, e, n) : wt.css(t, e);
          },
          t,
          e,
          arguments.length > 1
        );
      },
    }),
    (wt.Tween = R),
    (R.prototype = {
      constructor: R,
      init: function (t, e, n, i, o, r) {
        (this.elem = t),
          (this.prop = n),
          (this.easing = o || wt.easing._default),
          (this.options = e),
          (this.start = this.now = this.cur()),
          (this.end = i),
          (this.unit = r || (wt.cssNumber[n] ? '' : 'px'));
      },
      cur: function () {
        var t = R.propHooks[this.prop];
        return t && t.get ? t.get(this) : R.propHooks._default.get(this);
      },
      run: function (t) {
        var e,
          n = R.propHooks[this.prop];
        return (
          this.options.duration
            ? (this.pos = e =
                wt.easing[this.easing](
                  t,
                  this.options.duration * t,
                  0,
                  1,
                  this.options.duration
                ))
            : (this.pos = e = t),
          (this.now = (this.end - this.start) * e + this.start),
          this.options.step &&
            this.options.step.call(this.elem, this.now, this),
          n && n.set ? n.set(this) : R.propHooks._default.set(this),
          this
        );
      },
    }),
    (R.prototype.init.prototype = R.prototype),
    (R.propHooks = {
      _default: {
        get: function (t) {
          var e;
          return 1 !== t.elem.nodeType ||
            (null != t.elem[t.prop] && null == t.elem.style[t.prop])
            ? t.elem[t.prop]
            : (e = wt.css(t.elem, t.prop, '')) && 'auto' !== e
            ? e
            : 0;
        },
        set: function (t) {
          wt.fx.step[t.prop]
            ? wt.fx.step[t.prop](t)
            : 1 !== t.elem.nodeType ||
              (null == t.elem.style[wt.cssProps[t.prop]] &&
                !wt.cssHooks[t.prop])
            ? (t.elem[t.prop] = t.now)
            : wt.style(t.elem, t.prop, t.now + t.unit);
        },
      },
    }),
    (R.propHooks.scrollTop = R.propHooks.scrollLeft =
      {
        set: function (t) {
          t.elem.nodeType && t.elem.parentNode && (t.elem[t.prop] = t.now);
        },
      }),
    (wt.easing = {
      linear: function (t) {
        return t;
      },
      swing: function (t) {
        return 0.5 - Math.cos(t * Math.PI) / 2;
      },
      _default: 'swing',
    }),
    (wt.fx = R.prototype.init),
    (wt.fx.step = {});
  var ve,
    ye,
    be = /^(?:toggle|show|hide)$/,
    xe = /queueHooks$/;
  (wt.Animation = wt.extend(Z, {
    tweeners: {
      '*': [
        function (t, e) {
          var n = this.createTween(t, e);
          return y(n.elem, t, Wt.exec(e), n), n;
        },
      ],
    },
    tweener: function (t, e) {
      yt(t) ? ((e = t), (t = ['*'])) : (t = t.match(Mt));
      for (var n, i = 0, o = t.length; i < o; i++)
        (n = t[i]),
          (Z.tweeners[n] = Z.tweeners[n] || []),
          Z.tweeners[n].unshift(e);
    },
    prefilters: [Y],
    prefilter: function (t, e) {
      e ? Z.prefilters.unshift(t) : Z.prefilters.push(t);
    },
  })),
    (wt.speed = function (t, e, n) {
      var i =
        t && 'object' == typeof t
          ? wt.extend({}, t)
          : {
              complete: n || (!n && e) || (yt(t) && t),
              duration: t,
              easing: (n && e) || (e && !yt(e) && e),
            };
      return (
        wt.fx.off
          ? (i.duration = 0)
          : 'number' != typeof i.duration &&
            (i.duration in wt.fx.speeds
              ? (i.duration = wt.fx.speeds[i.duration])
              : (i.duration = wt.fx.speeds._default)),
        (null != i.queue && !0 !== i.queue) || (i.queue = 'fx'),
        (i.old = i.complete),
        (i.complete = function () {
          yt(i.old) && i.old.call(this), i.queue && wt.dequeue(this, i.queue);
        }),
        i
      );
    }),
    wt.fn.extend({
      fadeTo: function (t, e, n, i) {
        return this.filter(Vt)
          .css('opacity', 0)
          .show()
          .end()
          .animate({ opacity: e }, t, n, i);
      },
      animate: function (t, e, n, i) {
        var o = wt.isEmptyObject(t),
          r = wt.speed(e, n, i),
          s = function () {
            var e = Z(this, wt.extend({}, t), r);
            (o || Nt.get(this, 'finish')) && e.stop(!0);
          };
        return (
          (s.finish = s),
          o || !1 === r.queue ? this.each(s) : this.queue(r.queue, s)
        );
      },
      stop: function (t, e, n) {
        var i = function (t) {
          var e = t.stop;
          delete t.stop, e(n);
        };
        return (
          'string' != typeof t && ((n = e), (e = t), (t = void 0)),
          e && !1 !== t && this.queue(t || 'fx', []),
          this.each(function () {
            var e = !0,
              o = null != t && t + 'queueHooks',
              r = wt.timers,
              s = Nt.get(this);
            if (o) s[o] && s[o].stop && i(s[o]);
            else for (o in s) s[o] && s[o].stop && xe.test(o) && i(s[o]);
            for (o = r.length; o--; )
              r[o].elem !== this ||
                (null != t && r[o].queue !== t) ||
                (r[o].anim.stop(n), (e = !1), r.splice(o, 1));
            (!e && n) || wt.dequeue(this, t);
          })
        );
      },
      finish: function (t) {
        return (
          !1 !== t && (t = t || 'fx'),
          this.each(function () {
            var e,
              n = Nt.get(this),
              i = n[t + 'queue'],
              o = n[t + 'queueHooks'],
              r = wt.timers,
              s = i ? i.length : 0;
            for (
              n.finish = !0,
                wt.queue(this, t, []),
                o && o.stop && o.stop.call(this, !0),
                e = r.length;
              e--;

            )
              r[e].elem === this &&
                r[e].queue === t &&
                (r[e].anim.stop(!0), r.splice(e, 1));
            for (e = 0; e < s; e++)
              i[e] && i[e].finish && i[e].finish.call(this);
            delete n.finish;
          })
        );
      },
    }),
    wt.each(['toggle', 'show', 'hide'], function (t, e) {
      var n = wt.fn[e];
      wt.fn[e] = function (t, i, o) {
        return null == t || 'boolean' == typeof t
          ? n.apply(this, arguments)
          : this.animate(X(e, !0), t, i, o);
      };
    }),
    wt.each(
      {
        slideDown: X('show'),
        slideUp: X('hide'),
        slideToggle: X('toggle'),
        fadeIn: { opacity: 'show' },
        fadeOut: { opacity: 'hide' },
        fadeToggle: { opacity: 'toggle' },
      },
      function (t, e) {
        wt.fn[t] = function (t, n, i) {
          return this.animate(e, t, n, i);
        };
      }
    ),
    (wt.timers = []),
    (wt.fx.tick = function () {
      var t,
        e = 0,
        n = wt.timers;
      for (ve = Date.now(); e < n.length; e++)
        (t = n[e])() || n[e] !== t || n.splice(e--, 1);
      n.length || wt.fx.stop(), (ve = void 0);
    }),
    (wt.fx.timer = function (t) {
      wt.timers.push(t), wt.fx.start();
    }),
    (wt.fx.interval = 13),
    (wt.fx.start = function () {
      ye || ((ye = !0), B());
    }),
    (wt.fx.stop = function () {
      ye = null;
    }),
    (wt.fx.speeds = { slow: 600, fast: 200, _default: 400 }),
    (wt.fn.delay = function (e, n) {
      return (
        (e = wt.fx ? wt.fx.speeds[e] || e : e),
        (n = n || 'fx'),
        this.queue(n, function (n, i) {
          var o = t.setTimeout(n, e);
          i.stop = function () {
            t.clearTimeout(o);
          };
        })
      );
    }),
    (function () {
      var t = st.createElement('input'),
        e = st.createElement('select').appendChild(st.createElement('option'));
      (t.type = 'checkbox'),
        (vt.checkOn = '' !== t.value),
        (vt.optSelected = e.selected),
        ((t = st.createElement('input')).value = 't'),
        (t.type = 'radio'),
        (vt.radioValue = 't' === t.value);
    })();
  var we,
    Ce = wt.expr.attrHandle;
  wt.fn.extend({
    attr: function (t, e) {
      return jt(this, wt.attr, t, e, arguments.length > 1);
    },
    removeAttr: function (t) {
      return this.each(function () {
        wt.removeAttr(this, t);
      });
    },
  }),
    wt.extend({
      attr: function (t, e, n) {
        var i,
          o,
          r = t.nodeType;
        if (3 !== r && 8 !== r && 2 !== r)
          return void 0 === t.getAttribute
            ? wt.prop(t, e, n)
            : ((1 === r && wt.isXMLDoc(t)) ||
                (o =
                  wt.attrHooks[e.toLowerCase()] ||
                  (wt.expr.match.bool.test(e) ? we : void 0)),
              void 0 !== n
                ? null === n
                  ? void wt.removeAttr(t, e)
                  : o && 'set' in o && void 0 !== (i = o.set(t, n, e))
                  ? i
                  : (t.setAttribute(e, n + ''), n)
                : o && 'get' in o && null !== (i = o.get(t, e))
                ? i
                : null == (i = wt.find.attr(t, e))
                ? void 0
                : i);
      },
      attrHooks: {
        type: {
          set: function (t, e) {
            if (!vt.radioValue && 'radio' === e && r(t, 'input')) {
              var n = t.value;
              return t.setAttribute('type', e), n && (t.value = n), e;
            }
          },
        },
      },
      removeAttr: function (t, e) {
        var n,
          i = 0,
          o = e && e.match(Mt);
        if (o && 1 === t.nodeType) for (; (n = o[i++]); ) t.removeAttribute(n);
      },
    }),
    (we = {
      set: function (t, e, n) {
        return !1 === e ? wt.removeAttr(t, n) : t.setAttribute(n, n), n;
      },
    }),
    wt.each(wt.expr.match.bool.source.match(/\w+/g), function (t, e) {
      var n = Ce[e] || wt.find.attr;
      Ce[e] = function (t, e, i) {
        var o,
          r,
          s = e.toLowerCase();
        return (
          i ||
            ((r = Ce[s]),
            (Ce[s] = o),
            (o = null != n(t, e, i) ? s : null),
            (Ce[s] = r)),
          o
        );
      };
    });
  var _e = /^(?:input|select|textarea|button)$/i,
    Te = /^(?:a|area)$/i;
  wt.fn.extend({
    prop: function (t, e) {
      return jt(this, wt.prop, t, e, arguments.length > 1);
    },
    removeProp: function (t) {
      return this.each(function () {
        delete this[wt.propFix[t] || t];
      });
    },
  }),
    wt.extend({
      prop: function (t, e, n) {
        var i,
          o,
          r = t.nodeType;
        if (3 !== r && 8 !== r && 2 !== r)
          return (
            (1 === r && wt.isXMLDoc(t)) ||
              ((e = wt.propFix[e] || e), (o = wt.propHooks[e])),
            void 0 !== n
              ? o && 'set' in o && void 0 !== (i = o.set(t, n, e))
                ? i
                : (t[e] = n)
              : o && 'get' in o && null !== (i = o.get(t, e))
              ? i
              : t[e]
          );
      },
      propHooks: {
        tabIndex: {
          get: function (t) {
            var e = wt.find.attr(t, 'tabindex');
            return e
              ? parseInt(e, 10)
              : _e.test(t.nodeName) || (Te.test(t.nodeName) && t.href)
              ? 0
              : -1;
          },
        },
      },
      propFix: { for: 'htmlFor', class: 'className' },
    }),
    vt.optSelected ||
      (wt.propHooks.selected = {
        get: function (t) {
          var e = t.parentNode;
          return e && e.parentNode && e.parentNode.selectedIndex, null;
        },
        set: function (t) {
          var e = t.parentNode;
          e && (e.selectedIndex, e.parentNode && e.parentNode.selectedIndex);
        },
      }),
    wt.each(
      [
        'tabIndex',
        'readOnly',
        'maxLength',
        'cellSpacing',
        'cellPadding',
        'rowSpan',
        'colSpan',
        'useMap',
        'frameBorder',
        'contentEditable',
      ],
      function () {
        wt.propFix[this.toLowerCase()] = this;
      }
    ),
    wt.fn.extend({
      addClass: function (t) {
        var e,
          n,
          i,
          o,
          r,
          s,
          a,
          l = 0;
        if (yt(t))
          return this.each(function (e) {
            wt(this).addClass(t.call(this, e, G(this)));
          });
        if ((e = K(t)).length)
          for (; (n = this[l++]); )
            if (((o = G(n)), (i = 1 === n.nodeType && ' ' + Q(o) + ' '))) {
              for (s = 0; (r = e[s++]); )
                i.indexOf(' ' + r + ' ') < 0 && (i += r + ' ');
              o !== (a = Q(i)) && n.setAttribute('class', a);
            }
        return this;
      },
      removeClass: function (t) {
        var e,
          n,
          i,
          o,
          r,
          s,
          a,
          l = 0;
        if (yt(t))
          return this.each(function (e) {
            wt(this).removeClass(t.call(this, e, G(this)));
          });
        if (!arguments.length) return this.attr('class', '');
        if ((e = K(t)).length)
          for (; (n = this[l++]); )
            if (((o = G(n)), (i = 1 === n.nodeType && ' ' + Q(o) + ' '))) {
              for (s = 0; (r = e[s++]); )
                for (; i.indexOf(' ' + r + ' ') > -1; )
                  i = i.replace(' ' + r + ' ', ' ');
              o !== (a = Q(i)) && n.setAttribute('class', a);
            }
        return this;
      },
      toggleClass: function (t, e) {
        var n = typeof t,
          i = 'string' === n || Array.isArray(t);
        return 'boolean' == typeof e && i
          ? e
            ? this.addClass(t)
            : this.removeClass(t)
          : yt(t)
          ? this.each(function (n) {
              wt(this).toggleClass(t.call(this, n, G(this), e), e);
            })
          : this.each(function () {
              var e, o, r, s;
              if (i)
                for (o = 0, r = wt(this), s = K(t); (e = s[o++]); )
                  r.hasClass(e) ? r.removeClass(e) : r.addClass(e);
              else
                (void 0 !== t && 'boolean' !== n) ||
                  ((e = G(this)) && Nt.set(this, '__className__', e),
                  this.setAttribute &&
                    this.setAttribute(
                      'class',
                      e || !1 === t ? '' : Nt.get(this, '__className__') || ''
                    ));
            });
      },
      hasClass: function (t) {
        var e,
          n,
          i = 0;
        for (e = ' ' + t + ' '; (n = this[i++]); )
          if (1 === n.nodeType && (' ' + Q(G(n)) + ' ').indexOf(e) > -1)
            return !0;
        return !1;
      },
    });
  var ke = /\r/g;
  wt.fn.extend({
    val: function (t) {
      var e,
        n,
        i,
        o = this[0];
      return arguments.length
        ? ((i = yt(t)),
          this.each(function (n) {
            var o;
            1 === this.nodeType &&
              (null == (o = i ? t.call(this, n, wt(this).val()) : t)
                ? (o = '')
                : 'number' == typeof o
                ? (o += '')
                : Array.isArray(o) &&
                  (o = wt.map(o, function (t) {
                    return null == t ? '' : t + '';
                  })),
              ((e =
                wt.valHooks[this.type] ||
                wt.valHooks[this.nodeName.toLowerCase()]) &&
                'set' in e &&
                void 0 !== e.set(this, o, 'value')) ||
                (this.value = o));
          }))
        : o
        ? (e = wt.valHooks[o.type] || wt.valHooks[o.nodeName.toLowerCase()]) &&
          'get' in e &&
          void 0 !== (n = e.get(o, 'value'))
          ? n
          : 'string' == typeof (n = o.value)
          ? n.replace(ke, '')
          : null == n
          ? ''
          : n
        : void 0;
    },
  }),
    wt.extend({
      valHooks: {
        option: {
          get: function (t) {
            var e = wt.find.attr(t, 'value');
            return null != e ? e : Q(wt.text(t));
          },
        },
        select: {
          get: function (t) {
            var e,
              n,
              i,
              o = t.options,
              s = t.selectedIndex,
              a = 'select-one' === t.type,
              l = a ? null : [],
              c = a ? s + 1 : o.length;
            for (i = s < 0 ? c : a ? s : 0; i < c; i++)
              if (
                ((n = o[i]).selected || i === s) &&
                !n.disabled &&
                (!n.parentNode.disabled || !r(n.parentNode, 'optgroup'))
              ) {
                if (((e = wt(n).val()), a)) return e;
                l.push(e);
              }
            return l;
          },
          set: function (t, e) {
            for (
              var n, i, o = t.options, r = wt.makeArray(e), s = o.length;
              s--;

            )
              ((i = o[s]).selected =
                wt.inArray(wt.valHooks.option.get(i), r) > -1) && (n = !0);
            return n || (t.selectedIndex = -1), r;
          },
        },
      },
    }),
    wt.each(['radio', 'checkbox'], function () {
      (wt.valHooks[this] = {
        set: function (t, e) {
          if (Array.isArray(e))
            return (t.checked = wt.inArray(wt(t).val(), e) > -1);
        },
      }),
        vt.checkOn ||
          (wt.valHooks[this].get = function (t) {
            return null === t.getAttribute('value') ? 'on' : t.value;
          });
    }),
    (vt.focusin = 'onfocusin' in t);
  var Se = /^(?:focusinfocus|focusoutblur)$/,
    $e = function (t) {
      t.stopPropagation();
    };
  wt.extend(wt.event, {
    trigger: function (e, n, i, o) {
      var r,
        s,
        a,
        l,
        c,
        u,
        d,
        p,
        h = [i || st],
        f = ft.call(e, 'type') ? e.type : e,
        g = ft.call(e, 'namespace') ? e.namespace.split('.') : [];
      if (
        ((s = p = a = i = i || st),
        3 !== i.nodeType &&
          8 !== i.nodeType &&
          !Se.test(f + wt.event.triggered) &&
          (f.indexOf('.') > -1 && ((f = (g = f.split('.')).shift()), g.sort()),
          (c = f.indexOf(':') < 0 && 'on' + f),
          (e = e[wt.expando] ? e : new wt.Event(f, 'object' == typeof e && e)),
          (e.isTrigger = o ? 2 : 3),
          (e.namespace = g.join('.')),
          (e.rnamespace = e.namespace
            ? new RegExp('(^|\\.)' + g.join('\\.(?:.*\\.|)') + '(\\.|$)')
            : null),
          (e.result = void 0),
          e.target || (e.target = i),
          (n = null == n ? [e] : wt.makeArray(n, [e])),
          (d = wt.event.special[f] || {}),
          o || !d.trigger || !1 !== d.trigger.apply(i, n)))
      ) {
        if (!o && !d.noBubble && !bt(i)) {
          for (
            l = d.delegateType || f, Se.test(l + f) || (s = s.parentNode);
            s;
            s = s.parentNode
          )
            h.push(s), (a = s);
          a === (i.ownerDocument || st) &&
            h.push(a.defaultView || a.parentWindow || t);
        }
        for (r = 0; (s = h[r++]) && !e.isPropagationStopped(); )
          (p = s),
            (e.type = r > 1 ? l : d.bindType || f),
            (u = (Nt.get(s, 'events') || {})[e.type] && Nt.get(s, 'handle')) &&
              u.apply(s, n),
            (u = c && s[c]) &&
              u.apply &&
              Ht(s) &&
              ((e.result = u.apply(s, n)),
              !1 === e.result && e.preventDefault());
        return (
          (e.type = f),
          o ||
            e.isDefaultPrevented() ||
            (d._default && !1 !== d._default.apply(h.pop(), n)) ||
            !Ht(i) ||
            (c &&
              yt(i[f]) &&
              !bt(i) &&
              ((a = i[c]) && (i[c] = null),
              (wt.event.triggered = f),
              e.isPropagationStopped() && p.addEventListener(f, $e),
              i[f](),
              e.isPropagationStopped() && p.removeEventListener(f, $e),
              (wt.event.triggered = void 0),
              a && (i[c] = a))),
          e.result
        );
      }
    },
    simulate: function (t, e, n) {
      var i = wt.extend(new wt.Event(), n, { type: t, isSimulated: !0 });
      wt.event.trigger(i, null, e);
    },
  }),
    wt.fn.extend({
      trigger: function (t, e) {
        return this.each(function () {
          wt.event.trigger(t, e, this);
        });
      },
      triggerHandler: function (t, e) {
        var n = this[0];
        if (n) return wt.event.trigger(t, e, n, !0);
      },
    }),
    vt.focusin ||
      wt.each({ focus: 'focusin', blur: 'focusout' }, function (t, e) {
        var n = function (t) {
          wt.event.simulate(e, t.target, wt.event.fix(t));
        };
        wt.event.special[e] = {
          setup: function () {
            var i = this.ownerDocument || this,
              o = Nt.access(i, e);
            o || i.addEventListener(t, n, !0), Nt.access(i, e, (o || 0) + 1);
          },
          teardown: function () {
            var i = this.ownerDocument || this,
              o = Nt.access(i, e) - 1;
            o
              ? Nt.access(i, e, o)
              : (i.removeEventListener(t, n, !0), Nt.remove(i, e));
          },
        };
      });
  var Ee = t.location,
    Pe = Date.now(),
    Ae = /\?/;
  wt.parseXML = function (e) {
    var n;
    if (!e || 'string' != typeof e) return null;
    try {
      n = new t.DOMParser().parseFromString(e, 'text/xml');
    } catch (t) {
      n = void 0;
    }
    return (
      (n && !n.getElementsByTagName('parsererror').length) ||
        wt.error('Invalid XML: ' + e),
      n
    );
  };
  var Ie = /\[\]$/,
    Me = /\r?\n/g,
    Le = /^(?:submit|button|image|reset|file)$/i,
    De = /^(?:input|select|textarea|keygen)/i;
  (wt.param = function (t, e) {
    var n,
      i = [],
      o = function (t, e) {
        var n = yt(e) ? e() : e;
        i[i.length] =
          encodeURIComponent(t) + '=' + encodeURIComponent(null == n ? '' : n);
      };
    if (Array.isArray(t) || (t.jquery && !wt.isPlainObject(t)))
      wt.each(t, function () {
        o(this.name, this.value);
      });
    else for (n in t) J(n, t[n], e, o);
    return i.join('&');
  }),
    wt.fn.extend({
      serialize: function () {
        return wt.param(this.serializeArray());
      },
      serializeArray: function () {
        return this.map(function () {
          var t = wt.prop(this, 'elements');
          return t ? wt.makeArray(t) : this;
        })
          .filter(function () {
            var t = this.type;
            return (
              this.name &&
              !wt(this).is(':disabled') &&
              De.test(this.nodeName) &&
              !Le.test(t) &&
              (this.checked || !Zt.test(t))
            );
          })
          .map(function (t, e) {
            var n = wt(this).val();
            return null == n
              ? null
              : Array.isArray(n)
              ? wt.map(n, function (t) {
                  return { name: e.name, value: t.replace(Me, '\r\n') };
                })
              : { name: e.name, value: n.replace(Me, '\r\n') };
          })
          .get();
      },
    });
  var je = /%20/g,
    ze = /#.*$/,
    Oe = /([?&])_=[^&]*/,
    He = /^(.*?):[ \t]*([^\r\n]*)$/gm,
    Ne = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/,
    Fe = /^(?:GET|HEAD)$/,
    qe = /^\/\//,
    Re = {},
    Be = {},
    We = '*/'.concat('*'),
    Xe = st.createElement('a');
  (Xe.href = Ee.href),
    wt.extend({
      active: 0,
      lastModified: {},
      etag: {},
      ajaxSettings: {
        url: Ee.href,
        type: 'GET',
        isLocal: Ne.test(Ee.protocol),
        global: !0,
        processData: !0,
        async: !0,
        contentType: 'application/x-www-form-urlencoded; charset=UTF-8',
        accepts: {
          '*': We,
          text: 'text/plain',
          html: 'text/html',
          xml: 'application/xml, text/xml',
          json: 'application/json, text/javascript',
        },
        contents: { xml: /\bxml\b/, html: /\bhtml/, json: /\bjson\b/ },
        responseFields: {
          xml: 'responseXML',
          text: 'responseText',
          json: 'responseJSON',
        },
        converters: {
          '* text': String,
          'text html': !0,
          'text json': JSON.parse,
          'text xml': wt.parseXML,
        },
        flatOptions: { url: !0, context: !0 },
      },
      ajaxSetup: function (t, e) {
        return e ? nt(nt(t, wt.ajaxSettings), e) : nt(wt.ajaxSettings, t);
      },
      ajaxPrefilter: tt(Re),
      ajaxTransport: tt(Be),
      ajax: function (e, n) {
        function i(e, n, i, a) {
          var c,
            p,
            h,
            x,
            w,
            C = n;
          u ||
            ((u = !0),
            l && t.clearTimeout(l),
            (o = void 0),
            (s = a || ''),
            (_.readyState = e > 0 ? 4 : 0),
            (c = (e >= 200 && e < 300) || 304 === e),
            i && (x = it(f, _, i)),
            (x = ot(f, x, _, c)),
            c
              ? (f.ifModified &&
                  ((w = _.getResponseHeader('Last-Modified')) &&
                    (wt.lastModified[r] = w),
                  (w = _.getResponseHeader('etag')) && (wt.etag[r] = w)),
                204 === e || 'HEAD' === f.type
                  ? (C = 'nocontent')
                  : 304 === e
                  ? (C = 'notmodified')
                  : ((C = x.state), (p = x.data), (c = !(h = x.error))))
              : ((h = C), (!e && C) || ((C = 'error'), e < 0 && (e = 0))),
            (_.status = e),
            (_.statusText = (n || C) + ''),
            c ? v.resolveWith(g, [p, C, _]) : v.rejectWith(g, [_, C, h]),
            _.statusCode(b),
            (b = void 0),
            d && m.trigger(c ? 'ajaxSuccess' : 'ajaxError', [_, f, c ? p : h]),
            y.fireWith(g, [_, C]),
            d &&
              (m.trigger('ajaxComplete', [_, f]),
              --wt.active || wt.event.trigger('ajaxStop')));
        }
        'object' == typeof e && ((n = e), (e = void 0)), (n = n || {});
        var o,
          r,
          s,
          a,
          l,
          c,
          u,
          d,
          p,
          h,
          f = wt.ajaxSetup({}, n),
          g = f.context || f,
          m = f.context && (g.nodeType || g.jquery) ? wt(g) : wt.event,
          v = wt.Deferred(),
          y = wt.Callbacks('once memory'),
          b = f.statusCode || {},
          x = {},
          w = {},
          C = 'canceled',
          _ = {
            readyState: 0,
            getResponseHeader: function (t) {
              var e;
              if (u) {
                if (!a)
                  for (a = {}; (e = He.exec(s)); ) a[e[1].toLowerCase()] = e[2];
                e = a[t.toLowerCase()];
              }
              return null == e ? null : e;
            },
            getAllResponseHeaders: function () {
              return u ? s : null;
            },
            setRequestHeader: function (t, e) {
              return (
                null == u &&
                  ((t = w[t.toLowerCase()] = w[t.toLowerCase()] || t),
                  (x[t] = e)),
                this
              );
            },
            overrideMimeType: function (t) {
              return null == u && (f.mimeType = t), this;
            },
            statusCode: function (t) {
              var e;
              if (t)
                if (u) _.always(t[_.status]);
                else for (e in t) b[e] = [b[e], t[e]];
              return this;
            },
            abort: function (t) {
              var e = t || C;
              return o && o.abort(e), i(0, e), this;
            },
          };
        if (
          (v.promise(_),
          (f.url = ((e || f.url || Ee.href) + '').replace(
            qe,
            Ee.protocol + '//'
          )),
          (f.type = n.method || n.type || f.method || f.type),
          (f.dataTypes = (f.dataType || '*').toLowerCase().match(Mt) || ['']),
          null == f.crossDomain)
        ) {
          c = st.createElement('a');
          try {
            (c.href = f.url),
              (c.href = c.href),
              (f.crossDomain =
                Xe.protocol + '//' + Xe.host != c.protocol + '//' + c.host);
          } catch (t) {
            f.crossDomain = !0;
          }
        }
        if (
          (f.data &&
            f.processData &&
            'string' != typeof f.data &&
            (f.data = wt.param(f.data, f.traditional)),
          et(Re, f, n, _),
          u)
        )
          return _;
        (d = wt.event && f.global) &&
          0 == wt.active++ &&
          wt.event.trigger('ajaxStart'),
          (f.type = f.type.toUpperCase()),
          (f.hasContent = !Fe.test(f.type)),
          (r = f.url.replace(ze, '')),
          f.hasContent
            ? f.data &&
              f.processData &&
              0 ===
                (f.contentType || '').indexOf(
                  'application/x-www-form-urlencoded'
                ) &&
              (f.data = f.data.replace(je, '+'))
            : ((h = f.url.slice(r.length)),
              f.data &&
                (f.processData || 'string' == typeof f.data) &&
                ((r += (Ae.test(r) ? '&' : '?') + f.data), delete f.data),
              !1 === f.cache &&
                ((r = r.replace(Oe, '$1')),
                (h = (Ae.test(r) ? '&' : '?') + '_=' + Pe++ + h)),
              (f.url = r + h)),
          f.ifModified &&
            (wt.lastModified[r] &&
              _.setRequestHeader('If-Modified-Since', wt.lastModified[r]),
            wt.etag[r] && _.setRequestHeader('If-None-Match', wt.etag[r])),
          ((f.data && f.hasContent && !1 !== f.contentType) || n.contentType) &&
            _.setRequestHeader('Content-Type', f.contentType),
          _.setRequestHeader(
            'Accept',
            f.dataTypes[0] && f.accepts[f.dataTypes[0]]
              ? f.accepts[f.dataTypes[0]] +
                  ('*' !== f.dataTypes[0] ? ', ' + We + '; q=0.01' : '')
              : f.accepts['*']
          );
        for (p in f.headers) _.setRequestHeader(p, f.headers[p]);
        if (f.beforeSend && (!1 === f.beforeSend.call(g, _, f) || u))
          return _.abort();
        if (
          ((C = 'abort'),
          y.add(f.complete),
          _.done(f.success),
          _.fail(f.error),
          (o = et(Be, f, n, _)))
        ) {
          if (((_.readyState = 1), d && m.trigger('ajaxSend', [_, f]), u))
            return _;
          f.async &&
            f.timeout > 0 &&
            (l = t.setTimeout(function () {
              _.abort('timeout');
            }, f.timeout));
          try {
            (u = !1), o.send(x, i);
          } catch (t) {
            if (u) throw t;
            i(-1, t);
          }
        } else i(-1, 'No Transport');
        return _;
      },
      getJSON: function (t, e, n) {
        return wt.get(t, e, n, 'json');
      },
      getScript: function (t, e) {
        return wt.get(t, void 0, e, 'script');
      },
    }),
    wt.each(['get', 'post'], function (t, e) {
      wt[e] = function (t, n, i, o) {
        return (
          yt(n) && ((o = o || i), (i = n), (n = void 0)),
          wt.ajax(
            wt.extend(
              { url: t, type: e, dataType: o, data: n, success: i },
              wt.isPlainObject(t) && t
            )
          )
        );
      };
    }),
    (wt._evalUrl = function (t) {
      return wt.ajax({
        url: t,
        type: 'GET',
        dataType: 'script',
        cache: !0,
        async: !1,
        global: !1,
        throws: !0,
      });
    }),
    wt.fn.extend({
      wrapAll: function (t) {
        var e;
        return (
          this[0] &&
            (yt(t) && (t = t.call(this[0])),
            (e = wt(t, this[0].ownerDocument).eq(0).clone(!0)),
            this[0].parentNode && e.insertBefore(this[0]),
            e
              .map(function () {
                for (var t = this; t.firstElementChild; )
                  t = t.firstElementChild;
                return t;
              })
              .append(this)),
          this
        );
      },
      wrapInner: function (t) {
        return yt(t)
          ? this.each(function (e) {
              wt(this).wrapInner(t.call(this, e));
            })
          : this.each(function () {
              var e = wt(this),
                n = e.contents();
              n.length ? n.wrapAll(t) : e.append(t);
            });
      },
      wrap: function (t) {
        var e = yt(t);
        return this.each(function (n) {
          wt(this).wrapAll(e ? t.call(this, n) : t);
        });
      },
      unwrap: function (t) {
        return (
          this.parent(t)
            .not('body')
            .each(function () {
              wt(this).replaceWith(this.childNodes);
            }),
          this
        );
      },
    }),
    (wt.expr.pseudos.hidden = function (t) {
      return !wt.expr.pseudos.visible(t);
    }),
    (wt.expr.pseudos.visible = function (t) {
      return !!(t.offsetWidth || t.offsetHeight || t.getClientRects().length);
    }),
    (wt.ajaxSettings.xhr = function () {
      try {
        return new t.XMLHttpRequest();
      } catch (t) {}
    });
  var Ve = { 0: 200, 1223: 204 },
    Ye = wt.ajaxSettings.xhr();
  (vt.cors = !!Ye && 'withCredentials' in Ye),
    (vt.ajax = Ye = !!Ye),
    wt.ajaxTransport(function (e) {
      var n, i;
      if (vt.cors || (Ye && !e.crossDomain))
        return {
          send: function (o, r) {
            var s,
              a = e.xhr();
            if (
              (a.open(e.type, e.url, e.async, e.username, e.password),
              e.xhrFields)
            )
              for (s in e.xhrFields) a[s] = e.xhrFields[s];
            e.mimeType && a.overrideMimeType && a.overrideMimeType(e.mimeType),
              e.crossDomain ||
                o['X-Requested-With'] ||
                (o['X-Requested-With'] = 'XMLHttpRequest');
            for (s in o) a.setRequestHeader(s, o[s]);
            (n = function (t) {
              return function () {
                n &&
                  ((n =
                    i =
                    a.onload =
                    a.onerror =
                    a.onabort =
                    a.ontimeout =
                    a.onreadystatechange =
                      null),
                  'abort' === t
                    ? a.abort()
                    : 'error' === t
                    ? 'number' != typeof a.status
                      ? r(0, 'error')
                      : r(a.status, a.statusText)
                    : r(
                        Ve[a.status] || a.status,
                        a.statusText,
                        'text' !== (a.responseType || 'text') ||
                          'string' != typeof a.responseText
                          ? { binary: a.response }
                          : { text: a.responseText },
                        a.getAllResponseHeaders()
                      ));
              };
            }),
              (a.onload = n()),
              (i = a.onerror = a.ontimeout = n('error')),
              void 0 !== a.onabort
                ? (a.onabort = i)
                : (a.onreadystatechange = function () {
                    4 === a.readyState &&
                      t.setTimeout(function () {
                        n && i();
                      });
                  }),
              (n = n('abort'));
            try {
              a.send((e.hasContent && e.data) || null);
            } catch (t) {
              if (n) throw t;
            }
          },
          abort: function () {
            n && n();
          },
        };
    }),
    wt.ajaxPrefilter(function (t) {
      t.crossDomain && (t.contents.script = !1);
    }),
    wt.ajaxSetup({
      accepts: {
        script:
          'text/javascript, application/javascript, application/ecmascript, application/x-ecmascript',
      },
      contents: { script: /\b(?:java|ecma)script\b/ },
      converters: {
        'text script': function (t) {
          return wt.globalEval(t), t;
        },
      },
    }),
    wt.ajaxPrefilter('script', function (t) {
      void 0 === t.cache && (t.cache = !1), t.crossDomain && (t.type = 'GET');
    }),
    wt.ajaxTransport('script', function (t) {
      if (t.crossDomain) {
        var e, n;
        return {
          send: function (i, o) {
            (e = wt('<script>')
              .prop({ charset: t.scriptCharset, src: t.url })
              .on(
                'load error',
                (n = function (t) {
                  e.remove(),
                    (n = null),
                    t && o('error' === t.type ? 404 : 200, t.type);
                })
              )),
              st.head.appendChild(e[0]);
          },
          abort: function () {
            n && n();
          },
        };
      }
    });
  var Ue = [],
    Ze = /(=)\?(?=&|$)|\?\?/;
  wt.ajaxSetup({
    jsonp: 'callback',
    jsonpCallback: function () {
      var t = Ue.pop() || wt.expando + '_' + Pe++;
      return (this[t] = !0), t;
    },
  }),
    wt.ajaxPrefilter('json jsonp', function (e, n, i) {
      var o,
        r,
        s,
        a =
          !1 !== e.jsonp &&
          (Ze.test(e.url)
            ? 'url'
            : 'string' == typeof e.data &&
              0 ===
                (e.contentType || '').indexOf(
                  'application/x-www-form-urlencoded'
                ) &&
              Ze.test(e.data) &&
              'data');
      if (a || 'jsonp' === e.dataTypes[0])
        return (
          (o = e.jsonpCallback =
            yt(e.jsonpCallback) ? e.jsonpCallback() : e.jsonpCallback),
          a
            ? (e[a] = e[a].replace(Ze, '$1' + o))
            : !1 !== e.jsonp &&
              (e.url += (Ae.test(e.url) ? '&' : '?') + e.jsonp + '=' + o),
          (e.converters['script json'] = function () {
            return s || wt.error(o + ' was not called'), s[0];
          }),
          (e.dataTypes[0] = 'json'),
          (r = t[o]),
          (t[o] = function () {
            s = arguments;
          }),
          i.always(function () {
            void 0 === r ? wt(t).removeProp(o) : (t[o] = r),
              e[o] && ((e.jsonpCallback = n.jsonpCallback), Ue.push(o)),
              s && yt(r) && r(s[0]),
              (s = r = void 0);
          }),
          'script'
        );
    }),
    (vt.createHTMLDocument = (function () {
      var t = st.implementation.createHTMLDocument('').body;
      return (
        (t.innerHTML = '<form></form><form></form>'), 2 === t.childNodes.length
      );
    })()),
    (wt.parseHTML = function (t, e, n) {
      if ('string' != typeof t) return [];
      'boolean' == typeof e && ((n = e), (e = !1));
      var i, o, r;
      return (
        e ||
          (vt.createHTMLDocument
            ? (((i = (e =
                st.implementation.createHTMLDocument('')).createElement(
                'base'
              )).href = st.location.href),
              e.head.appendChild(i))
            : (e = st)),
        (o = $t.exec(t)),
        (r = !n && []),
        o
          ? [e.createElement(o[1])]
          : ((o = _([t], e, r)),
            r && r.length && wt(r).remove(),
            wt.merge([], o.childNodes))
      );
    }),
    (wt.fn.load = function (t, e, n) {
      var i,
        o,
        r,
        s = this,
        a = t.indexOf(' ');
      return (
        a > -1 && ((i = Q(t.slice(a))), (t = t.slice(0, a))),
        yt(e)
          ? ((n = e), (e = void 0))
          : e && 'object' == typeof e && (o = 'POST'),
        s.length > 0 &&
          wt
            .ajax({ url: t, type: o || 'GET', dataType: 'html', data: e })
            .done(function (t) {
              (r = arguments),
                s.html(i ? wt('<div>').append(wt.parseHTML(t)).find(i) : t);
            })
            .always(
              n &&
                function (t, e) {
                  s.each(function () {
                    n.apply(this, r || [t.responseText, e, t]);
                  });
                }
            ),
        this
      );
    }),
    wt.each(
      [
        'ajaxStart',
        'ajaxStop',
        'ajaxComplete',
        'ajaxError',
        'ajaxSuccess',
        'ajaxSend',
      ],
      function (t, e) {
        wt.fn[e] = function (t) {
          return this.on(e, t);
        };
      }
    ),
    (wt.expr.pseudos.animated = function (t) {
      return wt.grep(wt.timers, function (e) {
        return t === e.elem;
      }).length;
    }),
    (wt.offset = {
      setOffset: function (t, e, n) {
        var i,
          o,
          r,
          s,
          a,
          l,
          c = wt.css(t, 'position'),
          u = wt(t),
          d = {};
        'static' === c && (t.style.position = 'relative'),
          (a = u.offset()),
          (r = wt.css(t, 'top')),
          (l = wt.css(t, 'left')),
          ('absolute' === c || 'fixed' === c) && (r + l).indexOf('auto') > -1
            ? ((s = (i = u.position()).top), (o = i.left))
            : ((s = parseFloat(r) || 0), (o = parseFloat(l) || 0)),
          yt(e) && (e = e.call(t, n, wt.extend({}, a))),
          null != e.top && (d.top = e.top - a.top + s),
          null != e.left && (d.left = e.left - a.left + o),
          'using' in e ? e.using.call(t, d) : u.css(d);
      },
    }),
    wt.fn.extend({
      offset: function (t) {
        if (arguments.length)
          return void 0 === t
            ? this
            : this.each(function (e) {
                wt.offset.setOffset(this, t, e);
              });
        var e,
          n,
          i = this[0];
        return i
          ? i.getClientRects().length
            ? ((e = i.getBoundingClientRect()),
              (n = i.ownerDocument.defaultView),
              { top: e.top + n.pageYOffset, left: e.left + n.pageXOffset })
            : { top: 0, left: 0 }
          : void 0;
      },
      position: function () {
        if (this[0]) {
          var t,
            e,
            n,
            i = this[0],
            o = { top: 0, left: 0 };
          if ('fixed' === wt.css(i, 'position')) e = i.getBoundingClientRect();
          else {
            for (
              e = this.offset(),
                n = i.ownerDocument,
                t = i.offsetParent || n.documentElement;
              t &&
              (t === n.body || t === n.documentElement) &&
              'static' === wt.css(t, 'position');

            )
              t = t.parentNode;
            t &&
              t !== i &&
              1 === t.nodeType &&
              (((o = wt(t).offset()).top += wt.css(t, 'borderTopWidth', !0)),
              (o.left += wt.css(t, 'borderLeftWidth', !0)));
          }
          return {
            top: e.top - o.top - wt.css(i, 'marginTop', !0),
            left: e.left - o.left - wt.css(i, 'marginLeft', !0),
          };
        }
      },
      offsetParent: function () {
        return this.map(function () {
          for (
            var t = this.offsetParent;
            t && 'static' === wt.css(t, 'position');

          )
            t = t.offsetParent;
          return t || te;
        });
      },
    }),
    wt.each(
      { scrollLeft: 'pageXOffset', scrollTop: 'pageYOffset' },
      function (t, e) {
        var n = 'pageYOffset' === e;
        wt.fn[t] = function (i) {
          return jt(
            this,
            function (t, i, o) {
              var r;
              if (
                (bt(t) ? (r = t) : 9 === t.nodeType && (r = t.defaultView),
                void 0 === o)
              )
                return r ? r[e] : t[i];
              r
                ? r.scrollTo(n ? r.pageXOffset : o, n ? o : r.pageYOffset)
                : (t[i] = o);
            },
            t,
            i,
            arguments.length
          );
        };
      }
    ),
    wt.each(['top', 'left'], function (t, e) {
      wt.cssHooks[e] = z(vt.pixelPosition, function (t, n) {
        if (n)
          return (n = j(t, e)), le.test(n) ? wt(t).position()[e] + 'px' : n;
      });
    }),
    wt.each({ Height: 'height', Width: 'width' }, function (t, e) {
      wt.each(
        { padding: 'inner' + t, content: e, '': 'outer' + t },
        function (n, i) {
          wt.fn[i] = function (o, r) {
            var s = arguments.length && (n || 'boolean' != typeof o),
              a = n || (!0 === o || !0 === r ? 'margin' : 'border');
            return jt(
              this,
              function (e, n, o) {
                var r;
                return bt(e)
                  ? 0 === i.indexOf('outer')
                    ? e['inner' + t]
                    : e.document.documentElement['client' + t]
                  : 9 === e.nodeType
                  ? ((r = e.documentElement),
                    Math.max(
                      e.body['scroll' + t],
                      r['scroll' + t],
                      e.body['offset' + t],
                      r['offset' + t],
                      r['client' + t]
                    ))
                  : void 0 === o
                  ? wt.css(e, n, a)
                  : wt.style(e, n, o, a);
              },
              e,
              s ? o : void 0,
              s
            );
          };
        }
      );
    }),
    wt.each(
      'blur focus focusin focusout resize scroll click dblclick mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave change select submit keydown keypress keyup contextmenu'.split(
        ' '
      ),
      function (t, e) {
        wt.fn[e] = function (t, n) {
          return arguments.length > 0
            ? this.on(e, null, t, n)
            : this.trigger(e);
        };
      }
    ),
    wt.fn.extend({
      hover: function (t, e) {
        return this.mouseenter(t).mouseleave(e || t);
      },
    }),
    wt.fn.extend({
      bind: function (t, e, n) {
        return this.on(t, null, e, n);
      },
      unbind: function (t, e) {
        return this.off(t, null, e);
      },
      delegate: function (t, e, n, i) {
        return this.on(e, t, n, i);
      },
      undelegate: function (t, e, n) {
        return 1 === arguments.length
          ? this.off(t, '**')
          : this.off(e, t || '**', n);
      },
    }),
    (wt.proxy = function (t, e) {
      var n, i, o;
      if (('string' == typeof e && ((n = t[e]), (e = t), (t = n)), yt(t)))
        return (
          (i = lt.call(arguments, 2)),
          (o = function () {
            return t.apply(e || this, i.concat(lt.call(arguments)));
          }),
          (o.guid = t.guid = t.guid || wt.guid++),
          o
        );
    }),
    (wt.holdReady = function (t) {
      t ? wt.readyWait++ : wt.ready(!0);
    }),
    (wt.isArray = Array.isArray),
    (wt.parseJSON = JSON.parse),
    (wt.nodeName = r),
    (wt.isFunction = yt),
    (wt.isWindow = bt),
    (wt.camelCase = f),
    (wt.type = i),
    (wt.now = Date.now),
    (wt.isNumeric = function (t) {
      var e = wt.type(t);
      return ('number' === e || 'string' === e) && !isNaN(t - parseFloat(t));
    }),
    'function' == typeof define &&
      define.amd &&
      define('jquery', [], function () {
        return wt;
      });
  var Qe = t.jQuery,
    Ge = t.$;
  return (
    (wt.noConflict = function (e) {
      return (
        t.$ === wt && (t.$ = Ge), e && t.jQuery === wt && (t.jQuery = Qe), wt
      );
    }),
    e || (t.jQuery = t.$ = wt),
    wt
  );
}),
  (function (t, e, n, i) {
    function o(e, n) {
      (this.settings = null),
        (this.options = t.extend({}, o.Defaults, n)),
        (this.$element = t(e)),
        (this._handlers = {}),
        (this._plugins = {}),
        (this._supress = {}),
        (this._current = null),
        (this._speed = null),
        (this._coordinates = []),
        (this._breakpoint = null),
        (this._width = null),
        (this._items = []),
        (this._clones = []),
        (this._mergers = []),
        (this._widths = []),
        (this._invalidated = {}),
        (this._pipe = []),
        (this._drag = {
          time: null,
          target: null,
          pointer: null,
          stage: { start: null, current: null },
          direction: null,
        }),
        (this._states = {
          current: {},
          tags: {
            initializing: ['busy'],
            animating: ['busy'],
            dragging: ['interacting'],
          },
        }),
        t.each(
          ['onResize', 'onThrottledResize'],
          t.proxy(function (e, n) {
            this._handlers[n] = t.proxy(this[n], this);
          }, this)
        ),
        t.each(
          o.Plugins,
          t.proxy(function (t, e) {
            this._plugins[t.charAt(0).toLowerCase() + t.slice(1)] = new e(this);
          }, this)
        ),
        t.each(
          o.Workers,
          t.proxy(function (e, n) {
            this._pipe.push({ filter: n.filter, run: t.proxy(n.run, this) });
          }, this)
        ),
        this.setup(),
        this.initialize();
    }
    (o.Defaults = {
      items: 3,
      loop: !1,
      center: !1,
      rewind: !1,
      checkVisibility: !0,
      mouseDrag: !0,
      touchDrag: !0,
      pullDrag: !0,
      freeDrag: !1,
      margin: 0,
      stagePadding: 0,
      merge: !1,
      mergeFit: !0,
      autoWidth: !1,
      startPosition: 0,
      rtl: !1,
      smartSpeed: 250,
      fluidSpeed: !1,
      dragEndSpeed: !1,
      responsive: {},
      responsiveRefreshRate: 200,
      responsiveBaseElement: e,
      fallbackEasing: 'swing',
      slideTransition: '',
      info: !1,
      nestedItemSelector: !1,
      itemElement: 'div',
      stageElement: 'div',
      refreshClass: 'owl-refresh',
      loadedClass: 'owl-loaded',
      loadingClass: 'owl-loading',
      rtlClass: 'owl-rtl',
      responsiveClass: 'owl-responsive',
      dragClass: 'owl-drag',
      itemClass: 'owl-item',
      stageClass: 'owl-stage',
      stageOuterClass: 'owl-stage-outer',
      grabClass: 'owl-grab',
    }),
      (o.Width = { Default: 'default', Inner: 'inner', Outer: 'outer' }),
      (o.Type = { Event: 'event', State: 'state' }),
      (o.Plugins = {}),
      (o.Workers = [
        {
          filter: ['width', 'settings'],
          run: function () {
            this._width = this.$element.width();
          },
        },
        {
          filter: ['width', 'items', 'settings'],
          run: function (t) {
            t.current =
              this._items && this._items[this.relative(this._current)];
          },
        },
        {
          filter: ['items', 'settings'],
          run: function () {
            this.$stage.children('.cloned').remove();
          },
        },
        {
          filter: ['width', 'items', 'settings'],
          run: function (t) {
            var e = this.settings.margin || '',
              n = !this.settings.autoWidth,
              i = this.settings.rtl,
              o = {
                width: 'auto',
                'margin-left': i ? e : '',
                'margin-right': i ? '' : e,
              };
            !n && this.$stage.children().css(o), (t.css = o);
          },
        },
        {
          filter: ['width', 'items', 'settings'],
          run: function (t) {
            var e =
                (this.width() / this.settings.items).toFixed(3) -
                this.settings.margin,
              n = null,
              i = this._items.length,
              o = !this.settings.autoWidth,
              r = [];
            for (t.items = { merge: !1, width: e }; i--; )
              (n = this._mergers[i]),
                (n =
                  (this.settings.mergeFit &&
                    Math.min(n, this.settings.items)) ||
                  n),
                (t.items.merge = n > 1 || t.items.merge),
                (r[i] = o ? e * n : this._items[i].width());
            this._widths = r;
          },
        },
        {
          filter: ['items', 'settings'],
          run: function () {
            var e = [],
              n = this._items,
              i = this.settings,
              o = Math.max(2 * i.items, 4),
              r = 2 * Math.ceil(n.length / 2),
              s = i.loop && n.length ? (i.rewind ? o : Math.max(o, r)) : 0,
              a = '',
              l = '';
            for (s /= 2; s > 0; )
              e.push(this.normalize(e.length / 2, !0)),
                (a += n[e[e.length - 1]][0].outerHTML),
                e.push(this.normalize(n.length - 1 - (e.length - 1) / 2, !0)),
                (l = n[e[e.length - 1]][0].outerHTML + l),
                (s -= 1);
            (this._clones = e),
              t(a).addClass('cloned').appendTo(this.$stage),
              t(l).addClass('cloned').prependTo(this.$stage);
          },
        },
        {
          filter: ['width', 'items', 'settings'],
          run: function () {
            for (
              var t = this.settings.rtl ? 1 : -1,
                e = this._clones.length + this._items.length,
                n = -1,
                i = 0,
                o = 0,
                r = [];
              ++n < e;

            )
              (i = r[n - 1] || 0),
                (o = this._widths[this.relative(n)] + this.settings.margin),
                r.push(i + o * t);
            this._coordinates = r;
          },
        },
        {
          filter: ['width', 'items', 'settings'],
          run: function () {
            var t = this.settings.stagePadding,
              e = this._coordinates,
              n = {
                width: Math.ceil(Math.abs(e[e.length - 1])) + 2 * t,
                'padding-left': t || '',
                'padding-right': t || '',
              };
            this.$stage.css(n);
          },
        },
        {
          filter: ['width', 'items', 'settings'],
          run: function (t) {
            var e = this._coordinates.length,
              n = !this.settings.autoWidth,
              i = this.$stage.children();
            if (n && t.items.merge)
              for (; e--; )
                (t.css.width = this._widths[this.relative(e)]),
                  i.eq(e).css(t.css);
            else n && ((t.css.width = t.items.width), i.css(t.css));
          },
        },
        {
          filter: ['items'],
          run: function () {
            this._coordinates.length < 1 && this.$stage.removeAttr('style');
          },
        },
        {
          filter: ['width', 'items', 'settings'],
          run: function (t) {
            (t.current = t.current
              ? this.$stage.children().index(t.current)
              : 0),
              (t.current = Math.max(
                this.minimum(),
                Math.min(this.maximum(), t.current)
              )),
              this.reset(t.current);
          },
        },
        {
          filter: ['position'],
          run: function () {
            this.animate(this.coordinates(this._current));
          },
        },
        {
          filter: ['width', 'position', 'items', 'settings'],
          run: function () {
            var t,
              e,
              n,
              i,
              o = this.settings.rtl ? 1 : -1,
              r = 2 * this.settings.stagePadding,
              s = this.coordinates(this.current()) + r,
              a = s + this.width() * o,
              l = [];
            for (n = 0, i = this._coordinates.length; n < i; n++)
              (t = this._coordinates[n - 1] || 0),
                (e = Math.abs(this._coordinates[n]) + r * o),
                ((this.op(t, '<=', s) && this.op(t, '>', a)) ||
                  (this.op(e, '<', s) && this.op(e, '>', a))) &&
                  l.push(n);
            this.$stage.children('.active').removeClass('active'),
              this.$stage
                .children(':eq(' + l.join('), :eq(') + ')')
                .addClass('active'),
              this.$stage.children('.center').removeClass('center'),
              this.settings.center &&
                this.$stage.children().eq(this.current()).addClass('center');
          },
        },
      ]),
      (o.prototype.initializeStage = function () {
        (this.$stage = this.$element.find('.' + this.settings.stageClass)),
          this.$stage.length ||
            (this.$element.addClass(this.options.loadingClass),
            (this.$stage = t('<' + this.settings.stageElement + '>', {
              class: this.settings.stageClass,
            }).wrap(t('<div/>', { class: this.settings.stageOuterClass }))),
            this.$element.append(this.$stage.parent()));
      }),
      (o.prototype.initializeItems = function () {
        var e = this.$element.find('.owl-item');
        if (e.length)
          return (
            (this._items = e.get().map(function (e) {
              return t(e);
            })),
            (this._mergers = this._items.map(function () {
              return 1;
            })),
            void this.refresh()
          );
        this.replace(this.$element.children().not(this.$stage.parent())),
          this.isVisible() ? this.refresh() : this.invalidate('width'),
          this.$element
            .removeClass(this.options.loadingClass)
            .addClass(this.options.loadedClass);
      }),
      (o.prototype.initialize = function () {
        if (
          (this.enter('initializing'),
          this.trigger('initialize'),
          this.$element.toggleClass(this.settings.rtlClass, this.settings.rtl),
          this.settings.autoWidth && !this.is('pre-loading'))
        ) {
          var t, e, n;
          (t = this.$element.find('img')),
            (e = this.settings.nestedItemSelector
              ? '.' + this.settings.nestedItemSelector
              : i),
            (n = this.$element.children(e).width()),
            t.length && n <= 0 && this.preloadAutoWidthImages(t);
        }
        this.initializeStage(),
          this.initializeItems(),
          this.registerEventHandlers(),
          this.leave('initializing'),
          this.trigger('initialized');
      }),
      (o.prototype.isVisible = function () {
        return !this.settings.checkVisibility || this.$element.is(':visible');
      }),
      (o.prototype.setup = function () {
        var e = this.viewport(),
          n = this.options.responsive,
          i = -1,
          o = null;
        n
          ? (t.each(n, function (t) {
              t <= e && t > i && (i = Number(t));
            }),
            (o = t.extend({}, this.options, n[i])),
            'function' == typeof o.stagePadding &&
              (o.stagePadding = o.stagePadding()),
            delete o.responsive,
            o.responsiveClass &&
              this.$element.attr(
                'class',
                this.$element
                  .attr('class')
                  .replace(
                    new RegExp(
                      '(' + this.options.responsiveClass + '-)\\S+\\s',
                      'g'
                    ),
                    '$1' + i
                  )
              ))
          : (o = t.extend({}, this.options)),
          this.trigger('change', { property: { name: 'settings', value: o } }),
          (this._breakpoint = i),
          (this.settings = o),
          this.invalidate('settings'),
          this.trigger('changed', {
            property: { name: 'settings', value: this.settings },
          });
      }),
      (o.prototype.optionsLogic = function () {
        this.settings.autoWidth &&
          ((this.settings.stagePadding = !1), (this.settings.merge = !1));
      }),
      (o.prototype.prepare = function (e) {
        var n = this.trigger('prepare', { content: e });
        return (
          n.data ||
            (n.data = t('<' + this.settings.itemElement + '/>')
              .addClass(this.options.itemClass)
              .append(e)),
          this.trigger('prepared', { content: n.data }),
          n.data
        );
      }),
      (o.prototype.update = function () {
        for (
          var e = 0,
            n = this._pipe.length,
            i = t.proxy(function (t) {
              return this[t];
            }, this._invalidated),
            o = {};
          e < n;

        )
          (this._invalidated.all ||
            t.grep(this._pipe[e].filter, i).length > 0) &&
            this._pipe[e].run(o),
            e++;
        (this._invalidated = {}), !this.is('valid') && this.enter('valid');
      }),
      (o.prototype.width = function (t) {
        switch ((t = t || o.Width.Default)) {
          case o.Width.Inner:
          case o.Width.Outer:
            return this._width;
          default:
            return (
              this._width -
              2 * this.settings.stagePadding +
              this.settings.margin
            );
        }
      }),
      (o.prototype.refresh = function () {
        this.enter('refreshing'),
          this.trigger('refresh'),
          this.setup(),
          this.optionsLogic(),
          this.$element.addClass(this.options.refreshClass),
          this.update(),
          this.$element.removeClass(this.options.refreshClass),
          this.leave('refreshing'),
          this.trigger('refreshed');
      }),
      (o.prototype.onThrottledResize = function () {
        e.clearTimeout(this.resizeTimer),
          (this.resizeTimer = e.setTimeout(
            this._handlers.onResize,
            this.settings.responsiveRefreshRate
          ));
      }),
      (o.prototype.onResize = function () {
        return (
          !!this._items.length &&
          this._width !== this.$element.width() &&
          !!this.isVisible() &&
          (this.enter('resizing'),
          this.trigger('resize').isDefaultPrevented()
            ? (this.leave('resizing'), !1)
            : (this.invalidate('width'),
              this.refresh(),
              this.leave('resizing'),
              void this.trigger('resized')))
        );
      }),
      (o.prototype.registerEventHandlers = function () {
        t.support.transition &&
          this.$stage.on(
            t.support.transition.end + '.owl.core',
            t.proxy(this.onTransitionEnd, this)
          ),
          !1 !== this.settings.responsive &&
            this.on(e, 'resize', this._handlers.onThrottledResize),
          this.settings.mouseDrag &&
            (this.$element.addClass(this.options.dragClass),
            this.$stage.on(
              'mousedown.owl.core',
              t.proxy(this.onDragStart, this)
            ),
            this.$stage.on(
              'dragstart.owl.core selectstart.owl.core',
              function () {
                return !1;
              }
            )),
          this.settings.touchDrag &&
            (this.$stage.on(
              'touchstart.owl.core',
              t.proxy(this.onDragStart, this)
            ),
            this.$stage.on(
              'touchcancel.owl.core',
              t.proxy(this.onDragEnd, this)
            ));
      }),
      (o.prototype.onDragStart = function (e) {
        var i = null;
        3 !== e.which &&
          (t.support.transform
            ? ((i = this.$stage
                .css('transform')
                .replace(/.*\(|\)| /g, '')
                .split(',')),
              (i = {
                x: i[16 === i.length ? 12 : 4],
                y: i[16 === i.length ? 13 : 5],
              }))
            : ((i = this.$stage.position()),
              (i = {
                x: this.settings.rtl
                  ? i.left +
                    this.$stage.width() -
                    this.width() +
                    this.settings.margin
                  : i.left,
                y: i.top,
              })),
          this.is('animating') &&
            (t.support.transform ? this.animate(i.x) : this.$stage.stop(),
            this.invalidate('position')),
          this.$element.toggleClass(
            this.options.grabClass,
            'mousedown' === e.type
          ),
          this.speed(0),
          (this._drag.time = new Date().getTime()),
          (this._drag.target = t(e.target)),
          (this._drag.stage.start = i),
          (this._drag.stage.current = i),
          (this._drag.pointer = this.pointer(e)),
          t(n).on(
            'mouseup.owl.core touchend.owl.core',
            t.proxy(this.onDragEnd, this)
          ),
          t(n).one(
            'mousemove.owl.core touchmove.owl.core',
            t.proxy(function (e) {
              var i = this.difference(this._drag.pointer, this.pointer(e));
              t(n).on(
                'mousemove.owl.core touchmove.owl.core',
                t.proxy(this.onDragMove, this)
              ),
                (Math.abs(i.x) < Math.abs(i.y) && this.is('valid')) ||
                  (e.preventDefault(),
                  this.enter('dragging'),
                  this.trigger('drag'));
            }, this)
          ));
      }),
      (o.prototype.onDragMove = function (t) {
        var e = null,
          n = null,
          i = null,
          o = this.difference(this._drag.pointer, this.pointer(t)),
          r = this.difference(this._drag.stage.start, o);
        this.is('dragging') &&
          (t.preventDefault(),
          this.settings.loop
            ? ((e = this.coordinates(this.minimum())),
              (n = this.coordinates(this.maximum() + 1) - e),
              (r.x = ((((r.x - e) % n) + n) % n) + e))
            : ((e = this.settings.rtl
                ? this.coordinates(this.maximum())
                : this.coordinates(this.minimum())),
              (n = this.settings.rtl
                ? this.coordinates(this.minimum())
                : this.coordinates(this.maximum())),
              (i = this.settings.pullDrag ? (-1 * o.x) / 5 : 0),
              (r.x = Math.max(Math.min(r.x, e + i), n + i))),
          (this._drag.stage.current = r),
          this.animate(r.x));
      }),
      (o.prototype.onDragEnd = function (e) {
        var i = this.difference(this._drag.pointer, this.pointer(e)),
          o = this._drag.stage.current,
          r = (i.x > 0) ^ this.settings.rtl ? 'left' : 'right';
        t(n).off('.owl.core'),
          this.$element.removeClass(this.options.grabClass),
          ((0 !== i.x && this.is('dragging')) || !this.is('valid')) &&
            (this.speed(this.settings.dragEndSpeed || this.settings.smartSpeed),
            this.current(
              this.closest(o.x, 0 !== i.x ? r : this._drag.direction)
            ),
            this.invalidate('position'),
            this.update(),
            (this._drag.direction = r),
            (Math.abs(i.x) > 3 ||
              new Date().getTime() - this._drag.time > 300) &&
              this._drag.target.one('click.owl.core', function () {
                return !1;
              })),
          this.is('dragging') &&
            (this.leave('dragging'), this.trigger('dragged'));
      }),
      (o.prototype.closest = function (e, n) {
        var o = -1,
          r = 30,
          s = this.width(),
          a = this.coordinates();
        return (
          this.settings.freeDrag ||
            t.each(
              a,
              t.proxy(function (t, l) {
                return (
                  'left' === n && e > l - r && e < l + r
                    ? (o = t)
                    : 'right' === n && e > l - s - r && e < l - s + r
                    ? (o = t + 1)
                    : this.op(e, '<', l) &&
                      this.op(e, '>', a[t + 1] !== i ? a[t + 1] : l - s) &&
                      (o = 'left' === n ? t + 1 : t),
                  -1 === o
                );
              }, this)
            ),
          this.settings.loop ||
            (this.op(e, '>', a[this.minimum()])
              ? (o = e = this.minimum())
              : this.op(e, '<', a[this.maximum()]) && (o = e = this.maximum())),
          o
        );
      }),
      (o.prototype.animate = function (e) {
        var n = this.speed() > 0;
        this.is('animating') && this.onTransitionEnd(),
          n && (this.enter('animating'), this.trigger('translate')),
          t.support.transform3d && t.support.transition
            ? this.$stage.css({
                transform: 'translate3d(' + e + 'px,0px,0px)',
                transition:
                  this.speed() / 1e3 +
                  's' +
                  (this.settings.slideTransition
                    ? ' ' + this.settings.slideTransition
                    : ''),
              })
            : n
            ? this.$stage.animate(
                { left: e + 'px' },
                this.speed(),
                this.settings.fallbackEasing,
                t.proxy(this.onTransitionEnd, this)
              )
            : this.$stage.css({ left: e + 'px' });
      }),
      (o.prototype.is = function (t) {
        return this._states.current[t] && this._states.current[t] > 0;
      }),
      (o.prototype.current = function (t) {
        if (t === i) return this._current;
        if (0 === this._items.length) return i;
        if (((t = this.normalize(t)), this._current !== t)) {
          var e = this.trigger('change', {
            property: { name: 'position', value: t },
          });
          e.data !== i && (t = this.normalize(e.data)),
            (this._current = t),
            this.invalidate('position'),
            this.trigger('changed', {
              property: { name: 'position', value: this._current },
            });
        }
        return this._current;
      }),
      (o.prototype.invalidate = function (e) {
        return (
          'string' === t.type(e) &&
            ((this._invalidated[e] = !0),
            this.is('valid') && this.leave('valid')),
          t.map(this._invalidated, function (t, e) {
            return e;
          })
        );
      }),
      (o.prototype.reset = function (t) {
        (t = this.normalize(t)) !== i &&
          ((this._speed = 0),
          (this._current = t),
          this.suppress(['translate', 'translated']),
          this.animate(this.coordinates(t)),
          this.release(['translate', 'translated']));
      }),
      (o.prototype.normalize = function (t, e) {
        var n = this._items.length,
          o = e ? 0 : this._clones.length;
        return (
          !this.isNumeric(t) || n < 1
            ? (t = i)
            : (t < 0 || t >= n + o) &&
              (t = ((((t - o / 2) % n) + n) % n) + o / 2),
          t
        );
      }),
      (o.prototype.relative = function (t) {
        return (t -= this._clones.length / 2), this.normalize(t, !0);
      }),
      (o.prototype.maximum = function (t) {
        var e,
          n,
          i,
          o = this.settings,
          r = this._coordinates.length;
        if (o.loop) r = this._clones.length / 2 + this._items.length - 1;
        else if (o.autoWidth || o.merge) {
          if ((e = this._items.length))
            for (
              n = this._items[--e].width(), i = this.$element.width();
              e-- &&
              !((n += this._items[e].width() + this.settings.margin) > i);

            );
          r = e + 1;
        } else
          r = o.center ? this._items.length - 1 : this._items.length - o.items;
        return t && (r -= this._clones.length / 2), Math.max(r, 0);
      }),
      (o.prototype.minimum = function (t) {
        return t ? 0 : this._clones.length / 2;
      }),
      (o.prototype.items = function (t) {
        return t === i
          ? this._items.slice()
          : ((t = this.normalize(t, !0)), this._items[t]);
      }),
      (o.prototype.mergers = function (t) {
        return t === i
          ? this._mergers.slice()
          : ((t = this.normalize(t, !0)), this._mergers[t]);
      }),
      (o.prototype.clones = function (e) {
        var n = this._clones.length / 2,
          o = n + this._items.length,
          r = function (t) {
            return t % 2 == 0 ? o + t / 2 : n - (t + 1) / 2;
          };
        return e === i
          ? t.map(this._clones, function (t, e) {
              return r(e);
            })
          : t.map(this._clones, function (t, n) {
              return t === e ? r(n) : null;
            });
      }),
      (o.prototype.speed = function (t) {
        return t !== i && (this._speed = t), this._speed;
      }),
      (o.prototype.coordinates = function (e) {
        var n,
          o = 1,
          r = e - 1;
        return e === i
          ? t.map(
              this._coordinates,
              t.proxy(function (t, e) {
                return this.coordinates(e);
              }, this)
            )
          : (this.settings.center
              ? (this.settings.rtl && ((o = -1), (r = e + 1)),
                (n = this._coordinates[e]),
                (n +=
                  ((this.width() - n + (this._coordinates[r] || 0)) / 2) * o))
              : (n = this._coordinates[r] || 0),
            (n = Math.ceil(n)));
      }),
      (o.prototype.duration = function (t, e, n) {
        return 0 === n
          ? 0
          : Math.min(Math.max(Math.abs(e - t), 1), 6) *
              Math.abs(n || this.settings.smartSpeed);
      }),
      (o.prototype.to = function (t, e) {
        var n = this.current(),
          i = null,
          o = t - this.relative(n),
          r = (o > 0) - (o < 0),
          s = this._items.length,
          a = this.minimum(),
          l = this.maximum();
        this.settings.loop
          ? (!this.settings.rewind && Math.abs(o) > s / 2 && (o += -1 * r * s),
            (t = n + o),
            (i = ((((t - a) % s) + s) % s) + a) !== t &&
              i - o <= l &&
              i - o > 0 &&
              ((n = i - o), (t = i), this.reset(n)))
          : this.settings.rewind
          ? ((l += 1), (t = ((t % l) + l) % l))
          : (t = Math.max(a, Math.min(l, t))),
          this.speed(this.duration(n, t, e)),
          this.current(t),
          this.isVisible() && this.update();
      }),
      (o.prototype.next = function (t) {
        (t = t || !1), this.to(this.relative(this.current()) + 1, t);
      }),
      (o.prototype.prev = function (t) {
        (t = t || !1), this.to(this.relative(this.current()) - 1, t);
      }),
      (o.prototype.onTransitionEnd = function (t) {
        if (
          t !== i &&
          (t.stopPropagation(),
          (t.target || t.srcElement || t.originalTarget) !== this.$stage.get(0))
        )
          return !1;
        this.leave('animating'), this.trigger('translated');
      }),
      (o.prototype.viewport = function () {
        var i;
        return (
          this.options.responsiveBaseElement !== e
            ? (i = t(this.options.responsiveBaseElement).width())
            : e.innerWidth
            ? (i = e.innerWidth)
            : n.documentElement && n.documentElement.clientWidth
            ? (i = n.documentElement.clientWidth)
            : console.warn('Can not detect viewport width.'),
          i
        );
      }),
      (o.prototype.replace = function (e) {
        this.$stage.empty(),
          (this._items = []),
          e && (e = e instanceof jQuery ? e : t(e)),
          this.settings.nestedItemSelector &&
            (e = e.find('.' + this.settings.nestedItemSelector)),
          e
            .filter(function () {
              return 1 === this.nodeType;
            })
            .each(
              t.proxy(function (t, e) {
                (e = this.prepare(e)),
                  this.$stage.append(e),
                  this._items.push(e),
                  this._mergers.push(
                    1 *
                      e
                        .find('[data-merge]')
                        .addBack('[data-merge]')
                        .attr('data-merge') || 1
                  );
              }, this)
            ),
          this.reset(
            this.isNumeric(this.settings.startPosition)
              ? this.settings.startPosition
              : 0
          ),
          this.invalidate('items');
      }),
      (o.prototype.add = function (e, n) {
        var o = this.relative(this._current);
        (n = n === i ? this._items.length : this.normalize(n, !0)),
          (e = e instanceof jQuery ? e : t(e)),
          this.trigger('add', { content: e, position: n }),
          (e = this.prepare(e)),
          0 === this._items.length || n === this._items.length
            ? (0 === this._items.length && this.$stage.append(e),
              0 !== this._items.length && this._items[n - 1].after(e),
              this._items.push(e),
              this._mergers.push(
                1 *
                  e
                    .find('[data-merge]')
                    .addBack('[data-merge]')
                    .attr('data-merge') || 1
              ))
            : (this._items[n].before(e),
              this._items.splice(n, 0, e),
              this._mergers.splice(
                n,
                0,
                1 *
                  e
                    .find('[data-merge]')
                    .addBack('[data-merge]')
                    .attr('data-merge') || 1
              )),
          this._items[o] && this.reset(this._items[o].index()),
          this.invalidate('items'),
          this.trigger('added', { content: e, position: n });
      }),
      (o.prototype.remove = function (t) {
        (t = this.normalize(t, !0)) !== i &&
          (this.trigger('remove', { content: this._items[t], position: t }),
          this._items[t].remove(),
          this._items.splice(t, 1),
          this._mergers.splice(t, 1),
          this.invalidate('items'),
          this.trigger('removed', { content: null, position: t }));
      }),
      (o.prototype.preloadAutoWidthImages = function (e) {
        e.each(
          t.proxy(function (e, n) {
            this.enter('pre-loading'),
              (n = t(n)),
              t(new Image())
                .one(
                  'load',
                  t.proxy(function (t) {
                    n.attr('src', t.target.src),
                      n.css('opacity', 1),
                      this.leave('pre-loading'),
                      !this.is('pre-loading') &&
                        !this.is('initializing') &&
                        this.refresh();
                  }, this)
                )
                .attr(
                  'src',
                  n.attr('src') ||
                    n.attr('data-src') ||
                    n.attr('data-src-retina')
                );
          }, this)
        );
      }),
      (o.prototype.destroy = function () {
        this.$element.off('.owl.core'),
          this.$stage.off('.owl.core'),
          t(n).off('.owl.core'),
          !1 !== this.settings.responsive &&
            (e.clearTimeout(this.resizeTimer),
            this.off(e, 'resize', this._handlers.onThrottledResize));
        for (var i in this._plugins) this._plugins[i].destroy();
        this.$stage.children('.cloned').remove(),
          this.$stage.unwrap(),
          this.$stage.children().contents().unwrap(),
          this.$stage.children().unwrap(),
          this.$stage.remove(),
          this.$element
            .removeClass(this.options.refreshClass)
            .removeClass(this.options.loadingClass)
            .removeClass(this.options.loadedClass)
            .removeClass(this.options.rtlClass)
            .removeClass(this.options.dragClass)
            .removeClass(this.options.grabClass)
            .attr(
              'class',
              this.$element
                .attr('class')
                .replace(
                  new RegExp(this.options.responsiveClass + '-\\S+\\s', 'g'),
                  ''
                )
            )
            .removeData('owl.carousel');
      }),
      (o.prototype.op = function (t, e, n) {
        var i = this.settings.rtl;
        switch (e) {
          case '<':
            return i ? t > n : t < n;
          case '>':
            return i ? t < n : t > n;
          case '>=':
            return i ? t <= n : t >= n;
          case '<=':
            return i ? t >= n : t <= n;
        }
      }),
      (o.prototype.on = function (t, e, n, i) {
        t.addEventListener
          ? t.addEventListener(e, n, i)
          : t.attachEvent && t.attachEvent('on' + e, n);
      }),
      (o.prototype.off = function (t, e, n, i) {
        t.removeEventListener
          ? t.removeEventListener(e, n, i)
          : t.detachEvent && t.detachEvent('on' + e, n);
      }),
      (o.prototype.trigger = function (e, n, i, r, s) {
        var a = { item: { count: this._items.length, index: this.current() } },
          l = t.camelCase(
            t
              .grep(['on', e, i], function (t) {
                return t;
              })
              .join('-')
              .toLowerCase()
          ),
          c = t.Event(
            [e, 'owl', i || 'carousel'].join('.').toLowerCase(),
            t.extend({ relatedTarget: this }, a, n)
          );
        return (
          this._supress[e] ||
            (t.each(this._plugins, function (t, e) {
              e.onTrigger && e.onTrigger(c);
            }),
            this.register({ type: o.Type.Event, name: e }),
            this.$element.trigger(c),
            this.settings &&
              'function' == typeof this.settings[l] &&
              this.settings[l].call(this, c)),
          c
        );
      }),
      (o.prototype.enter = function (e) {
        t.each(
          [e].concat(this._states.tags[e] || []),
          t.proxy(function (t, e) {
            this._states.current[e] === i && (this._states.current[e] = 0),
              this._states.current[e]++;
          }, this)
        );
      }),
      (o.prototype.leave = function (e) {
        t.each(
          [e].concat(this._states.tags[e] || []),
          t.proxy(function (t, e) {
            this._states.current[e]--;
          }, this)
        );
      }),
      (o.prototype.register = function (e) {
        if (e.type === o.Type.Event) {
          if (
            (t.event.special[e.name] || (t.event.special[e.name] = {}),
            !t.event.special[e.name].owl)
          ) {
            var n = t.event.special[e.name]._default;
            (t.event.special[e.name]._default = function (t) {
              return !n ||
                !n.apply ||
                (t.namespace && -1 !== t.namespace.indexOf('owl'))
                ? t.namespace && t.namespace.indexOf('owl') > -1
                : n.apply(this, arguments);
            }),
              (t.event.special[e.name].owl = !0);
          }
        } else
          e.type === o.Type.State &&
            (this._states.tags[e.name]
              ? (this._states.tags[e.name] = this._states.tags[e.name].concat(
                  e.tags
                ))
              : (this._states.tags[e.name] = e.tags),
            (this._states.tags[e.name] = t.grep(
              this._states.tags[e.name],
              t.proxy(function (n, i) {
                return t.inArray(n, this._states.tags[e.name]) === i;
              }, this)
            )));
      }),
      (o.prototype.suppress = function (e) {
        t.each(
          e,
          t.proxy(function (t, e) {
            this._supress[e] = !0;
          }, this)
        );
      }),
      (o.prototype.release = function (e) {
        t.each(
          e,
          t.proxy(function (t, e) {
            delete this._supress[e];
          }, this)
        );
      }),
      (o.prototype.pointer = function (t) {
        var n = { x: null, y: null };
        return (
          (t = t.originalEvent || t || e.event),
          (t =
            t.touches && t.touches.length
              ? t.touches[0]
              : t.changedTouches && t.changedTouches.length
              ? t.changedTouches[0]
              : t),
          t.pageX
            ? ((n.x = t.pageX), (n.y = t.pageY))
            : ((n.x = t.clientX), (n.y = t.clientY)),
          n
        );
      }),
      (o.prototype.isNumeric = function (t) {
        return !isNaN(parseFloat(t));
      }),
      (o.prototype.difference = function (t, e) {
        return { x: t.x - e.x, y: t.y - e.y };
      }),
      (t.fn.owlCarousel = function (e) {
        var n = Array.prototype.slice.call(arguments, 1);
        return this.each(function () {
          var i = t(this),
            r = i.data('owl.carousel');
          r ||
            ((r = new o(this, 'object' == typeof e && e)),
            i.data('owl.carousel', r),
            t.each(
              [
                'next',
                'prev',
                'to',
                'destroy',
                'refresh',
                'replace',
                'add',
                'remove',
              ],
              function (e, n) {
                r.register({ type: o.Type.Event, name: n }),
                  r.$element.on(
                    n + '.owl.carousel.core',
                    t.proxy(function (t) {
                      t.namespace &&
                        t.relatedTarget !== this &&
                        (this.suppress([n]),
                        r[n].apply(this, [].slice.call(arguments, 1)),
                        this.release([n]));
                    }, r)
                  );
              }
            )),
            'string' == typeof e && '_' !== e.charAt(0) && r[e].apply(r, n);
        });
      }),
      (t.fn.owlCarousel.Constructor = o);
  })(window.Zepto || window.jQuery, window, document),
  (function (t, e, n, i) {
    var o = function (e) {
      (this._core = e),
        (this._interval = null),
        (this._visible = null),
        (this._handlers = {
          'initialized.owl.carousel': t.proxy(function (t) {
            t.namespace && this._core.settings.autoRefresh && this.watch();
          }, this),
        }),
        (this._core.options = t.extend({}, o.Defaults, this._core.options)),
        this._core.$element.on(this._handlers);
    };
    (o.Defaults = { autoRefresh: !0, autoRefreshInterval: 500 }),
      (o.prototype.watch = function () {
        this._interval ||
          ((this._visible = this._core.isVisible()),
          (this._interval = e.setInterval(
            t.proxy(this.refresh, this),
            this._core.settings.autoRefreshInterval
          )));
      }),
      (o.prototype.refresh = function () {
        this._core.isVisible() !== this._visible &&
          ((this._visible = !this._visible),
          this._core.$element.toggleClass('owl-hidden', !this._visible),
          this._visible &&
            this._core.invalidate('width') &&
            this._core.refresh());
      }),
      (o.prototype.destroy = function () {
        var t, n;
        e.clearInterval(this._interval);
        for (t in this._handlers) this._core.$element.off(t, this._handlers[t]);
        for (n in Object.getOwnPropertyNames(this))
          'function' != typeof this[n] && (this[n] = null);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.AutoRefresh = o);
  })(window.Zepto || window.jQuery, window, document),
  (function (t, e, n, i) {
    var o = function (e) {
      (this._core = e),
        (this._loaded = []),
        (this._handlers = {
          'initialized.owl.carousel change.owl.carousel resized.owl.carousel':
            t.proxy(function (e) {
              if (
                e.namespace &&
                this._core.settings &&
                this._core.settings.lazyLoad &&
                ((e.property && 'position' == e.property.name) ||
                  'initialized' == e.type)
              ) {
                var n = this._core.settings,
                  o = (n.center && Math.ceil(n.items / 2)) || n.items,
                  r = (n.center && -1 * o) || 0,
                  s =
                    (e.property && e.property.value !== i
                      ? e.property.value
                      : this._core.current()) + r,
                  a = this._core.clones().length,
                  l = t.proxy(function (t, e) {
                    this.load(e);
                  }, this);
                for (
                  n.lazyLoadEager > 0 &&
                  ((o += n.lazyLoadEager),
                  n.loop && ((s -= n.lazyLoadEager), o++));
                  r++ < o;

                )
                  this.load(a / 2 + this._core.relative(s)),
                    a && t.each(this._core.clones(this._core.relative(s)), l),
                    s++;
              }
            }, this),
        }),
        (this._core.options = t.extend({}, o.Defaults, this._core.options)),
        this._core.$element.on(this._handlers);
    };
    (o.Defaults = { lazyLoad: !1, lazyLoadEager: 0 }),
      (o.prototype.load = function (n) {
        var i = this._core.$stage.children().eq(n),
          o = i && i.find('.owl-lazy');
        !o ||
          t.inArray(i.get(0), this._loaded) > -1 ||
          (o.each(
            t.proxy(function (n, i) {
              var o,
                r = t(i),
                s =
                  (e.devicePixelRatio > 1 && r.attr('data-src-retina')) ||
                  r.attr('data-src') ||
                  r.attr('data-srcset');
              this._core.trigger('load', { element: r, url: s }, 'lazy'),
                r.is('img')
                  ? r
                      .one(
                        'load.owl.lazy',
                        t.proxy(function () {
                          r.css('opacity', 1),
                            this._core.trigger(
                              'loaded',
                              { element: r, url: s },
                              'lazy'
                            );
                        }, this)
                      )
                      .attr('src', s)
                  : r.is('source')
                  ? r
                      .one(
                        'load.owl.lazy',
                        t.proxy(function () {
                          this._core.trigger(
                            'loaded',
                            { element: r, url: s },
                            'lazy'
                          );
                        }, this)
                      )
                      .attr('srcset', s)
                  : ((o = new Image()),
                    (o.onload = t.proxy(function () {
                      r.css({
                        'background-image': 'url("' + s + '")',
                        opacity: '1',
                      }),
                        this._core.trigger(
                          'loaded',
                          { element: r, url: s },
                          'lazy'
                        );
                    }, this)),
                    (o.src = s));
            }, this)
          ),
          this._loaded.push(i.get(0)));
      }),
      (o.prototype.destroy = function () {
        var t, e;
        for (t in this.handlers) this._core.$element.off(t, this.handlers[t]);
        for (e in Object.getOwnPropertyNames(this))
          'function' != typeof this[e] && (this[e] = null);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.Lazy = o);
  })(window.Zepto || window.jQuery, window, document),
  (function (t, e, n, i) {
    var o = function (n) {
      (this._core = n),
        (this._previousHeight = null),
        (this._handlers = {
          'initialized.owl.carousel refreshed.owl.carousel': t.proxy(function (
            t
          ) {
            t.namespace && this._core.settings.autoHeight && this.update();
          },
          this),
          'changed.owl.carousel': t.proxy(function (t) {
            t.namespace &&
              this._core.settings.autoHeight &&
              'position' === t.property.name &&
              this.update();
          }, this),
          'loaded.owl.lazy': t.proxy(function (t) {
            t.namespace &&
              this._core.settings.autoHeight &&
              t.element.closest('.' + this._core.settings.itemClass).index() ===
                this._core.current() &&
              this.update();
          }, this),
        }),
        (this._core.options = t.extend({}, o.Defaults, this._core.options)),
        this._core.$element.on(this._handlers),
        (this._intervalId = null);
      var i = this;
      t(e).on('load', function () {
        i._core.settings.autoHeight && i.update();
      }),
        t(e).resize(function () {
          i._core.settings.autoHeight &&
            (null != i._intervalId && clearTimeout(i._intervalId),
            (i._intervalId = setTimeout(function () {
              i.update();
            }, 250)));
        });
    };
    (o.Defaults = { autoHeight: !1, autoHeightClass: 'owl-height' }),
      (o.prototype.update = function () {
        var e = this._core._current,
          n = e + this._core.settings.items,
          i = this._core.settings.lazyLoad,
          o = this._core.$stage.children().toArray().slice(e, n),
          r = [],
          s = 0;
        t.each(o, function (e, n) {
          r.push(t(n).height());
        }),
          (s = Math.max.apply(null, r)),
          s <= 1 && i && this._previousHeight && (s = this._previousHeight),
          (this._previousHeight = s),
          this._core.$stage
            .parent()
            .height(s)
            .addClass(this._core.settings.autoHeightClass);
      }),
      (o.prototype.destroy = function () {
        var t, e;
        for (t in this._handlers) this._core.$element.off(t, this._handlers[t]);
        for (e in Object.getOwnPropertyNames(this))
          'function' != typeof this[e] && (this[e] = null);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.AutoHeight = o);
  })(window.Zepto || window.jQuery, window, document),
  (function (t, e, n, i) {
    var o = function (e) {
      (this._core = e),
        (this._videos = {}),
        (this._playing = null),
        (this._handlers = {
          'initialized.owl.carousel': t.proxy(function (t) {
            t.namespace &&
              this._core.register({
                type: 'state',
                name: 'playing',
                tags: ['interacting'],
              });
          }, this),
          'resize.owl.carousel': t.proxy(function (t) {
            t.namespace &&
              this._core.settings.video &&
              this.isInFullScreen() &&
              t.preventDefault();
          }, this),
          'refreshed.owl.carousel': t.proxy(function (t) {
            t.namespace &&
              this._core.is('resizing') &&
              this._core.$stage.find('.cloned .owl-video-frame').remove();
          }, this),
          'changed.owl.carousel': t.proxy(function (t) {
            t.namespace &&
              'position' === t.property.name &&
              this._playing &&
              this.stop();
          }, this),
          'prepared.owl.carousel': t.proxy(function (e) {
            if (e.namespace) {
              var n = t(e.content).find('.owl-video');
              n.length &&
                (n.css('display', 'none'), this.fetch(n, t(e.content)));
            }
          }, this),
        }),
        (this._core.options = t.extend({}, o.Defaults, this._core.options)),
        this._core.$element.on(this._handlers),
        this._core.$element.on(
          'click.owl.video',
          '.owl-video-play-icon',
          t.proxy(function (t) {
            this.play(t);
          }, this)
        );
    };
    (o.Defaults = { video: !1, videoHeight: !1, videoWidth: !1 }),
      (o.prototype.fetch = function (t, e) {
        var n = (function () {
            return t.attr('data-vimeo-id')
              ? 'vimeo'
              : t.attr('data-vzaar-id')
              ? 'vzaar'
              : 'youtube';
          })(),
          i =
            t.attr('data-vimeo-id') ||
            t.attr('data-youtube-id') ||
            t.attr('data-vzaar-id'),
          o = t.attr('data-width') || this._core.settings.videoWidth,
          r = t.attr('data-height') || this._core.settings.videoHeight,
          s = t.attr('href');
        if (!s) throw new Error('Missing video URL.');
        if (
          ((i = s.match(
            /(http:|https:|)\/\/(player.|www.|app.)?(vimeo\.com|youtu(be\.com|\.be|be\.googleapis\.com|be\-nocookie\.com)|vzaar\.com)\/(video\/|videos\/|embed\/|channels\/.+\/|groups\/.+\/|watch\?v=|v\/)?([A-Za-z0-9._%-]*)(\&\S+)?/
          )),
          i[3].indexOf('youtu') > -1)
        )
          n = 'youtube';
        else if (i[3].indexOf('vimeo') > -1) n = 'vimeo';
        else {
          if (!(i[3].indexOf('vzaar') > -1))
            throw new Error('Video URL not supported.');
          n = 'vzaar';
        }
        (i = i[6]),
          (this._videos[s] = { type: n, id: i, width: o, height: r }),
          e.attr('data-video', s),
          this.thumbnail(t, this._videos[s]);
      }),
      (o.prototype.thumbnail = function (e, n) {
        var i,
          o,
          r,
          s =
            n.width && n.height
              ? 'width:' + n.width + 'px;height:' + n.height + 'px;'
              : '',
          a = e.find('img'),
          l = 'src',
          c = '',
          u = this._core.settings,
          d = function (n) {
            (o = '<div class="owl-video-play-icon"></div>'),
              (i = u.lazyLoad
                ? t('<div/>', { class: 'owl-video-tn ' + c, srcType: n })
                : t('<div/>', {
                    class: 'owl-video-tn',
                    style: 'opacity:1;background-image:url(' + n + ')',
                  })),
              e.after(i),
              e.after(o);
          };
        if (
          (e.wrap(t('<div/>', { class: 'owl-video-wrapper', style: s })),
          this._core.settings.lazyLoad && ((l = 'data-src'), (c = 'owl-lazy')),
          a.length)
        )
          return d(a.attr(l)), a.remove(), !1;
        'youtube' === n.type
          ? ((r = '//img.youtube.com/vi/' + n.id + '/hqdefault.jpg'), d(r))
          : 'vimeo' === n.type
          ? t.ajax({
              type: 'GET',
              url: '//vimeo.com/api/v2/video/' + n.id + '.json',
              jsonp: 'callback',
              dataType: 'jsonp',
              success: function (t) {
                (r = t[0].thumbnail_large), d(r);
              },
            })
          : 'vzaar' === n.type &&
            t.ajax({
              type: 'GET',
              url: '//vzaar.com/api/videos/' + n.id + '.json',
              jsonp: 'callback',
              dataType: 'jsonp',
              success: function (t) {
                (r = t.framegrab_url), d(r);
              },
            });
      }),
      (o.prototype.stop = function () {
        this._core.trigger('stop', null, 'video'),
          this._playing.find('.owl-video-frame').remove(),
          this._playing.removeClass('owl-video-playing'),
          (this._playing = null),
          this._core.leave('playing'),
          this._core.trigger('stopped', null, 'video');
      }),
      (o.prototype.play = function (e) {
        var n,
          i = t(e.target),
          o = i.closest('.' + this._core.settings.itemClass),
          r = this._videos[o.attr('data-video')],
          s = r.width || '100%',
          a = r.height || this._core.$stage.height();
        this._playing ||
          (this._core.enter('playing'),
          this._core.trigger('play', null, 'video'),
          (o = this._core.items(this._core.relative(o.index()))),
          this._core.reset(o.index()),
          (n = t(
            '<iframe frameborder="0" allowfullscreen mozallowfullscreen webkitAllowFullScreen ></iframe>'
          )),
          n.attr('height', a),
          n.attr('width', s),
          'youtube' === r.type
            ? n.attr(
                'src',
                '//www.youtube.com/embed/' +
                  r.id +
                  '?autoplay=1&rel=0&v=' +
                  r.id
              )
            : 'vimeo' === r.type
            ? n.attr('src', '//player.vimeo.com/video/' + r.id + '?autoplay=1')
            : 'vzaar' === r.type &&
              n.attr(
                'src',
                '//view.vzaar.com/' + r.id + '/player?autoplay=true'
              ),
          t(n)
            .wrap('<div class="owl-video-frame" />')
            .insertAfter(o.find('.owl-video')),
          (this._playing = o.addClass('owl-video-playing')));
      }),
      (o.prototype.isInFullScreen = function () {
        var e =
          n.fullscreenElement ||
          n.mozFullScreenElement ||
          n.webkitFullscreenElement;
        return e && t(e).parent().hasClass('owl-video-frame');
      }),
      (o.prototype.destroy = function () {
        var t, e;
        this._core.$element.off('click.owl.video');
        for (t in this._handlers) this._core.$element.off(t, this._handlers[t]);
        for (e in Object.getOwnPropertyNames(this))
          'function' != typeof this[e] && (this[e] = null);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.Video = o);
  })(window.Zepto || window.jQuery, window, document),
  (function (t, e, n, i) {
    var o = function (e) {
      (this.core = e),
        (this.core.options = t.extend({}, o.Defaults, this.core.options)),
        (this.swapping = !0),
        (this.previous = i),
        (this.next = i),
        (this.handlers = {
          'change.owl.carousel': t.proxy(function (t) {
            t.namespace &&
              'position' == t.property.name &&
              ((this.previous = this.core.current()),
              (this.next = t.property.value));
          }, this),
          'drag.owl.carousel dragged.owl.carousel translated.owl.carousel':
            t.proxy(function (t) {
              t.namespace && (this.swapping = 'translated' == t.type);
            }, this),
          'translate.owl.carousel': t.proxy(function (t) {
            t.namespace &&
              this.swapping &&
              (this.core.options.animateOut || this.core.options.animateIn) &&
              this.swap();
          }, this),
        }),
        this.core.$element.on(this.handlers);
    };
    (o.Defaults = { animateOut: !1, animateIn: !1 }),
      (o.prototype.swap = function () {
        if (
          1 === this.core.settings.items &&
          t.support.animation &&
          t.support.transition
        ) {
          this.core.speed(0);
          var e,
            n = t.proxy(this.clear, this),
            i = this.core.$stage.children().eq(this.previous),
            o = this.core.$stage.children().eq(this.next),
            r = this.core.settings.animateIn,
            s = this.core.settings.animateOut;
          this.core.current() !== this.previous &&
            (s &&
              ((e =
                this.core.coordinates(this.previous) -
                this.core.coordinates(this.next)),
              i
                .one(t.support.animation.end, n)
                .css({ left: e + 'px' })
                .addClass('animated owl-animated-out')
                .addClass(s)),
            r &&
              o
                .one(t.support.animation.end, n)
                .addClass('animated owl-animated-in')
                .addClass(r));
        }
      }),
      (o.prototype.clear = function (e) {
        t(e.target)
          .css({ left: '' })
          .removeClass('animated owl-animated-out owl-animated-in')
          .removeClass(this.core.settings.animateIn)
          .removeClass(this.core.settings.animateOut),
          this.core.onTransitionEnd();
      }),
      (o.prototype.destroy = function () {
        var t, e;
        for (t in this.handlers) this.core.$element.off(t, this.handlers[t]);
        for (e in Object.getOwnPropertyNames(this))
          'function' != typeof this[e] && (this[e] = null);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.Animate = o);
  })(window.Zepto || window.jQuery, window, document),
  (function (t, e, n, i) {
    var o = function (e) {
      (this._core = e),
        (this._call = null),
        (this._time = 0),
        (this._timeout = 0),
        (this._paused = !0),
        (this._handlers = {
          'changed.owl.carousel': t.proxy(function (t) {
            t.namespace && 'settings' === t.property.name
              ? this._core.settings.autoplay
                ? this.play()
                : this.stop()
              : t.namespace &&
                'position' === t.property.name &&
                this._paused &&
                (this._time = 0);
          }, this),
          'initialized.owl.carousel': t.proxy(function (t) {
            t.namespace && this._core.settings.autoplay && this.play();
          }, this),
          'play.owl.autoplay': t.proxy(function (t, e, n) {
            t.namespace && this.play(e, n);
          }, this),
          'stop.owl.autoplay': t.proxy(function (t) {
            t.namespace && this.stop();
          }, this),
          'mouseover.owl.autoplay': t.proxy(function () {
            this._core.settings.autoplayHoverPause &&
              this._core.is('rotating') &&
              this.pause();
          }, this),
          'mouseleave.owl.autoplay': t.proxy(function () {
            this._core.settings.autoplayHoverPause &&
              this._core.is('rotating') &&
              this.play();
          }, this),
          'touchstart.owl.core': t.proxy(function () {
            this._core.settings.autoplayHoverPause &&
              this._core.is('rotating') &&
              this.pause();
          }, this),
          'touchend.owl.core': t.proxy(function () {
            this._core.settings.autoplayHoverPause && this.play();
          }, this),
        }),
        this._core.$element.on(this._handlers),
        (this._core.options = t.extend({}, o.Defaults, this._core.options));
    };
    (o.Defaults = {
      autoplay: !1,
      autoplayTimeout: 5e3,
      autoplayHoverPause: !1,
      autoplaySpeed: !1,
    }),
      (o.prototype._next = function (i) {
        (this._call = e.setTimeout(
          t.proxy(this._next, this, i),
          this._timeout * (Math.round(this.read() / this._timeout) + 1) -
            this.read()
        )),
          this._core.is('interacting') ||
            n.hidden ||
            this._core.next(i || this._core.settings.autoplaySpeed);
      }),
      (o.prototype.read = function () {
        return new Date().getTime() - this._time;
      }),
      (o.prototype.play = function (n, i) {
        var o;
        this._core.is('rotating') || this._core.enter('rotating'),
          (n = n || this._core.settings.autoplayTimeout),
          (o = Math.min(this._time % (this._timeout || n), n)),
          this._paused
            ? ((this._time = this.read()), (this._paused = !1))
            : e.clearTimeout(this._call),
          (this._time += (this.read() % n) - o),
          (this._timeout = n),
          (this._call = e.setTimeout(t.proxy(this._next, this, i), n - o));
      }),
      (o.prototype.stop = function () {
        this._core.is('rotating') &&
          ((this._time = 0),
          (this._paused = !0),
          e.clearTimeout(this._call),
          this._core.leave('rotating'));
      }),
      (o.prototype.pause = function () {
        this._core.is('rotating') &&
          !this._paused &&
          ((this._time = this.read()),
          (this._paused = !0),
          e.clearTimeout(this._call));
      }),
      (o.prototype.destroy = function () {
        var t, e;
        this.stop();
        for (t in this._handlers) this._core.$element.off(t, this._handlers[t]);
        for (e in Object.getOwnPropertyNames(this))
          'function' != typeof this[e] && (this[e] = null);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.autoplay = o);
  })(window.Zepto || window.jQuery, window, document),
  (function (t, e, n, i) {
    'use strict';
    var o = function (e) {
      (this._core = e),
        (this._initialized = !1),
        (this._pages = []),
        (this._controls = {}),
        (this._templates = []),
        (this.$element = this._core.$element),
        (this._overrides = {
          next: this._core.next,
          prev: this._core.prev,
          to: this._core.to,
        }),
        (this._handlers = {
          'prepared.owl.carousel': t.proxy(function (e) {
            e.namespace &&
              this._core.settings.dotsData &&
              this._templates.push(
                '<div class="' +
                  this._core.settings.dotClass +
                  '">' +
                  t(e.content)
                    .find('[data-dot]')
                    .addBack('[data-dot]')
                    .attr('data-dot') +
                  '</div>'
              );
          }, this),
          'added.owl.carousel': t.proxy(function (t) {
            t.namespace &&
              this._core.settings.dotsData &&
              this._templates.splice(t.position, 0, this._templates.pop());
          }, this),
          'remove.owl.carousel': t.proxy(function (t) {
            t.namespace &&
              this._core.settings.dotsData &&
              this._templates.splice(t.position, 1);
          }, this),
          'changed.owl.carousel': t.proxy(function (t) {
            t.namespace && 'position' == t.property.name && this.draw();
          }, this),
          'initialized.owl.carousel': t.proxy(function (t) {
            t.namespace &&
              !this._initialized &&
              (this._core.trigger('initialize', null, 'navigation'),
              this.initialize(),
              this.update(),
              this.draw(),
              (this._initialized = !0),
              this._core.trigger('initialized', null, 'navigation'));
          }, this),
          'refreshed.owl.carousel': t.proxy(function (t) {
            t.namespace &&
              this._initialized &&
              (this._core.trigger('refresh', null, 'navigation'),
              this.update(),
              this.draw(),
              this._core.trigger('refreshed', null, 'navigation'));
          }, this),
        }),
        (this._core.options = t.extend({}, o.Defaults, this._core.options)),
        this.$element.on(this._handlers);
    };
    (o.Defaults = {
      nav: !1,
      navText: [
        '<span aria-label="Previous">&#x2039;</span>',
        '<span aria-label="Next">&#x203a;</span>',
      ],
      navSpeed: !1,
      navElement: 'button type="button" role="presentation"',
      navContainer: !1,
      navContainerClass: 'owl-nav',
      navClass: ['owl-prev', 'owl-next'],
      slideBy: 1,
      dotClass: 'owl-dot',
      dotsClass: 'owl-dots',
      dots: !0,
      dotsEach: !1,
      dotsData: !1,
      dotsSpeed: !1,
      dotsContainer: !1,
    }),
      (o.prototype.initialize = function () {
        var e,
          n = this._core.settings;
        (this._controls.$relative = (
          n.navContainer
            ? t(n.navContainer)
            : t('<div>').addClass(n.navContainerClass).appendTo(this.$element)
        ).addClass('disabled')),
          (this._controls.$previous = t('<' + n.navElement + '>')
            .addClass(n.navClass[0])
            .html(n.navText[0])
            .prependTo(this._controls.$relative)
            .on(
              'click',
              t.proxy(function (t) {
                this.prev(n.navSpeed);
              }, this)
            )),
          (this._controls.$next = t('<' + n.navElement + '>')
            .addClass(n.navClass[1])
            .html(n.navText[1])
            .appendTo(this._controls.$relative)
            .on(
              'click',
              t.proxy(function (t) {
                this.next(n.navSpeed);
              }, this)
            )),
          n.dotsData ||
            (this._templates = [
              t('<button role="button">')
                .addClass(n.dotClass)
                .append(t('<span>'))
                .prop('outerHTML'),
            ]),
          (this._controls.$absolute = (
            n.dotsContainer
              ? t(n.dotsContainer)
              : t('<div>').addClass(n.dotsClass).appendTo(this.$element)
          ).addClass('disabled')),
          this._controls.$absolute.on(
            'click',
            'button',
            t.proxy(function (e) {
              var i = t(e.target).parent().is(this._controls.$absolute)
                ? t(e.target).index()
                : t(e.target).parent().index();
              e.preventDefault(), this.to(i, n.dotsSpeed);
            }, this)
          );
        for (e in this._overrides) this._core[e] = t.proxy(this[e], this);
      }),
      (o.prototype.destroy = function () {
        var t, e, n, i, o;
        o = this._core.settings;
        for (t in this._handlers) this.$element.off(t, this._handlers[t]);
        for (e in this._controls)
          '$relative' === e && o.navContainer
            ? this._controls[e].html('')
            : this._controls[e].remove();
        for (i in this.overides) this._core[i] = this._overrides[i];
        for (n in Object.getOwnPropertyNames(this))
          'function' != typeof this[n] && (this[n] = null);
      }),
      (o.prototype.update = function () {
        var t,
          e,
          n,
          i = this._core.clones().length / 2,
          o = i + this._core.items().length,
          r = this._core.maximum(!0),
          s = this._core.settings,
          a = s.center || s.autoWidth || s.dotsData ? 1 : s.dotsEach || s.items;
        if (
          ('page' !== s.slideBy && (s.slideBy = Math.min(s.slideBy, s.items)),
          s.dots || 'page' == s.slideBy)
        )
          for (this._pages = [], t = i, e = 0, n = 0; t < o; t++) {
            if (e >= a || 0 === e) {
              if (
                (this._pages.push({
                  start: Math.min(r, t - i),
                  end: t - i + a - 1,
                }),
                Math.min(r, t - i) === r)
              )
                break;
              (e = 0), ++n;
            }
            e += this._core.mergers(this._core.relative(t));
          }
      }),
      (o.prototype.draw = function () {
        var e,
          n = this._core.settings,
          i = this._core.items().length <= n.items,
          o = this._core.relative(this._core.current()),
          r = n.loop || n.rewind;
        this._controls.$relative.toggleClass('disabled', !n.nav || i),
          n.nav &&
            (this._controls.$previous.toggleClass(
              'disabled',
              !r && o <= this._core.minimum(!0)
            ),
            this._controls.$next.toggleClass(
              'disabled',
              !r && o >= this._core.maximum(!0)
            )),
          this._controls.$absolute.toggleClass('disabled', !n.dots || i),
          n.dots &&
            ((e =
              this._pages.length - this._controls.$absolute.children().length),
            n.dotsData && 0 !== e
              ? this._controls.$absolute.html(this._templates.join(''))
              : e > 0
              ? this._controls.$absolute.append(
                  new Array(e + 1).join(this._templates[0])
                )
              : e < 0 && this._controls.$absolute.children().slice(e).remove(),
            this._controls.$absolute.find('.active').removeClass('active'),
            this._controls.$absolute
              .children()
              .eq(t.inArray(this.current(), this._pages))
              .addClass('active'));
      }),
      (o.prototype.onTrigger = function (e) {
        var n = this._core.settings;
        e.page = {
          index: t.inArray(this.current(), this._pages),
          count: this._pages.length,
          size:
            n &&
            (n.center || n.autoWidth || n.dotsData ? 1 : n.dotsEach || n.items),
        };
      }),
      (o.prototype.current = function () {
        var e = this._core.relative(this._core.current());
        return t
          .grep(
            this._pages,
            t.proxy(function (t, n) {
              return t.start <= e && t.end >= e;
            }, this)
          )
          .pop();
      }),
      (o.prototype.getPosition = function (e) {
        var n,
          i,
          o = this._core.settings;
        return (
          'page' == o.slideBy
            ? ((n = t.inArray(this.current(), this._pages)),
              (i = this._pages.length),
              e ? ++n : --n,
              (n = this._pages[((n % i) + i) % i].start))
            : ((n = this._core.relative(this._core.current())),
              (i = this._core.items().length),
              e ? (n += o.slideBy) : (n -= o.slideBy)),
          n
        );
      }),
      (o.prototype.next = function (e) {
        t.proxy(this._overrides.to, this._core)(this.getPosition(!0), e);
      }),
      (o.prototype.prev = function (e) {
        t.proxy(this._overrides.to, this._core)(this.getPosition(!1), e);
      }),
      (o.prototype.to = function (e, n, i) {
        var o;
        !i && this._pages.length
          ? ((o = this._pages.length),
            t.proxy(this._overrides.to, this._core)(
              this._pages[((e % o) + o) % o].start,
              n
            ))
          : t.proxy(this._overrides.to, this._core)(e, n);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.Navigation = o);
  })(window.Zepto || window.jQuery, window, document),
  (function (t, e, n, i) {
    'use strict';
    var o = function (n) {
      (this._core = n),
        (this._hashes = {}),
        (this.$element = this._core.$element),
        (this._handlers = {
          'initialized.owl.carousel': t.proxy(function (n) {
            n.namespace &&
              'URLHash' === this._core.settings.startPosition &&
              t(e).trigger('hashchange.owl.navigation');
          }, this),
          'prepared.owl.carousel': t.proxy(function (e) {
            if (e.namespace) {
              var n = t(e.content)
                .find('[data-hash]')
                .addBack('[data-hash]')
                .attr('data-hash');
              if (!n) return;
              this._hashes[n] = e.content;
            }
          }, this),
          'changed.owl.carousel': t.proxy(function (n) {
            if (n.namespace && 'position' === n.property.name) {
              var i = this._core.items(
                  this._core.relative(this._core.current())
                ),
                o = t
                  .map(this._hashes, function (t, e) {
                    return t === i ? e : null;
                  })
                  .join();
              if (!o || e.location.hash.slice(1) === o) return;
              e.location.hash = o;
            }
          }, this),
        }),
        (this._core.options = t.extend({}, o.Defaults, this._core.options)),
        this.$element.on(this._handlers),
        t(e).on(
          'hashchange.owl.navigation',
          t.proxy(function (t) {
            var n = e.location.hash.substring(1),
              o = this._core.$stage.children(),
              r = this._hashes[n] && o.index(this._hashes[n]);
            r !== i &&
              r !== this._core.current() &&
              this._core.to(this._core.relative(r), !1, !0);
          }, this)
        );
    };
    (o.Defaults = { URLhashListener: !1 }),
      (o.prototype.destroy = function () {
        var n, i;
        t(e).off('hashchange.owl.navigation');
        for (n in this._handlers) this._core.$element.off(n, this._handlers[n]);
        for (i in Object.getOwnPropertyNames(this))
          'function' != typeof this[i] && (this[i] = null);
      }),
      (t.fn.owlCarousel.Constructor.Plugins.Hash = o);
  })(window.Zepto || window.jQuery, window, document),
  (function (t, e, n, i) {
    function o(e, n) {
      var o = !1,
        r = e.charAt(0).toUpperCase() + e.slice(1);
      return (
        t.each((e + ' ' + a.join(r + ' ') + r).split(' '), function (t, e) {
          if (s[e] !== i) return (o = !n || e), !1;
        }),
        o
      );
    }
    function r(t) {
      return o(t, !0);
    }
    var s = t('<support>').get(0).style,
      a = 'Webkit Moz O ms'.split(' '),
      l = {
        transition: {
          end: {
            WebkitTransition: 'webkitTransitionEnd',
            MozTransition: 'transitionend',
            OTransition: 'oTransitionEnd',
            transition: 'transitionend',
          },
        },
        animation: {
          end: {
            WebkitAnimation: 'webkitAnimationEnd',
            MozAnimation: 'animationend',
            OAnimation: 'oAnimationEnd',
            animation: 'animationend',
          },
        },
      },
      c = {
        csstransforms: function () {
          return !!o('transform');
        },
        csstransforms3d: function () {
          return !!o('perspective');
        },
        csstransitions: function () {
          return !!o('transition');
        },
        cssanimations: function () {
          return !!o('animation');
        },
      };
    c.csstransitions() &&
      ((t.support.transition = new String(r('transition'))),
      (t.support.transition.end = l.transition.end[t.support.transition])),
      c.cssanimations() &&
        ((t.support.animation = new String(r('animation'))),
        (t.support.animation.end = l.animation.end[t.support.animation])),
      c.csstransforms() &&
        ((t.support.transform = new String(r('transform'))),
        (t.support.transform3d = c.csstransforms3d()));
  })(window.Zepto || window.jQuery, window, document),
  (function (t, e, n, i) {
    'use strict';
    function o(t, e) {
      var i,
        o,
        r,
        s = [],
        a = 0;
      (t && t.isDefaultPrevented()) ||
        (t.preventDefault(),
        (e = e || {}),
        t && t.data && (e = f(t.data.options, e)),
        (i = e.$target || n(t.currentTarget).trigger('blur')),
        ((r = n.fancybox.getInstance()) && r.$trigger && r.$trigger.is(i)) ||
          (e.selector
            ? (s = n(e.selector))
            : ((o = i.attr('data-fancybox') || ''),
              o
                ? ((s = t.data ? t.data.items : []),
                  (s = s.length
                    ? s.filter('[data-fancybox="' + o + '"]')
                    : n('[data-fancybox="' + o + '"]')))
                : (s = [i])),
          (a = n(s).index(i)),
          a < 0 && (a = 0),
          (r = n.fancybox.open(s, e, a)),
          (r.$trigger = i)));
    }
    if (((t.console = t.console || { info: function (t) {} }), n)) {
      if (n.fn.fancybox)
        return void console.info('fancyBox already initialized');
      var r = {
          closeExisting: !1,
          loop: !1,
          gutter: 50,
          keyboard: !0,
          preventCaptionOverlap: !0,
          arrows: !0,
          infobar: !0,
          smallBtn: 'auto',
          toolbar: 'auto',
          buttons: ['zoom', 'slideShow', 'thumbs', 'close'],
          idleTime: 3,
          protect: !1,
          modal: !1,
          image: { preload: !1 },
          ajax: { settings: { data: { fancybox: !0 } } },
          iframe: {
            tpl: '<iframe id="fancybox-frame{rnd}" name="fancybox-frame{rnd}" class="fancybox-iframe" allowfullscreen="allowfullscreen" allow="autoplay; fullscreen" src=""></iframe>',
            preload: !0,
            css: {},
            attr: { scrolling: 'auto' },
          },
          video: {
            tpl: '<video class="fancybox-video" controls controlsList="nodownload" poster="{{poster}}"><source src="{{src}}" type="{{format}}" />Sorry, your browser doesn\'t support embedded videos, <a href="{{src}}">download</a> and watch with your favorite video player!</video>',
            format: '',
            autoStart: !0,
          },
          defaultType: 'image',
          animationEffect: 'zoom',
          animationDuration: 366,
          zoomOpacity: 'auto',
          transitionEffect: 'fade',
          transitionDuration: 366,
          slideClass: '',
          baseClass: '',
          baseTpl:
            '<div class="fancybox-container" role="dialog" tabindex="-1"><div class="fancybox-bg"></div><div class="fancybox-inner"><div class="fancybox-infobar"><span data-fancybox-index></span>&nbsp;/&nbsp;<span data-fancybox-count></span></div><div class="fancybox-toolbar">{{buttons}}</div><div class="fancybox-navigation">{{arrows}}</div><div class="fancybox-stage"></div><div class="fancybox-caption"><div class="fancybox-caption__body"></div></div></div></div>',
          spinnerTpl: '<div class="fancybox-loading"></div>',
          errorTpl: '<div class="fancybox-error"><p>{{ERROR}}</p></div>',
          btnTpl: {
            download:
              '<a download data-fancybox-download class="fancybox-button fancybox-button--download" title="{{DOWNLOAD}}" href="javascript:;"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M18.62 17.09V19H5.38v-1.91zm-2.97-6.96L17 11.45l-5 4.87-5-4.87 1.36-1.32 2.68 2.64V5h1.92v7.77z"/></svg></a>',
            zoom: '<button data-fancybox-zoom class="fancybox-button fancybox-button--zoom" title="{{ZOOM}}"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M18.7 17.3l-3-3a5.9 5.9 0 0 0-.6-7.6 5.9 5.9 0 0 0-8.4 0 5.9 5.9 0 0 0 0 8.4 5.9 5.9 0 0 0 7.7.7l3 3a1 1 0 0 0 1.3 0c.4-.5.4-1 0-1.5zM8.1 13.8a4 4 0 0 1 0-5.7 4 4 0 0 1 5.7 0 4 4 0 0 1 0 5.7 4 4 0 0 1-5.7 0z"/></svg></button>',
            close:
              '<button data-fancybox-close class="fancybox-button fancybox-button--close" title="{{CLOSE}}"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M12 10.6L6.6 5.2 5.2 6.6l5.4 5.4-5.4 5.4 1.4 1.4 5.4-5.4 5.4 5.4 1.4-1.4-5.4-5.4 5.4-5.4-1.4-1.4-5.4 5.4z"/></svg></button>',
            arrowLeft:
              '<button data-fancybox-prev class="fancybox-button fancybox-button--arrow_left" title="{{PREV}}"><div><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M11.28 15.7l-1.34 1.37L5 12l4.94-5.07 1.34 1.38-2.68 2.72H19v1.94H8.6z"/></svg></div></button>',
            arrowRight:
              '<button data-fancybox-next class="fancybox-button fancybox-button--arrow_right" title="{{NEXT}}"><div><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M15.4 12.97l-2.68 2.72 1.34 1.38L19 12l-4.94-5.07-1.34 1.38 2.68 2.72H5v1.94z"/></svg></div></button>',
            smallBtn:
              '<button type="button" data-fancybox-close class="fancybox-button fancybox-close-small" title="{{CLOSE}}"><svg width="37" height="37" viewBox="0 0 37 37" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M0.000151038 35.3553L35.3555 8.22544e-06L36.7697 1.41422L1.41436 36.7696L0.000151038 35.3553Z" fill="black"/><path d="M1.41421 0L36.7696 35.3553L35.3553 36.7696L0 1.41421L1.41421 0Z" fill="black"/></svg></button>',
          },
          parentEl: 'body',
          hideScrollbar: !0,
          autoFocus: !0,
          backFocus: !0,
          trapFocus: !0,
          fullScreen: { autoStart: !1 },
          touch: { vertical: !0, momentum: !0 },
          hash: null,
          media: {},
          slideShow: { autoStart: !1, speed: 3e3 },
          thumbs: {
            autoStart: !1,
            hideOnClose: !0,
            parentEl: '.fancybox-container',
            axis: 'y',
          },
          wheel: 'auto',
          onInit: n.noop,
          beforeLoad: n.noop,
          afterLoad: n.noop,
          beforeShow: n.noop,
          afterShow: n.noop,
          beforeClose: n.noop,
          afterClose: n.noop,
          onActivate: n.noop,
          onDeactivate: n.noop,
          clickContent: function (t, e) {
            return 'image' === t.type && 'zoom';
          },
          clickSlide: 'close',
          clickOutside: 'close',
          dblclickContent: !1,
          dblclickSlide: !1,
          dblclickOutside: !1,
          mobile: {
            preventCaptionOverlap: !1,
            idleTime: !1,
            clickContent: function (t, e) {
              return 'image' === t.type && 'toggleControls';
            },
            clickSlide: function (t, e) {
              return 'image' === t.type ? 'toggleControls' : 'close';
            },
            dblclickContent: function (t, e) {
              return 'image' === t.type && 'zoom';
            },
            dblclickSlide: function (t, e) {
              return 'image' === t.type && 'zoom';
            },
          },
          lang: 'en',
          i18n: {
            en: {
              CLOSE: 'Close',
              NEXT: 'Next',
              PREV: 'Previous',
              ERROR:
                'The requested content cannot be loaded. <br/> Please try again later.',
              PLAY_START: 'Start slideshow',
              PLAY_STOP: 'Pause slideshow',
              FULL_SCREEN: 'Full screen',
              THUMBS: 'Thumbnails',
              DOWNLOAD: 'Download',
              SHARE: 'Share',
              ZOOM: 'Zoom',
            },
            de: {
              CLOSE: 'Schlie&szlig;en',
              NEXT: 'Weiter',
              PREV: 'Zur&uuml;ck',
              ERROR:
                'Die angeforderten Daten konnten nicht geladen werden. <br/> Bitte versuchen Sie es sp&auml;ter nochmal.',
              PLAY_START: 'Diaschau starten',
              PLAY_STOP: 'Diaschau beenden',
              FULL_SCREEN: 'Vollbild',
              THUMBS: 'Vorschaubilder',
              DOWNLOAD: 'Herunterladen',
              SHARE: 'Teilen',
              ZOOM: 'Vergr&ouml;&szlig;ern',
            },
          },
        },
        s = n(t),
        a = n(e),
        l = 0,
        c = function (t) {
          return t && t.hasOwnProperty && t instanceof n;
        },
        u = (function () {
          return (
            t.requestAnimationFrame ||
            t.webkitRequestAnimationFrame ||
            t.mozRequestAnimationFrame ||
            t.oRequestAnimationFrame ||
            function (e) {
              return t.setTimeout(e, 1e3 / 60);
            }
          );
        })(),
        d = (function () {
          return (
            t.cancelAnimationFrame ||
            t.webkitCancelAnimationFrame ||
            t.mozCancelAnimationFrame ||
            t.oCancelAnimationFrame ||
            function (e) {
              t.clearTimeout(e);
            }
          );
        })(),
        p = (function () {
          var t,
            n = e.createElement('fakeelement'),
            o = {
              transition: 'transitionend',
              OTransition: 'oTransitionEnd',
              MozTransition: 'transitionend',
              WebkitTransition: 'webkitTransitionEnd',
            };
          for (t in o) if (n.style[t] !== i) return o[t];
          return 'transitionend';
        })(),
        h = function (t) {
          return t && t.length && t[0].offsetHeight;
        },
        f = function (t, e) {
          var i = n.extend(!0, {}, t, e);
          return (
            n.each(e, function (t, e) {
              n.isArray(e) && (i[t] = e);
            }),
            i
          );
        },
        g = function (t) {
          var i, o;
          return (
            !(!t || t.ownerDocument !== e) &&
            (n('.fancybox-container').css('pointer-events', 'none'),
            (i = {
              x: t.getBoundingClientRect().left + t.offsetWidth / 2,
              y: t.getBoundingClientRect().top + t.offsetHeight / 2,
            }),
            (o = e.elementFromPoint(i.x, i.y) === t),
            n('.fancybox-container').css('pointer-events', ''),
            o)
          );
        },
        m = function (t, e, i) {
          var o = this;
          (o.opts = f({ index: i }, n.fancybox.defaults)),
            n.isPlainObject(e) && (o.opts = f(o.opts, e)),
            n.fancybox.isMobile && (o.opts = f(o.opts, o.opts.mobile)),
            (o.id = o.opts.id || ++l),
            (o.currIndex = parseInt(o.opts.index, 10) || 0),
            (o.prevIndex = null),
            (o.prevPos = null),
            (o.currPos = 0),
            (o.firstRun = !0),
            (o.group = []),
            (o.slides = {}),
            o.addContent(t),
            o.group.length && o.init();
        };
      n.extend(m.prototype, {
        init: function () {
          var i,
            o,
            r = this,
            s = r.group[r.currIndex],
            a = s.opts;
          a.closeExisting && n.fancybox.close(!0),
            n('body').addClass('fancybox-active'),
            !n.fancybox.getInstance() &&
              !1 !== a.hideScrollbar &&
              !n.fancybox.isMobile &&
              e.body.scrollHeight > t.innerHeight &&
              (n('head').append(
                '<style id="fancybox-style-noscroll" type="text/css">.compensate-for-scrollbar{margin-right:' +
                  (t.innerWidth - e.documentElement.clientWidth) +
                  'px;}</style>'
              ),
              n('body').addClass('compensate-for-scrollbar')),
            (o = ''),
            n.each(a.buttons, function (t, e) {
              o += a.btnTpl[e] || '';
            }),
            (i = n(
              r.translate(
                r,
                a.baseTpl
                  .replace('{{buttons}}', o)
                  .replace(
                    '{{arrows}}',
                    a.btnTpl.arrowLeft + a.btnTpl.arrowRight
                  )
              )
            )
              .attr('id', 'fancybox-container-' + r.id)
              .addClass(a.baseClass)
              .data('FancyBox', r)
              .appendTo(a.parentEl)),
            (r.$refs = { container: i }),
            [
              'bg',
              'inner',
              'infobar',
              'toolbar',
              'stage',
              'caption',
              'navigation',
            ].forEach(function (t) {
              r.$refs[t] = i.find('.fancybox-' + t);
            }),
            r.trigger('onInit'),
            r.activate(),
            r.jumpTo(r.currIndex);
        },
        translate: function (t, e) {
          var n = t.opts.i18n[t.opts.lang] || t.opts.i18n.en;
          return e.replace(/\{\{(\w+)\}\}/g, function (t, e) {
            return n[e] === i ? t : n[e];
          });
        },
        addContent: function (t) {
          var e,
            o = this,
            r = n.makeArray(t);
          n.each(r, function (t, e) {
            var r,
              s,
              a,
              l,
              c,
              u = {},
              d = {};
            n.isPlainObject(e)
              ? ((u = e), (d = e.opts || e))
              : 'object' === n.type(e) && n(e).length
              ? ((r = n(e)),
                (d = r.data() || {}),
                (d = n.extend(!0, {}, d, d.options)),
                (d.$orig = r),
                (u.src = o.opts.src || d.src || r.attr('href')),
                u.type || u.src || ((u.type = 'inline'), (u.src = e)))
              : (u = { type: 'html', src: e + '' }),
              (u.opts = n.extend(!0, {}, o.opts, d)),
              n.isArray(d.buttons) && (u.opts.buttons = d.buttons),
              n.fancybox.isMobile &&
                u.opts.mobile &&
                (u.opts = f(u.opts, u.opts.mobile)),
              (s = u.type || u.opts.type),
              (l = u.src || ''),
              !s &&
                l &&
                ((a = l.match(/\.(mp4|mov|ogv|webm)((\?|#).*)?$/i))
                  ? ((s = 'video'),
                    u.opts.video.format ||
                      (u.opts.video.format =
                        'video/' + ('ogv' === a[1] ? 'ogg' : a[1])))
                  : l.match(
                      /(^data:image\/[a-z0-9+\/=]*,)|(\.(jp(e|g|eg)|gif|png|bmp|webp|svg|ico)((\?|#).*)?$)/i
                    )
                  ? (s = 'image')
                  : l.match(/\.(pdf)((\?|#).*)?$/i)
                  ? ((s = 'iframe'),
                    (u = n.extend(!0, u, {
                      contentType: 'pdf',
                      opts: { iframe: { preload: !1 } },
                    })))
                  : '#' === l.charAt(0) && (s = 'inline')),
              s ? (u.type = s) : o.trigger('objectNeedsType', u),
              u.contentType ||
                (u.contentType =
                  n.inArray(u.type, ['html', 'inline', 'ajax']) > -1
                    ? 'html'
                    : u.type),
              (u.index = o.group.length),
              'auto' == u.opts.smallBtn &&
                (u.opts.smallBtn =
                  n.inArray(u.type, ['html', 'inline', 'ajax']) > -1),
              'auto' === u.opts.toolbar && (u.opts.toolbar = !u.opts.smallBtn),
              (u.$thumb = u.opts.$thumb || null),
              u.opts.$trigger &&
                u.index === o.opts.index &&
                ((u.$thumb = u.opts.$trigger.find('img:first')),
                u.$thumb.length && (u.opts.$orig = u.opts.$trigger)),
              (u.$thumb && u.$thumb.length) ||
                !u.opts.$orig ||
                (u.$thumb = u.opts.$orig.find('img:first')),
              u.$thumb && !u.$thumb.length && (u.$thumb = null),
              (u.thumb = u.opts.thumb || (u.$thumb ? u.$thumb[0].src : null)),
              'function' === n.type(u.opts.caption) &&
                (u.opts.caption = u.opts.caption.apply(e, [o, u])),
              'function' === n.type(o.opts.caption) &&
                (u.opts.caption = o.opts.caption.apply(e, [o, u])),
              u.opts.caption instanceof n ||
                (u.opts.caption =
                  u.opts.caption === i ? '' : u.opts.caption + ''),
              'ajax' === u.type &&
                ((c = l.split(/\s+/, 2)),
                c.length > 1 &&
                  ((u.src = c.shift()), (u.opts.filter = c.shift()))),
              u.opts.modal &&
                (u.opts = n.extend(!0, u.opts, {
                  trapFocus: !0,
                  infobar: 0,
                  toolbar: 0,
                  smallBtn: 0,
                  keyboard: 0,
                  slideShow: 0,
                  fullScreen: 0,
                  thumbs: 0,
                  touch: 0,
                  clickContent: !1,
                  clickSlide: !1,
                  clickOutside: !1,
                  dblclickContent: !1,
                  dblclickSlide: !1,
                  dblclickOutside: !1,
                })),
              o.group.push(u);
          }),
            Object.keys(o.slides).length &&
              (o.updateControls(),
              (e = o.Thumbs) && e.isActive && (e.create(), e.focus()));
        },
        addEvents: function () {
          var e = this;
          e.removeEvents(),
            e.$refs.container
              .on('click.fb-close', '[data-fancybox-close]', function (t) {
                t.stopPropagation(), t.preventDefault(), e.close(t);
              })
              .on(
                'touchstart.fb-prev click.fb-prev',
                '[data-fancybox-prev]',
                function (t) {
                  t.stopPropagation(), t.preventDefault(), e.previous();
                }
              )
              .on(
                'touchstart.fb-next click.fb-next',
                '[data-fancybox-next]',
                function (t) {
                  t.stopPropagation(), t.preventDefault(), e.next();
                }
              )
              .on('click.fb', '[data-fancybox-zoom]', function (t) {
                e[e.isScaledDown() ? 'scaleToActual' : 'scaleToFit']();
              }),
            s.on('orientationchange.fb resize.fb', function (t) {
              t && t.originalEvent && 'resize' === t.originalEvent.type
                ? (e.requestId && d(e.requestId),
                  (e.requestId = u(function () {
                    e.update(t);
                  })))
                : (e.current &&
                    'iframe' === e.current.type &&
                    e.$refs.stage.hide(),
                  setTimeout(
                    function () {
                      e.$refs.stage.show(), e.update(t);
                    },
                    n.fancybox.isMobile ? 600 : 250
                  ));
            }),
            a.on('keydown.fb', function (t) {
              var i = n.fancybox ? n.fancybox.getInstance() : null,
                o = i.current,
                r = t.keyCode || t.which;
              if (9 == r) return void (o.opts.trapFocus && e.focus(t));
              if (
                !(
                  !o.opts.keyboard ||
                  t.ctrlKey ||
                  t.altKey ||
                  t.shiftKey ||
                  n(t.target).is('input,textarea,video,audio,select')
                )
              )
                return 8 === r || 27 === r
                  ? (t.preventDefault(), void e.close(t))
                  : 37 === r || 38 === r
                  ? (t.preventDefault(), void e.previous())
                  : 39 === r || 40 === r
                  ? (t.preventDefault(), void e.next())
                  : void e.trigger('afterKeydown', t, r);
            }),
            e.group[e.currIndex].opts.idleTime &&
              ((e.idleSecondsCounter = 0),
              a.on(
                'mousemove.fb-idle mouseleave.fb-idle mousedown.fb-idle touchstart.fb-idle touchmove.fb-idle scroll.fb-idle keydown.fb-idle',
                function (t) {
                  (e.idleSecondsCounter = 0),
                    e.isIdle && e.showControls(),
                    (e.isIdle = !1);
                }
              ),
              (e.idleInterval = t.setInterval(function () {
                ++e.idleSecondsCounter >= e.group[e.currIndex].opts.idleTime &&
                  !e.isDragging &&
                  ((e.isIdle = !0),
                  (e.idleSecondsCounter = 0),
                  e.hideControls());
              }, 1e3)));
        },
        removeEvents: function () {
          var e = this;
          s.off('orientationchange.fb resize.fb'),
            a.off('keydown.fb .fb-idle'),
            this.$refs.container.off('.fb-close .fb-prev .fb-next'),
            e.idleInterval &&
              (t.clearInterval(e.idleInterval), (e.idleInterval = null));
        },
        previous: function (t) {
          return this.jumpTo(this.currPos - 1, t);
        },
        next: function (t) {
          return this.jumpTo(this.currPos + 1, t);
        },
        jumpTo: function (t, e) {
          var o,
            r,
            s,
            a,
            l,
            c,
            u,
            d,
            p,
            f = this,
            g = f.group.length;
          if (!(f.isDragging || f.isClosing || (f.isAnimating && f.firstRun))) {
            if (
              ((t = parseInt(t, 10)),
              !(s = f.current ? f.current.opts.loop : f.opts.loop) &&
                (t < 0 || t >= g))
            )
              return !1;
            if (
              ((o = f.firstRun = !Object.keys(f.slides).length),
              (l = f.current),
              (f.prevIndex = f.currIndex),
              (f.prevPos = f.currPos),
              (a = f.createSlide(t)),
              g > 1 &&
                ((s || a.index < g - 1) && f.createSlide(t + 1),
                (s || a.index > 0) && f.createSlide(t - 1)),
              (f.current = a),
              (f.currIndex = a.index),
              (f.currPos = a.pos),
              f.trigger('beforeShow', o),
              f.updateControls(),
              (a.forcedDuration = i),
              n.isNumeric(e)
                ? (a.forcedDuration = e)
                : (e = a.opts[o ? 'animationDuration' : 'transitionDuration']),
              (e = parseInt(e, 10)),
              (r = f.isMoved(a)),
              a.$slide.addClass('fancybox-slide--current'),
              o)
            )
              return (
                a.opts.animationEffect &&
                  e &&
                  f.$refs.container.css('transition-duration', e + 'ms'),
                f.$refs.container.addClass('fancybox-is-open').trigger('focus'),
                f.loadSlide(a),
                void f.preload('image')
              );
            (c = n.fancybox.getTranslate(l.$slide)),
              (u = n.fancybox.getTranslate(f.$refs.stage)),
              n.each(f.slides, function (t, e) {
                n.fancybox.stop(e.$slide, !0);
              }),
              l.pos !== a.pos && (l.isComplete = !1),
              l.$slide.removeClass(
                'fancybox-slide--complete fancybox-slide--current'
              ),
              r
                ? ((p = c.left - (l.pos * c.width + l.pos * l.opts.gutter)),
                  n.each(f.slides, function (t, i) {
                    i.$slide
                      .removeClass('fancybox-animated')
                      .removeClass(function (t, e) {
                        return (e.match(/(^|\s)fancybox-fx-\S+/g) || []).join(
                          ' '
                        );
                      });
                    var o = i.pos * c.width + i.pos * i.opts.gutter;
                    n.fancybox.setTranslate(i.$slide, {
                      top: 0,
                      left: o - u.left + p,
                    }),
                      i.pos !== a.pos &&
                        i.$slide.addClass(
                          'fancybox-slide--' +
                            (i.pos > a.pos ? 'next' : 'previous')
                        ),
                      h(i.$slide),
                      n.fancybox.animate(
                        i.$slide,
                        {
                          top: 0,
                          left:
                            (i.pos - a.pos) * c.width +
                            (i.pos - a.pos) * i.opts.gutter,
                        },
                        e,
                        function () {
                          i.$slide
                            .css({ transform: '', opacity: '' })
                            .removeClass(
                              'fancybox-slide--next fancybox-slide--previous'
                            ),
                            i.pos === f.currPos && f.complete();
                        }
                      );
                  }))
                : e &&
                  a.opts.transitionEffect &&
                  ((d =
                    'fancybox-animated fancybox-fx-' + a.opts.transitionEffect),
                  l.$slide.addClass(
                    'fancybox-slide--' + (l.pos > a.pos ? 'next' : 'previous')
                  ),
                  n.fancybox.animate(
                    l.$slide,
                    d,
                    e,
                    function () {
                      l.$slide
                        .removeClass(d)
                        .removeClass(
                          'fancybox-slide--next fancybox-slide--previous'
                        );
                    },
                    !1
                  )),
              a.isLoaded ? f.revealContent(a) : f.loadSlide(a),
              f.preload('image');
          }
        },
        createSlide: function (t) {
          var e,
            i,
            o = this;
          return (
            (i = t % o.group.length),
            (i = i < 0 ? o.group.length + i : i),
            !o.slides[t] &&
              o.group[i] &&
              ((e = n('<div class="fancybox-slide"></div>').appendTo(
                o.$refs.stage
              )),
              (o.slides[t] = n.extend(!0, {}, o.group[i], {
                pos: t,
                $slide: e,
                isLoaded: !1,
              })),
              o.updateSlide(o.slides[t])),
            o.slides[t]
          );
        },
        scaleToActual: function (t, e, o) {
          var r,
            s,
            a,
            l,
            c,
            u = this,
            d = u.current,
            p = d.$content,
            h = n.fancybox.getTranslate(d.$slide).width,
            f = n.fancybox.getTranslate(d.$slide).height,
            g = d.width,
            m = d.height;
          u.isAnimating ||
            u.isMoved() ||
            !p ||
            'image' != d.type ||
            !d.isLoaded ||
            d.hasError ||
            ((u.isAnimating = !0),
            n.fancybox.stop(p),
            (t = t === i ? 0.5 * h : t),
            (e = e === i ? 0.5 * f : e),
            (r = n.fancybox.getTranslate(p)),
            (r.top -= n.fancybox.getTranslate(d.$slide).top),
            (r.left -= n.fancybox.getTranslate(d.$slide).left),
            (l = g / r.width),
            (c = m / r.height),
            (s = 0.5 * h - 0.5 * g),
            (a = 0.5 * f - 0.5 * m),
            g > h &&
              ((s = r.left * l - (t * l - t)),
              s > 0 && (s = 0),
              s < h - g && (s = h - g)),
            m > f &&
              ((a = r.top * c - (e * c - e)),
              a > 0 && (a = 0),
              a < f - m && (a = f - m)),
            u.updateCursor(g, m),
            n.fancybox.animate(
              p,
              { top: a, left: s, scaleX: l, scaleY: c },
              o || 366,
              function () {
                u.isAnimating = !1;
              }
            ),
            u.SlideShow && u.SlideShow.isActive && u.SlideShow.stop());
        },
        scaleToFit: function (t) {
          var e,
            i = this,
            o = i.current,
            r = o.$content;
          i.isAnimating ||
            i.isMoved() ||
            !r ||
            'image' != o.type ||
            !o.isLoaded ||
            o.hasError ||
            ((i.isAnimating = !0),
            n.fancybox.stop(r),
            (e = i.getFitPos(o)),
            i.updateCursor(e.width, e.height),
            n.fancybox.animate(
              r,
              {
                top: e.top,
                left: e.left,
                scaleX: e.width / r.width(),
                scaleY: e.height / r.height(),
              },
              t || 366,
              function () {
                i.isAnimating = !1;
              }
            ));
        },
        getFitPos: function (t) {
          var e,
            i,
            o,
            r,
            s = this,
            a = t.$content,
            l = t.$slide,
            c = t.width || t.opts.width,
            u = t.height || t.opts.height,
            d = {};
          return (
            !!(t.isLoaded && a && a.length) &&
            ((e = n.fancybox.getTranslate(s.$refs.stage).width),
            (i = n.fancybox.getTranslate(s.$refs.stage).height),
            (e -=
              parseFloat(l.css('paddingLeft')) +
              parseFloat(l.css('paddingRight')) +
              parseFloat(a.css('marginLeft')) +
              parseFloat(a.css('marginRight'))),
            (i -=
              parseFloat(l.css('paddingTop')) +
              parseFloat(l.css('paddingBottom')) +
              parseFloat(a.css('marginTop')) +
              parseFloat(a.css('marginBottom'))),
            (c && u) || ((c = e), (u = i)),
            (o = Math.min(1, e / c, i / u)),
            (c *= o),
            (u *= o),
            c > e - 0.5 && (c = e),
            u > i - 0.5 && (u = i),
            'image' === t.type
              ? ((d.top =
                  Math.floor(0.5 * (i - u)) + parseFloat(l.css('paddingTop'))),
                (d.left =
                  Math.floor(0.5 * (e - c)) + parseFloat(l.css('paddingLeft'))))
              : 'video' === t.contentType &&
                ((r =
                  t.opts.width && t.opts.height
                    ? c / u
                    : t.opts.ratio || 16 / 9),
                u > c / r ? (u = c / r) : c > u * r && (c = u * r)),
            (d.width = c),
            (d.height = u),
            d)
          );
        },
        update: function (t) {
          var e = this;
          n.each(e.slides, function (n, i) {
            e.updateSlide(i, t);
          });
        },
        updateSlide: function (t, e) {
          var i = this,
            o = t && t.$content,
            r = t.width || t.opts.width,
            s = t.height || t.opts.height,
            a = t.$slide;
          i.adjustCaption(t),
            o &&
              (r || s || 'video' === t.contentType) &&
              !t.hasError &&
              (n.fancybox.stop(o),
              n.fancybox.setTranslate(o, i.getFitPos(t)),
              t.pos === i.currPos && ((i.isAnimating = !1), i.updateCursor())),
            i.adjustLayout(t),
            a.length &&
              (a.trigger('refresh'),
              t.pos === i.currPos &&
                i.$refs.toolbar
                  .add(i.$refs.navigation.find('.fancybox-button--arrow_right'))
                  .toggleClass(
                    'compensate-for-scrollbar',
                    a.get(0).scrollHeight > a.get(0).clientHeight
                  )),
            i.trigger('onUpdate', t, e);
        },
        centerSlide: function (t) {
          var e = this,
            o = e.current,
            r = o.$slide;
          !e.isClosing &&
            o &&
            (r.siblings().css({ transform: '', opacity: '' }),
            r
              .parent()
              .children()
              .removeClass('fancybox-slide--previous fancybox-slide--next'),
            n.fancybox.animate(
              r,
              { top: 0, left: 0, opacity: 1 },
              t === i ? 0 : t,
              function () {
                r.css({ transform: '', opacity: '' }),
                  o.isComplete || e.complete();
              },
              !1
            ));
        },
        isMoved: function (t) {
          var e,
            i,
            o = t || this.current;
          return (
            !!o &&
            ((i = n.fancybox.getTranslate(this.$refs.stage)),
            (e = n.fancybox.getTranslate(o.$slide)),
            !o.$slide.hasClass('fancybox-animated') &&
              (Math.abs(e.top - i.top) > 0.5 ||
                Math.abs(e.left - i.left) > 0.5))
          );
        },
        updateCursor: function (t, e) {
          var i,
            o,
            r = this,
            s = r.current,
            a = r.$refs.container;
          s &&
            !r.isClosing &&
            r.Guestures &&
            (a.removeClass(
              'fancybox-is-zoomable fancybox-can-zoomIn fancybox-can-zoomOut fancybox-can-swipe fancybox-can-pan'
            ),
            (i = r.canPan(t, e)),
            (o = !!i || r.isZoomable()),
            a.toggleClass('fancybox-is-zoomable', o),
            n('[data-fancybox-zoom]').prop('disabled', !o),
            i
              ? a.addClass('fancybox-can-pan')
              : o &&
                ('zoom' === s.opts.clickContent ||
                  (n.isFunction(s.opts.clickContent) &&
                    'zoom' == s.opts.clickContent(s)))
              ? a.addClass('fancybox-can-zoomIn')
              : s.opts.touch &&
                (s.opts.touch.vertical || r.group.length > 1) &&
                'video' !== s.contentType &&
                a.addClass('fancybox-can-swipe'));
        },
        isZoomable: function () {
          var t,
            e = this,
            n = e.current;
          if (n && !e.isClosing && 'image' === n.type && !n.hasError) {
            if (!n.isLoaded) return !0;
            if (
              (t = e.getFitPos(n)) &&
              (n.width > t.width || n.height > t.height)
            )
              return !0;
          }
          return !1;
        },
        isScaledDown: function (t, e) {
          var o = this,
            r = !1,
            s = o.current,
            a = s.$content;
          return (
            t !== i && e !== i
              ? (r = t < s.width && e < s.height)
              : a &&
                ((r = n.fancybox.getTranslate(a)),
                (r = r.width < s.width && r.height < s.height)),
            r
          );
        },
        canPan: function (t, e) {
          var o = this,
            r = o.current,
            s = null,
            a = !1;
          return (
            'image' === r.type &&
              (r.isComplete || (t && e)) &&
              !r.hasError &&
              ((a = o.getFitPos(r)),
              t !== i && e !== i
                ? (s = { width: t, height: e })
                : r.isComplete && (s = n.fancybox.getTranslate(r.$content)),
              s &&
                a &&
                (a =
                  Math.abs(s.width - a.width) > 1.5 ||
                  Math.abs(s.height - a.height) > 1.5)),
            a
          );
        },
        loadSlide: function (t) {
          var e,
            i,
            o,
            r = this;
          if (!t.isLoading && !t.isLoaded) {
            if (((t.isLoading = !0), !1 === r.trigger('beforeLoad', t)))
              return (t.isLoading = !1), !1;
            switch (
              ((e = t.type),
              (i = t.$slide),
              i.off('refresh').trigger('onReset').addClass(t.opts.slideClass),
              e)
            ) {
              case 'image':
                r.setImage(t);
                break;
              case 'iframe':
                r.setIframe(t);
                break;
              case 'html':
                r.setContent(t, t.src || t.content);
                break;
              case 'video':
                r.setContent(
                  t,
                  t.opts.video.tpl
                    .replace(/\{\{src\}\}/gi, t.src)
                    .replace(
                      '{{format}}',
                      t.opts.videoFormat || t.opts.video.format || ''
                    )
                    .replace('{{poster}}', t.thumb || '')
                );
                break;
              case 'inline':
                n(t.src).length ? r.setContent(t, n(t.src)) : r.setError(t);
                break;
              case 'ajax':
                r.showLoading(t),
                  (o = n.ajax(
                    n.extend({}, t.opts.ajax.settings, {
                      url: t.src,
                      success: function (e, n) {
                        'success' === n && r.setContent(t, e);
                      },
                      error: function (e, n) {
                        e && 'abort' !== n && r.setError(t);
                      },
                    })
                  )),
                  i.one('onReset', function () {
                    o.abort();
                  });
                break;
              default:
                r.setError(t);
            }
            return !0;
          }
        },
        setImage: function (t) {
          var i,
            o = this;
          setTimeout(function () {
            var e = t.$image;
            o.isClosing ||
              !t.isLoading ||
              (e && e.length && e[0].complete) ||
              t.hasError ||
              o.showLoading(t);
          }, 50),
            o.checkSrcset(t),
            (t.$content = n('<div class="fancybox-content"></div>')
              .addClass('fancybox-is-hidden')
              .appendTo(t.$slide.addClass('fancybox-slide--image'))),
            !1 !== t.opts.preload &&
              t.opts.width &&
              t.opts.height &&
              t.thumb &&
              ((t.width = t.opts.width),
              (t.height = t.opts.height),
              (i = e.createElement('img')),
              (i.onerror = function () {
                n(this).remove(), (t.$ghost = null);
              }),
              (i.onload = function () {
                o.afterLoad(t);
              }),
              (t.$ghost = n(i)
                .addClass('fancybox-image')
                .appendTo(t.$content)
                .attr('src', t.thumb))),
            o.setBigImage(t);
        },
        checkSrcset: function (e) {
          var n,
            i,
            o,
            r,
            s = e.opts.srcset || e.opts.image.srcset;
          if (s) {
            (o = t.devicePixelRatio || 1),
              (r = t.innerWidth * o),
              (i = s.split(',').map(function (t) {
                var e = {};
                return (
                  t
                    .trim()
                    .split(/\s+/)
                    .forEach(function (t, n) {
                      var i = parseInt(t.substring(0, t.length - 1), 10);
                      if (0 === n) return (e.url = t);
                      i && ((e.value = i), (e.postfix = t[t.length - 1]));
                    }),
                  e
                );
              })),
              i.sort(function (t, e) {
                return t.value - e.value;
              });
            for (var a = 0; a < i.length; a++) {
              var l = i[a];
              if (
                ('w' === l.postfix && l.value >= r) ||
                ('x' === l.postfix && l.value >= o)
              ) {
                n = l;
                break;
              }
            }
            !n && i.length && (n = i[i.length - 1]),
              n &&
                ((e.src = n.url),
                e.width &&
                  e.height &&
                  'w' == n.postfix &&
                  ((e.height = (e.width / e.height) * n.value),
                  (e.width = n.value)),
                (e.opts.srcset = s));
          }
        },
        setBigImage: function (t) {
          var i = this,
            o = e.createElement('img'),
            r = n(o);
          (t.$image = r
            .one('error', function () {
              i.setError(t);
            })
            .one('load', function () {
              var e;
              t.$ghost ||
                (i.resolveImageSlideSize(
                  t,
                  this.naturalWidth,
                  this.naturalHeight
                ),
                i.afterLoad(t)),
                i.isClosing ||
                  (t.opts.srcset &&
                    ((e = t.opts.sizes),
                    (e && 'auto' !== e) ||
                      (e =
                        (t.width / t.height > 1 && s.width() / s.height() > 1
                          ? '100'
                          : Math.round((t.width / t.height) * 100)) + 'vw'),
                    r.attr('sizes', e).attr('srcset', t.opts.srcset)),
                  t.$ghost &&
                    setTimeout(function () {
                      t.$ghost && !i.isClosing && t.$ghost.hide();
                    }, Math.min(300, Math.max(1e3, t.height / 1600))),
                  i.hideLoading(t));
            })
            .addClass('fancybox-image')
            .attr('src', t.src)
            .appendTo(t.$content)),
            (o.complete || 'complete' == o.readyState) &&
            r.naturalWidth &&
            r.naturalHeight
              ? r.trigger('load')
              : o.error && r.trigger('error');
        },
        resolveImageSlideSize: function (t, e, n) {
          var i = parseInt(t.opts.width, 10),
            o = parseInt(t.opts.height, 10);
          (t.width = e),
            (t.height = n),
            i > 0 && ((t.width = i), (t.height = Math.floor((i * n) / e))),
            o > 0 && ((t.width = Math.floor((o * e) / n)), (t.height = o));
        },
        setIframe: function (t) {
          var e,
            o = this,
            r = t.opts.iframe,
            s = t.$slide;
          (t.$content = n(
            '<div class="fancybox-content' +
              (r.preload ? ' fancybox-is-hidden' : '') +
              '"></div>'
          )
            .css(r.css)
            .appendTo(s)),
            s.addClass('fancybox-slide--' + t.contentType),
            (t.$iframe = e =
              n(r.tpl.replace(/\{rnd\}/g, new Date().getTime()))
                .attr(r.attr)
                .appendTo(t.$content)),
            r.preload
              ? (o.showLoading(t),
                e.on('load.fb error.fb', function (e) {
                  (this.isReady = 1),
                    t.$slide.trigger('refresh'),
                    o.afterLoad(t);
                }),
                s.on('refresh.fb', function () {
                  var n,
                    o,
                    a = t.$content,
                    l = r.css.width,
                    c = r.css.height;
                  if (1 === e[0].isReady) {
                    try {
                      (n = e.contents()), (o = n.find('body'));
                    } catch (t) {}
                    o &&
                      o.length &&
                      o.children().length &&
                      (s.css('overflow', 'visible'),
                      a.css({
                        width: '100%',
                        'max-width': '100%',
                        height: '9999px',
                      }),
                      l === i &&
                        (l = Math.ceil(
                          Math.max(o[0].clientWidth, o.outerWidth(!0))
                        )),
                      a.css('width', l || '').css('max-width', ''),
                      c === i &&
                        (c = Math.ceil(
                          Math.max(o[0].clientHeight, o.outerHeight(!0))
                        )),
                      a.css('height', c || ''),
                      s.css('overflow', 'auto')),
                      a.removeClass('fancybox-is-hidden');
                  }
                }))
              : o.afterLoad(t),
            e.attr('src', t.src),
            s.one('onReset', function () {
              try {
                n(this)
                  .find('iframe')
                  .hide()
                  .unbind()
                  .attr('src', '//about:blank');
              } catch (t) {}
              n(this).off('refresh.fb').empty(),
                (t.isLoaded = !1),
                (t.isRevealed = !1);
            });
        },
        setContent: function (t, e) {
          var i = this;
          i.isClosing ||
            (i.hideLoading(t),
            t.$content && n.fancybox.stop(t.$content),
            t.$slide.empty(),
            c(e) && e.parent().length
              ? ((e.hasClass('fancybox-content') ||
                  e.parent().hasClass('fancybox-content')) &&
                  e.parents('.fancybox-slide').trigger('onReset'),
                (t.$placeholder = n('<div>').hide().insertAfter(e)),
                e.css('display', 'inline-block'))
              : t.hasError ||
                ('string' === n.type(e) &&
                  (e = n('<div>').append(n.trim(e)).contents()),
                t.opts.filter && (e = n('<div>').html(e).find(t.opts.filter))),
            t.$slide.one('onReset', function () {
              n(this).find('video,audio').trigger('pause'),
                t.$placeholder &&
                  (t.$placeholder
                    .after(e.removeClass('fancybox-content').hide())
                    .remove(),
                  (t.$placeholder = null)),
                t.$smallBtn && (t.$smallBtn.remove(), (t.$smallBtn = null)),
                t.hasError ||
                  (n(this).empty(), (t.isLoaded = !1), (t.isRevealed = !1));
            }),
            n(e).appendTo(t.$slide),
            n(e).is('video,audio') &&
              (n(e).addClass('fancybox-video'),
              n(e).wrap('<div></div>'),
              (t.contentType = 'video'),
              (t.opts.width = t.opts.width || n(e).attr('width')),
              (t.opts.height = t.opts.height || n(e).attr('height'))),
            (t.$content = t.$slide
              .children()
              .filter('div,form,main,video,audio,article,.fancybox-content')
              .first()),
            t.$content.siblings().hide(),
            t.$content.length ||
              (t.$content = t.$slide
                .wrapInner('<div></div>')
                .children()
                .first()),
            t.$content.addClass('fancybox-content'),
            t.$slide.addClass('fancybox-slide--' + t.contentType),
            i.afterLoad(t));
        },
        setError: function (t) {
          (t.hasError = !0),
            t.$slide
              .trigger('onReset')
              .removeClass('fancybox-slide--' + t.contentType)
              .addClass('fancybox-slide--error'),
            (t.contentType = 'html'),
            this.setContent(t, this.translate(t, t.opts.errorTpl)),
            t.pos === this.currPos && (this.isAnimating = !1);
        },
        showLoading: function (t) {
          var e = this;
          (t = t || e.current) &&
            !t.$spinner &&
            (t.$spinner = n(e.translate(e, e.opts.spinnerTpl))
              .appendTo(t.$slide)
              .hide()
              .fadeIn('fast'));
        },
        hideLoading: function (t) {
          var e = this;
          (t = t || e.current) &&
            t.$spinner &&
            (t.$spinner.stop().remove(), delete t.$spinner);
        },
        afterLoad: function (t) {
          var e = this;
          e.isClosing ||
            ((t.isLoading = !1),
            (t.isLoaded = !0),
            e.trigger('afterLoad', t),
            e.hideLoading(t),
            !t.opts.smallBtn ||
              (t.$smallBtn && t.$smallBtn.length) ||
              (t.$smallBtn = n(e.translate(t, t.opts.btnTpl.smallBtn)).appendTo(
                t.$content
              )),
            t.opts.protect &&
              t.$content &&
              !t.hasError &&
              (t.$content.on('contextmenu.fb', function (t) {
                return 2 == t.button && t.preventDefault(), !0;
              }),
              'image' === t.type &&
                n('<div class="fancybox-spaceball"></div>').appendTo(
                  t.$content
                )),
            e.adjustCaption(t),
            e.adjustLayout(t),
            t.pos === e.currPos && e.updateCursor(),
            e.revealContent(t));
        },
        adjustCaption: function (t) {
          var e,
            n = this,
            i = t || n.current,
            o = i.opts.caption,
            r = i.opts.preventCaptionOverlap,
            s = n.$refs.caption,
            a = !1;
          s.toggleClass('fancybox-caption--separate', r),
            r &&
              o &&
              o.length &&
              (i.pos !== n.currPos
                ? ((e = s.clone().appendTo(s.parent())),
                  e.children().eq(0).empty().html(o),
                  (a = e.outerHeight(!0)),
                  e.empty().remove())
                : n.$caption && (a = n.$caption.outerHeight(!0)),
              i.$slide.css('padding-bottom', a || ''));
        },
        adjustLayout: function (t) {
          var e,
            n,
            i,
            o,
            r = this,
            s = t || r.current;
          s.isLoaded &&
            !0 !== s.opts.disableLayoutFix &&
            (s.$content.css('margin-bottom', ''),
            s.$content.outerHeight() > s.$slide.height() + 0.5 &&
              ((i = s.$slide[0].style['padding-bottom']),
              (o = s.$slide.css('padding-bottom')),
              parseFloat(o) > 0 &&
                ((e = s.$slide[0].scrollHeight),
                s.$slide.css('padding-bottom', 0),
                Math.abs(e - s.$slide[0].scrollHeight) < 1 && (n = o),
                s.$slide.css('padding-bottom', i))),
            s.$content.css('margin-bottom', n));
        },
        revealContent: function (t) {
          var e,
            o,
            r,
            s,
            a = this,
            l = t.$slide,
            c = !1,
            u = !1,
            d = a.isMoved(t),
            p = t.isRevealed;
          return (
            (t.isRevealed = !0),
            (e = t.opts[a.firstRun ? 'animationEffect' : 'transitionEffect']),
            (r =
              t.opts[a.firstRun ? 'animationDuration' : 'transitionDuration']),
            (r = parseInt(t.forcedDuration === i ? r : t.forcedDuration, 10)),
            (!d && t.pos === a.currPos && r) || (e = !1),
            'zoom' === e &&
              (t.pos === a.currPos &&
              r &&
              'image' === t.type &&
              !t.hasError &&
              (u = a.getThumbPos(t))
                ? (c = a.getFitPos(t))
                : (e = 'fade')),
            'zoom' === e
              ? ((a.isAnimating = !0),
                (c.scaleX = c.width / u.width),
                (c.scaleY = c.height / u.height),
                (s = t.opts.zoomOpacity),
                'auto' == s &&
                  (s = Math.abs(t.width / t.height - u.width / u.height) > 0.1),
                s && ((u.opacity = 0.1), (c.opacity = 1)),
                n.fancybox.setTranslate(
                  t.$content.removeClass('fancybox-is-hidden'),
                  u
                ),
                h(t.$content),
                void n.fancybox.animate(t.$content, c, r, function () {
                  (a.isAnimating = !1), a.complete();
                }))
              : (a.updateSlide(t),
                e
                  ? (n.fancybox.stop(l),
                    (o =
                      'fancybox-slide--' +
                      (t.pos >= a.prevPos ? 'next' : 'previous') +
                      ' fancybox-animated fancybox-fx-' +
                      e),
                    l.addClass(o).removeClass('fancybox-slide--current'),
                    t.$content.removeClass('fancybox-is-hidden'),
                    h(l),
                    'image' !== t.type && t.$content.hide().show(0),
                    void n.fancybox.animate(
                      l,
                      'fancybox-slide--current',
                      r,
                      function () {
                        l.removeClass(o).css({ transform: '', opacity: '' }),
                          t.pos === a.currPos && a.complete();
                      },
                      !0
                    ))
                  : (t.$content.removeClass('fancybox-is-hidden'),
                    p ||
                      !d ||
                      'image' !== t.type ||
                      t.hasError ||
                      t.$content.hide().fadeIn('fast'),
                    void (t.pos === a.currPos && a.complete())))
          );
        },
        getThumbPos: function (t) {
          var e,
            i,
            o,
            r,
            s,
            a = !1,
            l = t.$thumb;
          return (
            !(!l || !g(l[0])) &&
            ((e = n.fancybox.getTranslate(l)),
            (i = parseFloat(l.css('border-top-width') || 0)),
            (o = parseFloat(l.css('border-right-width') || 0)),
            (r = parseFloat(l.css('border-bottom-width') || 0)),
            (s = parseFloat(l.css('border-left-width') || 0)),
            (a = {
              top: e.top + i,
              left: e.left + s,
              width: e.width - o - s,
              height: e.height - i - r,
              scaleX: 1,
              scaleY: 1,
            }),
            e.width > 0 && e.height > 0 && a)
          );
        },
        complete: function () {
          var t,
            e = this,
            i = e.current,
            o = {};
          !e.isMoved() &&
            i.isLoaded &&
            (i.isComplete ||
              ((i.isComplete = !0),
              i.$slide.siblings().trigger('onReset'),
              e.preload('inline'),
              h(i.$slide),
              i.$slide.addClass('fancybox-slide--complete'),
              n.each(e.slides, function (t, i) {
                i.pos >= e.currPos - 1 && i.pos <= e.currPos + 1
                  ? (o[i.pos] = i)
                  : i && (n.fancybox.stop(i.$slide), i.$slide.off().remove());
              }),
              (e.slides = o)),
            (e.isAnimating = !1),
            e.updateCursor(),
            e.trigger('afterShow'),
            i.opts.video.autoStart &&
              i.$slide
                .find('video,audio')
                .filter(':visible:first')
                .trigger('play')
                .one('ended', function () {
                  Document.exitFullscreen
                    ? Document.exitFullscreen()
                    : this.webkitExitFullscreen && this.webkitExitFullscreen(),
                    e.next();
                }),
            i.opts.autoFocus &&
              'html' === i.contentType &&
              ((t = i.$content.find('input[autofocus]:enabled:visible:first')),
              t.length ? t.trigger('focus') : e.focus(null, !0)),
            i.$slide.scrollTop(0).scrollLeft(0));
        },
        preload: function (t) {
          var e,
            n,
            i = this;
          i.group.length < 2 ||
            ((n = i.slides[i.currPos + 1]),
            (e = i.slides[i.currPos - 1]),
            e && e.type === t && i.loadSlide(e),
            n && n.type === t && i.loadSlide(n));
        },
        focus: function (t, i) {
          var o,
            r,
            s = this,
            a = [
              'a[href]',
              'area[href]',
              'input:not([disabled]):not([type="hidden"]):not([aria-hidden])',
              'select:not([disabled]):not([aria-hidden])',
              'textarea:not([disabled]):not([aria-hidden])',
              'button:not([disabled]):not([aria-hidden])',
              'iframe',
              'object',
              'embed',
              'video',
              'audio',
              '[contenteditable]',
              '[tabindex]:not([tabindex^="-"])',
            ].join(',');
          s.isClosing ||
            ((o =
              !t && s.current && s.current.isComplete
                ? s.current.$slide.find(
                    '*:visible' + (i ? ':not(.fancybox-close-small)' : '')
                  )
                : s.$refs.container.find('*:visible')),
            (o = o.filter(a).filter(function () {
              return (
                'hidden' !== n(this).css('visibility') &&
                !n(this).hasClass('disabled')
              );
            })),
            o.length
              ? ((r = o.index(e.activeElement)),
                t && t.shiftKey
                  ? (r < 0 || 0 == r) &&
                    (t.preventDefault(), o.eq(o.length - 1).trigger('focus'))
                  : (r < 0 || r == o.length - 1) &&
                    (t && t.preventDefault(), o.eq(0).trigger('focus')))
              : s.$refs.container.trigger('focus'));
        },
        activate: function () {
          var t = this;
          n('.fancybox-container').each(function () {
            var e = n(this).data('FancyBox');
            e &&
              e.id !== t.id &&
              !e.isClosing &&
              (e.trigger('onDeactivate'), e.removeEvents(), (e.isVisible = !1));
          }),
            (t.isVisible = !0),
            (t.current || t.isIdle) && (t.update(), t.updateControls()),
            t.trigger('onActivate'),
            t.addEvents();
        },
        close: function (t, e) {
          var i,
            o,
            r,
            s,
            a,
            l,
            c,
            d = this,
            p = d.current,
            f = function () {
              d.cleanUp(t);
            };
          return (
            !d.isClosing &&
            ((d.isClosing = !0),
            !1 === d.trigger('beforeClose', t)
              ? ((d.isClosing = !1),
                u(function () {
                  d.update();
                }),
                !1)
              : (d.removeEvents(),
                (r = p.$content),
                (i = p.opts.animationEffect),
                (o = n.isNumeric(e) ? e : i ? p.opts.animationDuration : 0),
                p.$slide.removeClass(
                  'fancybox-slide--complete fancybox-slide--next fancybox-slide--previous fancybox-animated'
                ),
                !0 !== t ? n.fancybox.stop(p.$slide) : (i = !1),
                p.$slide.siblings().trigger('onReset').remove(),
                o &&
                  d.$refs.container
                    .removeClass('fancybox-is-open')
                    .addClass('fancybox-is-closing')
                    .css('transition-duration', o + 'ms'),
                d.hideLoading(p),
                d.hideControls(!0),
                d.updateCursor(),
                'zoom' !== i ||
                  (r &&
                    o &&
                    'image' === p.type &&
                    !d.isMoved() &&
                    !p.hasError &&
                    (c = d.getThumbPos(p))) ||
                  (i = 'fade'),
                'zoom' === i
                  ? (n.fancybox.stop(r),
                    (s = n.fancybox.getTranslate(r)),
                    (l = {
                      top: s.top,
                      left: s.left,
                      scaleX: s.width / c.width,
                      scaleY: s.height / c.height,
                      width: c.width,
                      height: c.height,
                    }),
                    (a = p.opts.zoomOpacity),
                    'auto' == a &&
                      (a =
                        Math.abs(p.width / p.height - c.width / c.height) >
                        0.1),
                    a && (c.opacity = 0),
                    n.fancybox.setTranslate(r, l),
                    h(r),
                    n.fancybox.animate(r, c, o, f),
                    !0)
                  : (i && o
                      ? n.fancybox.animate(
                          p.$slide
                            .addClass('fancybox-slide--previous')
                            .removeClass('fancybox-slide--current'),
                          'fancybox-animated fancybox-fx-' + i,
                          o,
                          f
                        )
                      : !0 === t
                      ? setTimeout(f, o)
                      : f(),
                    !0)))
          );
        },
        cleanUp: function (e) {
          var i,
            o,
            r,
            s = this,
            a = s.current.opts.$orig;
          s.current.$slide.trigger('onReset'),
            s.$refs.container.empty().remove(),
            s.trigger('afterClose', e),
            s.current.opts.backFocus &&
              ((a && a.length && a.is(':visible')) || (a = s.$trigger),
              a &&
                a.length &&
                ((o = t.scrollX),
                (r = t.scrollY),
                a.trigger('focus'),
                n('html, body').scrollTop(r).scrollLeft(o))),
            (s.current = null),
            (i = n.fancybox.getInstance()),
            i
              ? i.activate()
              : (n('body').removeClass(
                  'fancybox-active compensate-for-scrollbar'
                ),
                n('#fancybox-style-noscroll').remove());
        },
        trigger: function (t, e) {
          var i,
            o = Array.prototype.slice.call(arguments, 1),
            r = this,
            s = e && e.opts ? e : r.current;
          if (
            (s ? o.unshift(s) : (s = r),
            o.unshift(r),
            n.isFunction(s.opts[t]) && (i = s.opts[t].apply(s, o)),
            !1 === i)
          )
            return i;
          'afterClose' !== t && r.$refs
            ? r.$refs.container.trigger(t + '.fb', o)
            : a.trigger(t + '.fb', o);
        },
        updateControls: function () {
          var t = this,
            i = t.current,
            o = i.index,
            r = t.$refs.container,
            s = t.$refs.caption,
            a = i.opts.caption;
          i.$slide.trigger('refresh'),
            a && a.length
              ? ((t.$caption = s), s.children().eq(0).html(a))
              : (t.$caption = null),
            t.hasHiddenControls || t.isIdle || t.showControls(),
            r.find('[data-fancybox-count]').html(t.group.length),
            r.find('[data-fancybox-index]').html(o + 1),
            r
              .find('[data-fancybox-prev]')
              .prop('disabled', !i.opts.loop && o <= 0),
            r
              .find('[data-fancybox-next]')
              .prop('disabled', !i.opts.loop && o >= t.group.length - 1),
            'image' === i.type
              ? r
                  .find('[data-fancybox-zoom]')
                  .show()
                  .end()
                  .find('[data-fancybox-download]')
                  .attr('href', i.opts.image.src || i.src)
                  .show()
              : i.opts.toolbar &&
                r.find('[data-fancybox-download],[data-fancybox-zoom]').hide(),
            n(e.activeElement).is(':hidden,[disabled]') &&
              t.$refs.container.trigger('focus');
        },
        hideControls: function (t) {
          var e = this,
            n = ['infobar', 'toolbar', 'nav'];
          (!t && e.current.opts.preventCaptionOverlap) || n.push('caption'),
            this.$refs.container.removeClass(
              n
                .map(function (t) {
                  return 'fancybox-show-' + t;
                })
                .join(' ')
            ),
            (this.hasHiddenControls = !0);
        },
        showControls: function () {
          var t = this,
            e = t.current ? t.current.opts : t.opts,
            n = t.$refs.container;
          (t.hasHiddenControls = !1),
            (t.idleSecondsCounter = 0),
            n
              .toggleClass('fancybox-show-toolbar', !(!e.toolbar || !e.buttons))
              .toggleClass(
                'fancybox-show-infobar',
                !!(e.infobar && t.group.length > 1)
              )
              .toggleClass('fancybox-show-caption', !!t.$caption)
              .toggleClass(
                'fancybox-show-nav',
                !!(e.arrows && t.group.length > 1)
              )
              .toggleClass('fancybox-is-modal', !!e.modal);
        },
        toggleControls: function () {
          this.hasHiddenControls ? this.showControls() : this.hideControls();
        },
      }),
        (n.fancybox = {
          version: '3.5.7',
          defaults: r,
          getInstance: function (t) {
            var e = n(
                '.fancybox-container:not(".fancybox-is-closing"):last'
              ).data('FancyBox'),
              i = Array.prototype.slice.call(arguments, 1);
            return (
              e instanceof m &&
              ('string' === n.type(t)
                ? e[t].apply(e, i)
                : 'function' === n.type(t) && t.apply(e, i),
              e)
            );
          },
          open: function (t, e, n) {
            return new m(t, e, n);
          },
          close: function (t) {
            var e = this.getInstance();
            e && (e.close(), !0 === t && this.close(t));
          },
          destroy: function () {
            this.close(!0), a.add('body').off('click.fb-start', '**');
          },
          isMobile:
            /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(
              navigator.userAgent
            ),
          use3d: (function () {
            var n = e.createElement('div');
            return (
              t.getComputedStyle &&
              t.getComputedStyle(n) &&
              t.getComputedStyle(n).getPropertyValue('transform') &&
              !(e.documentMode && e.documentMode < 11)
            );
          })(),
          getTranslate: function (t) {
            var e;
            return (
              !(!t || !t.length) &&
              ((e = t[0].getBoundingClientRect()),
              {
                top: e.top || 0,
                left: e.left || 0,
                width: e.width,
                height: e.height,
                opacity: parseFloat(t.css('opacity')),
              })
            );
          },
          setTranslate: function (t, e) {
            var n = '',
              o = {};
            if (t && e)
              return (
                (e.left === i && e.top === i) ||
                  ((n =
                    (e.left === i ? t.position().left : e.left) +
                    'px, ' +
                    (e.top === i ? t.position().top : e.top) +
                    'px'),
                  (n = this.use3d
                    ? 'translate3d(' + n + ', 0px)'
                    : 'translate(' + n + ')')),
                e.scaleX !== i && e.scaleY !== i
                  ? (n += ' scale(' + e.scaleX + ', ' + e.scaleY + ')')
                  : e.scaleX !== i && (n += ' scaleX(' + e.scaleX + ')'),
                n.length && (o.transform = n),
                e.opacity !== i && (o.opacity = e.opacity),
                e.width !== i && (o.width = e.width),
                e.height !== i && (o.height = e.height),
                t.css(o)
              );
          },
          animate: function (t, e, o, r, s) {
            var a,
              l = this;
            n.isFunction(o) && ((r = o), (o = null)),
              l.stop(t),
              (a = l.getTranslate(t)),
              t.on(p, function (c) {
                (!c ||
                  !c.originalEvent ||
                  (t.is(c.originalEvent.target) &&
                    'z-index' != c.originalEvent.propertyName)) &&
                  (l.stop(t),
                  n.isNumeric(o) && t.css('transition-duration', ''),
                  n.isPlainObject(e)
                    ? e.scaleX !== i &&
                      e.scaleY !== i &&
                      l.setTranslate(t, {
                        top: e.top,
                        left: e.left,
                        width: a.width * e.scaleX,
                        height: a.height * e.scaleY,
                        scaleX: 1,
                        scaleY: 1,
                      })
                    : !0 !== s && t.removeClass(e),
                  n.isFunction(r) && r(c));
              }),
              n.isNumeric(o) && t.css('transition-duration', o + 'ms'),
              n.isPlainObject(e)
                ? (e.scaleX !== i &&
                    e.scaleY !== i &&
                    (delete e.width,
                    delete e.height,
                    t.parent().hasClass('fancybox-slide--image') &&
                      t.parent().addClass('fancybox-is-scaling')),
                  n.fancybox.setTranslate(t, e))
                : t.addClass(e),
              t.data(
                'timer',
                setTimeout(function () {
                  t.trigger(p);
                }, o + 33)
              );
          },
          stop: function (t, e) {
            t &&
              t.length &&
              (clearTimeout(t.data('timer')),
              e && t.trigger(p),
              t.off(p).css('transition-duration', ''),
              t.parent().removeClass('fancybox-is-scaling'));
          },
        }),
        (n.fn.fancybox = function (t) {
          var e;
          return (
            (t = t || {}),
            (e = t.selector || !1),
            e
              ? n('body')
                  .off('click.fb-start', e)
                  .on('click.fb-start', e, { options: t }, o)
              : this.off('click.fb-start').on(
                  'click.fb-start',
                  { items: this, options: t },
                  o
                ),
            this
          );
        }),
        a.on('click.fb-start', '[data-fancybox]', o),
        a.on('click.fb-start', '[data-fancybox-trigger]', function (t) {
          n('[data-fancybox="' + n(this).attr('data-fancybox-trigger') + '"]')
            .eq(n(this).attr('data-fancybox-index') || 0)
            .trigger('click.fb-start', { $trigger: n(this) });
        }),
        (function () {
          var t = '.fancybox-button',
            e = 'fancybox-focus',
            i = null;
          a.on('mousedown mouseup focus blur', t, function (o) {
            switch (o.type) {
              case 'mousedown':
                i = n(this);
                break;
              case 'mouseup':
                i = null;
                break;
              case 'focusin':
                n(t).removeClass(e),
                  n(this).is(i) ||
                    n(this).is('[disabled]') ||
                    n(this).addClass(e);
                break;
              case 'focusout':
                n(t).removeClass(e);
            }
          });
        })();
    }
  })(window, document, jQuery),
  (function (t) {
    'use strict';
    var e = {
        youtube: {
          matcher:
            /(youtube\.com|youtu\.be|youtube\-nocookie\.com)\/(watch\?(.*&)?v=|v\/|u\/|embed\/?)?(videoseries\?list=(.*)|[\w-]{11}|\?listType=(.*)&list=(.*))(.*)/i,
          params: {
            autoplay: 1,
            autohide: 1,
            fs: 1,
            rel: 0,
            hd: 1,
            wmode: 'transparent',
            enablejsapi: 1,
            html5: 1,
          },
          paramPlace: 8,
          type: 'iframe',
          url: 'https://www.youtube-nocookie.com/embed/$4',
          thumb: 'https://img.youtube.com/vi/$4/hqdefault.jpg',
        },
        vimeo: {
          matcher: /^.+vimeo.com\/(.*\/)?([\d]+)(.*)?/,
          params: {
            autoplay: 1,
            hd: 1,
            show_title: 1,
            show_byline: 1,
            show_portrait: 0,
            fullscreen: 1,
          },
          paramPlace: 3,
          type: 'iframe',
          url: '//player.vimeo.com/video/$2',
        },
        instagram: {
          matcher: /(instagr\.am|instagram\.com)\/p\/([a-zA-Z0-9_\-]+)\/?/i,
          type: 'image',
          url: '//$1/p/$2/media/?size=l',
        },
        gmap_place: {
          matcher:
            /(maps\.)?google\.([a-z]{2,3}(\.[a-z]{2})?)\/(((maps\/(place\/(.*)\/)?\@(.*),(\d+.?\d+?)z))|(\?ll=))(.*)?/i,
          type: 'iframe',
          url: function (t) {
            return (
              '//maps.google.' +
              t[2] +
              '/?ll=' +
              (t[9]
                ? t[9] +
                  '&z=' +
                  Math.floor(t[10]) +
                  (t[12] ? t[12].replace(/^\//, '&') : '')
                : t[12] + ''
              ).replace(/\?/, '&') +
              '&output=' +
              (t[12] && t[12].indexOf('layer=c') > 0 ? 'svembed' : 'embed')
            );
          },
        },
        gmap_search: {
          matcher:
            /(maps\.)?google\.([a-z]{2,3}(\.[a-z]{2})?)\/(maps\/search\/)(.*)/i,
          type: 'iframe',
          url: function (t) {
            return (
              '//maps.google.' +
              t[2] +
              '/maps?q=' +
              t[5].replace('query=', 'q=').replace('api=1', '') +
              '&output=embed'
            );
          },
        },
      },
      n = function (e, n, i) {
        if (e)
          return (
            (i = i || ''),
            'object' === t.type(i) && (i = t.param(i, !0)),
            t.each(n, function (t, n) {
              e = e.replace('$' + t, n || '');
            }),
            i.length && (e += (e.indexOf('?') > 0 ? '&' : '?') + i),
            e
          );
      };
    t(document).on('objectNeedsType.fb', function (i, o, r) {
      var s,
        a,
        l,
        c,
        u,
        d,
        p,
        h = r.src || '',
        f = !1;
      (s = t.extend(!0, {}, e, r.opts.media)),
        t.each(s, function (e, i) {
          if ((l = h.match(i.matcher))) {
            if (
              ((f = i.type), (p = e), (d = {}), i.paramPlace && l[i.paramPlace])
            ) {
              (u = l[i.paramPlace]),
                '?' == u[0] && (u = u.substring(1)),
                (u = u.split('&'));
              for (var o = 0; o < u.length; ++o) {
                var s = u[o].split('=', 2);
                2 == s.length &&
                  (d[s[0]] = decodeURIComponent(s[1].replace(/\+/g, ' ')));
              }
            }
            return (
              (c = t.extend(!0, {}, i.params, r.opts[e], d)),
              (h =
                'function' === t.type(i.url)
                  ? i.url.call(this, l, c, r)
                  : n(i.url, l, c)),
              (a =
                'function' === t.type(i.thumb)
                  ? i.thumb.call(this, l, c, r)
                  : n(i.thumb, l)),
              'youtube' === e
                ? (h = h.replace(/&t=((\d+)m)?(\d+)s/, function (t, e, n, i) {
                    return (
                      '&start=' +
                      ((n ? 60 * parseInt(n, 10) : 0) + parseInt(i, 10))
                    );
                  }))
                : 'vimeo' === e && (h = h.replace('&%23', '#')),
              !1
            );
          }
        }),
        f
          ? (r.opts.thumb ||
              (r.opts.$thumb && r.opts.$thumb.length) ||
              (r.opts.thumb = a),
            'iframe' === f &&
              (r.opts = t.extend(!0, r.opts, {
                iframe: { preload: !1, attr: { scrolling: 'no' } },
              })),
            t.extend(r, {
              type: f,
              src: h,
              origSrc: r.src,
              contentSource: p,
              contentType:
                'image' === f
                  ? 'image'
                  : 'gmap_place' == p || 'gmap_search' == p
                  ? 'map'
                  : 'video',
            }))
          : h && (r.type = r.opts.defaultType);
    });
    var i = {
      youtube: {
        src: 'https://www.youtube.com/iframe_api',
        class: 'YT',
        loading: !1,
        loaded: !1,
      },
      vimeo: {
        src: 'https://player.vimeo.com/api/player.js',
        class: 'Vimeo',
        loading: !1,
        loaded: !1,
      },
      load: function (t) {
        var e,
          n = this;
        if (this[t].loaded)
          return void setTimeout(function () {
            n.done(t);
          });
        this[t].loading ||
          ((this[t].loading = !0),
          (e = document.createElement('script')),
          (e.type = 'text/javascript'),
          (e.src = this[t].src),
          'youtube' === t
            ? (window.onYouTubeIframeAPIReady = function () {
                (n[t].loaded = !0), n.done(t);
              })
            : (e.onload = function () {
                (n[t].loaded = !0), n.done(t);
              }),
          document.body.appendChild(e));
      },
      done: function (e) {
        var n, i, o;
        'youtube' === e && delete window.onYouTubeIframeAPIReady,
          (n = t.fancybox.getInstance()) &&
            ((i = n.current.$content.find('iframe')),
            'youtube' === e && void 0 !== YT && YT
              ? (o = new YT.Player(i.attr('id'), {
                  events: {
                    onStateChange: function (t) {
                      0 == t.data && n.next();
                    },
                  },
                }))
              : 'vimeo' === e &&
                void 0 !== Vimeo &&
                Vimeo &&
                ((o = new Vimeo.Player(i)),
                o.on('ended', function () {
                  n.next();
                })));
      },
    };
    t(document).on({
      'afterShow.fb': function (t, e, n) {
        e.group.length > 1 &&
          ('youtube' === n.contentSource || 'vimeo' === n.contentSource) &&
          i.load(n.contentSource);
      },
    });
  })(jQuery),
  (function (t, e, n) {
    'use strict';
    var i = (function () {
        return (
          t.requestAnimationFrame ||
          t.webkitRequestAnimationFrame ||
          t.mozRequestAnimationFrame ||
          t.oRequestAnimationFrame ||
          function (e) {
            return t.setTimeout(e, 1e3 / 60);
          }
        );
      })(),
      o = (function () {
        return (
          t.cancelAnimationFrame ||
          t.webkitCancelAnimationFrame ||
          t.mozCancelAnimationFrame ||
          t.oCancelAnimationFrame ||
          function (e) {
            t.clearTimeout(e);
          }
        );
      })(),
      r = function (e) {
        var n = [];
        (e = e.originalEvent || e || t.e),
          (e =
            e.touches && e.touches.length
              ? e.touches
              : e.changedTouches && e.changedTouches.length
              ? e.changedTouches
              : [e]);
        for (var i in e)
          e[i].pageX
            ? n.push({ x: e[i].pageX, y: e[i].pageY })
            : e[i].clientX && n.push({ x: e[i].clientX, y: e[i].clientY });
        return n;
      },
      s = function (t, e, n) {
        return e && t
          ? 'x' === n
            ? t.x - e.x
            : 'y' === n
            ? t.y - e.y
            : Math.sqrt(Math.pow(t.x - e.x, 2) + Math.pow(t.y - e.y, 2))
          : 0;
      },
      a = function (t) {
        if (
          t.is(
            'a,area,button,[role="button"],input,label,select,summary,textarea,video,audio,iframe'
          ) ||
          n.isFunction(t.get(0).onclick) ||
          t.data('selectable')
        )
          return !0;
        for (var e = 0, i = t[0].attributes, o = i.length; e < o; e++)
          if ('data-fancybox-' === i[e].nodeName.substr(0, 14)) return !0;
        return !1;
      },
      l = function (e) {
        var n = t.getComputedStyle(e)['overflow-y'],
          i = t.getComputedStyle(e)['overflow-x'],
          o =
            ('scroll' === n || 'auto' === n) && e.scrollHeight > e.clientHeight,
          r = ('scroll' === i || 'auto' === i) && e.scrollWidth > e.clientWidth;
        return o || r;
      },
      c = function (t) {
        for (var e = !1; ; ) {
          if ((e = l(t.get(0)))) break;
          if (
            ((t = t.parent()),
            !t.length || t.hasClass('fancybox-stage') || t.is('body'))
          )
            break;
        }
        return e;
      },
      u = function (t) {
        var e = this;
        (e.instance = t),
          (e.$bg = t.$refs.bg),
          (e.$stage = t.$refs.stage),
          (e.$container = t.$refs.container),
          e.destroy(),
          e.$container.on(
            'touchstart.fb.touch mousedown.fb.touch',
            n.proxy(e, 'ontouchstart')
          );
      };
    (u.prototype.destroy = function () {
      var t = this;
      t.$container.off('.fb.touch'),
        n(e).off('.fb.touch'),
        t.requestId && (o(t.requestId), (t.requestId = null)),
        t.tapped && (clearTimeout(t.tapped), (t.tapped = null));
    }),
      (u.prototype.ontouchstart = function (i) {
        var o = this,
          l = n(i.target),
          u = o.instance,
          d = u.current,
          p = d.$slide,
          h = d.$content,
          f = 'touchstart' == i.type;
        if (
          (f && o.$container.off('mousedown.fb.touch'),
          (!i.originalEvent || 2 != i.originalEvent.button) &&
            p.length &&
            l.length &&
            !a(l) &&
            !a(l.parent()) &&
            (l.is('img') ||
              !(i.originalEvent.clientX > l[0].clientWidth + l.offset().left)))
        ) {
          if (!d || u.isAnimating || d.$slide.hasClass('fancybox-animated'))
            return i.stopPropagation(), void i.preventDefault();
          (o.realPoints = o.startPoints = r(i)),
            o.startPoints.length &&
              (d.touch && i.stopPropagation(),
              (o.startEvent = i),
              (o.canTap = !0),
              (o.$target = l),
              (o.$content = h),
              (o.opts = d.opts.touch),
              (o.isPanning = !1),
              (o.isSwiping = !1),
              (o.isZooming = !1),
              (o.isScrolling = !1),
              (o.canPan = u.canPan()),
              (o.startTime = new Date().getTime()),
              (o.distanceX = o.distanceY = o.distance = 0),
              (o.canvasWidth = Math.round(p[0].clientWidth)),
              (o.canvasHeight = Math.round(p[0].clientHeight)),
              (o.contentLastPos = null),
              (o.contentStartPos = n.fancybox.getTranslate(o.$content) || {
                top: 0,
                left: 0,
              }),
              (o.sliderStartPos = n.fancybox.getTranslate(p)),
              (o.stagePos = n.fancybox.getTranslate(u.$refs.stage)),
              (o.sliderStartPos.top -= o.stagePos.top),
              (o.sliderStartPos.left -= o.stagePos.left),
              (o.contentStartPos.top -= o.stagePos.top),
              (o.contentStartPos.left -= o.stagePos.left),
              n(e)
                .off('.fb.touch')
                .on(
                  f
                    ? 'touchend.fb.touch touchcancel.fb.touch'
                    : 'mouseup.fb.touch mouseleave.fb.touch',
                  n.proxy(o, 'ontouchend')
                )
                .on(
                  f ? 'touchmove.fb.touch' : 'mousemove.fb.touch',
                  n.proxy(o, 'ontouchmove')
                ),
              n.fancybox.isMobile &&
                e.addEventListener('scroll', o.onscroll, !0),
              (((o.opts || o.canPan) &&
                (l.is(o.$stage) || o.$stage.find(l).length)) ||
                (l.is('.fancybox-image') && i.preventDefault(),
                n.fancybox.isMobile &&
                  l.parents('.fancybox-caption').length)) &&
                ((o.isScrollable = c(l) || c(l.parent())),
                (n.fancybox.isMobile && o.isScrollable) || i.preventDefault(),
                (1 === o.startPoints.length || d.hasError) &&
                  (o.canPan
                    ? (n.fancybox.stop(o.$content), (o.isPanning = !0))
                    : (o.isSwiping = !0),
                  o.$container.addClass('fancybox-is-grabbing')),
                2 === o.startPoints.length &&
                  'image' === d.type &&
                  (d.isLoaded || d.$ghost) &&
                  ((o.canTap = !1),
                  (o.isSwiping = !1),
                  (o.isPanning = !1),
                  (o.isZooming = !0),
                  n.fancybox.stop(o.$content),
                  (o.centerPointStartX =
                    0.5 * (o.startPoints[0].x + o.startPoints[1].x) -
                    n(t).scrollLeft()),
                  (o.centerPointStartY =
                    0.5 * (o.startPoints[0].y + o.startPoints[1].y) -
                    n(t).scrollTop()),
                  (o.percentageOfImageAtPinchPointX =
                    (o.centerPointStartX - o.contentStartPos.left) /
                    o.contentStartPos.width),
                  (o.percentageOfImageAtPinchPointY =
                    (o.centerPointStartY - o.contentStartPos.top) /
                    o.contentStartPos.height),
                  (o.startDistanceBetweenFingers = s(
                    o.startPoints[0],
                    o.startPoints[1]
                  )))));
        }
      }),
      (u.prototype.onscroll = function (t) {
        var n = this;
        (n.isScrolling = !0), e.removeEventListener('scroll', n.onscroll, !0);
      }),
      (u.prototype.ontouchmove = function (t) {
        var e = this;
        return void 0 !== t.originalEvent.buttons &&
          0 === t.originalEvent.buttons
          ? void e.ontouchend(t)
          : e.isScrolling
          ? void (e.canTap = !1)
          : ((e.newPoints = r(t)),
            void (
              (e.opts || e.canPan) &&
              e.newPoints.length &&
              e.newPoints.length &&
              ((e.isSwiping && !0 === e.isSwiping) || t.preventDefault(),
              (e.distanceX = s(e.newPoints[0], e.startPoints[0], 'x')),
              (e.distanceY = s(e.newPoints[0], e.startPoints[0], 'y')),
              (e.distance = s(e.newPoints[0], e.startPoints[0])),
              e.distance > 0 &&
                (e.isSwiping
                  ? e.onSwipe(t)
                  : e.isPanning
                  ? e.onPan()
                  : e.isZooming && e.onZoom()))
            ));
      }),
      (u.prototype.onSwipe = function (e) {
        var r,
          s = this,
          a = s.instance,
          l = s.isSwiping,
          c = s.sliderStartPos.left || 0;
        if (!0 !== l)
          'x' == l &&
            (s.distanceX > 0 &&
            (s.instance.group.length < 2 ||
              (0 === s.instance.current.index && !s.instance.current.opts.loop))
              ? (c += Math.pow(s.distanceX, 0.8))
              : s.distanceX < 0 &&
                (s.instance.group.length < 2 ||
                  (s.instance.current.index === s.instance.group.length - 1 &&
                    !s.instance.current.opts.loop))
              ? (c -= Math.pow(-s.distanceX, 0.8))
              : (c += s.distanceX)),
            (s.sliderLastPos = {
              top: 'x' == l ? 0 : s.sliderStartPos.top + s.distanceY,
              left: c,
            }),
            s.requestId && (o(s.requestId), (s.requestId = null)),
            (s.requestId = i(function () {
              s.sliderLastPos &&
                (n.each(s.instance.slides, function (t, e) {
                  var i = e.pos - s.instance.currPos;
                  n.fancybox.setTranslate(e.$slide, {
                    top: s.sliderLastPos.top,
                    left:
                      s.sliderLastPos.left +
                      i * s.canvasWidth +
                      i * e.opts.gutter,
                  });
                }),
                s.$container.addClass('fancybox-is-sliding'));
            }));
        else if (Math.abs(s.distance) > 10) {
          if (
            ((s.canTap = !1),
            a.group.length < 2 && s.opts.vertical
              ? (s.isSwiping = 'y')
              : a.isDragging ||
                !1 === s.opts.vertical ||
                ('auto' === s.opts.vertical && n(t).width() > 800)
              ? (s.isSwiping = 'x')
              : ((r = Math.abs(
                  (180 * Math.atan2(s.distanceY, s.distanceX)) / Math.PI
                )),
                (s.isSwiping = r > 45 && r < 135 ? 'y' : 'x')),
            'y' === s.isSwiping && n.fancybox.isMobile && s.isScrollable)
          )
            return void (s.isScrolling = !0);
          (a.isDragging = s.isSwiping),
            (s.startPoints = s.newPoints),
            n.each(a.slides, function (t, e) {
              var i, o;
              n.fancybox.stop(e.$slide),
                (i = n.fancybox.getTranslate(e.$slide)),
                (o = n.fancybox.getTranslate(a.$refs.stage)),
                e.$slide
                  .css({
                    transform: '',
                    opacity: '',
                    'transition-duration': '',
                  })
                  .removeClass('fancybox-animated')
                  .removeClass(function (t, e) {
                    return (e.match(/(^|\s)fancybox-fx-\S+/g) || []).join(' ');
                  }),
                e.pos === a.current.pos &&
                  ((s.sliderStartPos.top = i.top - o.top),
                  (s.sliderStartPos.left = i.left - o.left)),
                n.fancybox.setTranslate(e.$slide, {
                  top: i.top - o.top,
                  left: i.left - o.left,
                });
            }),
            a.SlideShow && a.SlideShow.isActive && a.SlideShow.stop();
        }
      }),
      (u.prototype.onPan = function () {
        var t = this;
        if (s(t.newPoints[0], t.realPoints[0]) < (n.fancybox.isMobile ? 10 : 5))
          return void (t.startPoints = t.newPoints);
        (t.canTap = !1),
          (t.contentLastPos = t.limitMovement()),
          t.requestId && o(t.requestId),
          (t.requestId = i(function () {
            n.fancybox.setTranslate(t.$content, t.contentLastPos);
          }));
      }),
      (u.prototype.limitMovement = function () {
        var t,
          e,
          n,
          i,
          o,
          r,
          s = this,
          a = s.canvasWidth,
          l = s.canvasHeight,
          c = s.distanceX,
          u = s.distanceY,
          d = s.contentStartPos,
          p = d.left,
          h = d.top,
          f = d.width,
          g = d.height;
        return (
          (o = f > a ? p + c : p),
          (r = h + u),
          (t = Math.max(0, 0.5 * a - 0.5 * f)),
          (e = Math.max(0, 0.5 * l - 0.5 * g)),
          (n = Math.min(a - f, 0.5 * a - 0.5 * f)),
          (i = Math.min(l - g, 0.5 * l - 0.5 * g)),
          c > 0 && o > t && (o = t - 1 + Math.pow(-t + p + c, 0.8) || 0),
          c < 0 && o < n && (o = n + 1 - Math.pow(n - p - c, 0.8) || 0),
          u > 0 && r > e && (r = e - 1 + Math.pow(-e + h + u, 0.8) || 0),
          u < 0 && r < i && (r = i + 1 - Math.pow(i - h - u, 0.8) || 0),
          { top: r, left: o }
        );
      }),
      (u.prototype.limitPosition = function (t, e, n, i) {
        var o = this,
          r = o.canvasWidth,
          s = o.canvasHeight;
        return (
          n > r
            ? ((t = t > 0 ? 0 : t), (t = t < r - n ? r - n : t))
            : (t = Math.max(0, r / 2 - n / 2)),
          i > s
            ? ((e = e > 0 ? 0 : e), (e = e < s - i ? s - i : e))
            : (e = Math.max(0, s / 2 - i / 2)),
          { top: e, left: t }
        );
      }),
      (u.prototype.onZoom = function () {
        var e = this,
          r = e.contentStartPos,
          a = r.width,
          l = r.height,
          c = r.left,
          u = r.top,
          d = s(e.newPoints[0], e.newPoints[1]),
          p = d / e.startDistanceBetweenFingers,
          h = Math.floor(a * p),
          f = Math.floor(l * p),
          g = (a - h) * e.percentageOfImageAtPinchPointX,
          m = (l - f) * e.percentageOfImageAtPinchPointY,
          v = (e.newPoints[0].x + e.newPoints[1].x) / 2 - n(t).scrollLeft(),
          y = (e.newPoints[0].y + e.newPoints[1].y) / 2 - n(t).scrollTop(),
          b = v - e.centerPointStartX,
          x = y - e.centerPointStartY,
          w = c + (g + b),
          C = u + (m + x),
          _ = { top: C, left: w, scaleX: p, scaleY: p };
        (e.canTap = !1),
          (e.newWidth = h),
          (e.newHeight = f),
          (e.contentLastPos = _),
          e.requestId && o(e.requestId),
          (e.requestId = i(function () {
            n.fancybox.setTranslate(e.$content, e.contentLastPos);
          }));
      }),
      (u.prototype.ontouchend = function (t) {
        var i = this,
          s = i.isSwiping,
          a = i.isPanning,
          l = i.isZooming,
          c = i.isScrolling;
        if (
          ((i.endPoints = r(t)),
          (i.dMs = Math.max(new Date().getTime() - i.startTime, 1)),
          i.$container.removeClass('fancybox-is-grabbing'),
          n(e).off('.fb.touch'),
          e.removeEventListener('scroll', i.onscroll, !0),
          i.requestId && (o(i.requestId), (i.requestId = null)),
          (i.isSwiping = !1),
          (i.isPanning = !1),
          (i.isZooming = !1),
          (i.isScrolling = !1),
          (i.instance.isDragging = !1),
          i.canTap)
        )
          return i.onTap(t);
        (i.speed = 100),
          (i.velocityX = (i.distanceX / i.dMs) * 0.5),
          (i.velocityY = (i.distanceY / i.dMs) * 0.5),
          a ? i.endPanning() : l ? i.endZooming() : i.endSwiping(s, c);
      }),
      (u.prototype.endSwiping = function (t, e) {
        var i = this,
          o = !1,
          r = i.instance.group.length,
          s = Math.abs(i.distanceX),
          a = 'x' == t && r > 1 && ((i.dMs > 130 && s > 10) || s > 50),
          l = 300;
        (i.sliderLastPos = null),
          'y' == t && !e && Math.abs(i.distanceY) > 50
            ? (n.fancybox.animate(
                i.instance.current.$slide,
                {
                  top: i.sliderStartPos.top + i.distanceY + 150 * i.velocityY,
                  opacity: 0,
                },
                200
              ),
              (o = i.instance.close(!0, 250)))
            : a && i.distanceX > 0
            ? (o = i.instance.previous(l))
            : a && i.distanceX < 0 && (o = i.instance.next(l)),
          !1 !== o || ('x' != t && 'y' != t) || i.instance.centerSlide(200),
          i.$container.removeClass('fancybox-is-sliding');
      }),
      (u.prototype.endPanning = function () {
        var t,
          e,
          i,
          o = this;
        o.contentLastPos &&
          (!1 === o.opts.momentum || o.dMs > 350
            ? ((t = o.contentLastPos.left), (e = o.contentLastPos.top))
            : ((t = o.contentLastPos.left + 500 * o.velocityX),
              (e = o.contentLastPos.top + 500 * o.velocityY)),
          (i = o.limitPosition(
            t,
            e,
            o.contentStartPos.width,
            o.contentStartPos.height
          )),
          (i.width = o.contentStartPos.width),
          (i.height = o.contentStartPos.height),
          n.fancybox.animate(o.$content, i, 366));
      }),
      (u.prototype.endZooming = function () {
        var t,
          e,
          i,
          o,
          r = this,
          s = r.instance.current,
          a = r.newWidth,
          l = r.newHeight;
        r.contentLastPos &&
          ((t = r.contentLastPos.left),
          (e = r.contentLastPos.top),
          (o = { top: e, left: t, width: a, height: l, scaleX: 1, scaleY: 1 }),
          n.fancybox.setTranslate(r.$content, o),
          a < r.canvasWidth && l < r.canvasHeight
            ? r.instance.scaleToFit(150)
            : a > s.width || l > s.height
            ? r.instance.scaleToActual(
                r.centerPointStartX,
                r.centerPointStartY,
                150
              )
            : ((i = r.limitPosition(t, e, a, l)),
              n.fancybox.animate(r.$content, i, 150)));
      }),
      (u.prototype.onTap = function (e) {
        var i,
          o = this,
          s = n(e.target),
          a = o.instance,
          l = a.current,
          c = (e && r(e)) || o.startPoints,
          u = c[0] ? c[0].x - n(t).scrollLeft() - o.stagePos.left : 0,
          d = c[0] ? c[0].y - n(t).scrollTop() - o.stagePos.top : 0,
          p = function (t) {
            var i = l.opts[t];
            if ((n.isFunction(i) && (i = i.apply(a, [l, e])), i))
              switch (i) {
                case 'close':
                  a.close(o.startEvent);
                  break;
                case 'toggleControls':
                  a.toggleControls();
                  break;
                case 'next':
                  a.next();
                  break;
                case 'nextOrClose':
                  a.group.length > 1 ? a.next() : a.close(o.startEvent);
                  break;
                case 'zoom':
                  'image' == l.type &&
                    (l.isLoaded || l.$ghost) &&
                    (a.canPan()
                      ? a.scaleToFit()
                      : a.isScaledDown()
                      ? a.scaleToActual(u, d)
                      : a.group.length < 2 && a.close(o.startEvent));
              }
          };
        if (
          (!e.originalEvent || 2 != e.originalEvent.button) &&
          (s.is('img') || !(u > s[0].clientWidth + s.offset().left))
        ) {
          if (
            s.is(
              '.fancybox-bg,.fancybox-inner,.fancybox-outer,.fancybox-container'
            )
          )
            i = 'Outside';
          else if (s.is('.fancybox-slide')) i = 'Slide';
          else {
            if (
              !a.current.$content ||
              !a.current.$content.find(s).addBack().filter(s).length
            )
              return;
            i = 'Content';
          }
          if (o.tapped) {
            if (
              (clearTimeout(o.tapped),
              (o.tapped = null),
              Math.abs(u - o.tapX) > 50 || Math.abs(d - o.tapY) > 50)
            )
              return this;
            p('dblclick' + i);
          } else
            (o.tapX = u),
              (o.tapY = d),
              l.opts['dblclick' + i] &&
              l.opts['dblclick' + i] !== l.opts['click' + i]
                ? (o.tapped = setTimeout(function () {
                    (o.tapped = null), a.isAnimating || p('click' + i);
                  }, 500))
                : p('click' + i);
          return this;
        }
      }),
      n(e)
        .on('onActivate.fb', function (t, e) {
          e && !e.Guestures && (e.Guestures = new u(e));
        })
        .on('beforeClose.fb', function (t, e) {
          e && e.Guestures && e.Guestures.destroy();
        });
  })(window, document, jQuery),
  (function (t, e) {
    'use strict';
    e.extend(!0, e.fancybox.defaults, {
      btnTpl: {
        slideShow:
          '<button data-fancybox-play class="fancybox-button fancybox-button--play" title="{{PLAY_START}}"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M6.5 5.4v13.2l11-6.6z"/></svg><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M8.33 5.75h2.2v12.5h-2.2V5.75zm5.15 0h2.2v12.5h-2.2V5.75z"/></svg></button>',
      },
      slideShow: { autoStart: !1, speed: 3e3, progress: !0 },
    });
    var n = function (t) {
      (this.instance = t), this.init();
    };
    e.extend(n.prototype, {
      timer: null,
      isActive: !1,
      $button: null,
      init: function () {
        var t = this,
          n = t.instance,
          i = n.group[n.currIndex].opts.slideShow;
        (t.$button = n.$refs.toolbar
          .find('[data-fancybox-play]')
          .on('click', function () {
            t.toggle();
          })),
          n.group.length < 2 || !i
            ? t.$button.hide()
            : i.progress &&
              (t.$progress = e(
                '<div class="fancybox-progress"></div>'
              ).appendTo(n.$refs.inner));
      },
      set: function (t) {
        var n = this,
          i = n.instance,
          o = i.current;
        o && (!0 === t || o.opts.loop || i.currIndex < i.group.length - 1)
          ? n.isActive &&
            'video' !== o.contentType &&
            (n.$progress &&
              e.fancybox.animate(
                n.$progress.show(),
                { scaleX: 1 },
                o.opts.slideShow.speed
              ),
            (n.timer = setTimeout(function () {
              i.current.opts.loop || i.current.index != i.group.length - 1
                ? i.next()
                : i.jumpTo(0);
            }, o.opts.slideShow.speed)))
          : (n.stop(), (i.idleSecondsCounter = 0), i.showControls());
      },
      clear: function () {
        var t = this;
        clearTimeout(t.timer),
          (t.timer = null),
          t.$progress && t.$progress.removeAttr('style').hide();
      },
      start: function () {
        var t = this,
          e = t.instance.current;
        e &&
          (t.$button
            .attr(
              'title',
              (e.opts.i18n[e.opts.lang] || e.opts.i18n.en).PLAY_STOP
            )
            .removeClass('fancybox-button--play')
            .addClass('fancybox-button--pause'),
          (t.isActive = !0),
          e.isComplete && t.set(!0),
          t.instance.trigger('onSlideShowChange', !0));
      },
      stop: function () {
        var t = this,
          e = t.instance.current;
        t.clear(),
          t.$button
            .attr(
              'title',
              (e.opts.i18n[e.opts.lang] || e.opts.i18n.en).PLAY_START
            )
            .removeClass('fancybox-button--pause')
            .addClass('fancybox-button--play'),
          (t.isActive = !1),
          t.instance.trigger('onSlideShowChange', !1),
          t.$progress && t.$progress.removeAttr('style').hide();
      },
      toggle: function () {
        var t = this;
        t.isActive ? t.stop() : t.start();
      },
    }),
      e(t).on({
        'onInit.fb': function (t, e) {
          e && !e.SlideShow && (e.SlideShow = new n(e));
        },
        'beforeShow.fb': function (t, e, n, i) {
          var o = e && e.SlideShow;
          i
            ? o && n.opts.slideShow.autoStart && o.start()
            : o && o.isActive && o.clear();
        },
        'afterShow.fb': function (t, e, n) {
          var i = e && e.SlideShow;
          i && i.isActive && i.set();
        },
        'afterKeydown.fb': function (n, i, o, r, s) {
          var a = i && i.SlideShow;
          !a ||
            !o.opts.slideShow ||
            (80 !== s && 32 !== s) ||
            e(t.activeElement).is('button,a,input') ||
            (r.preventDefault(), a.toggle());
        },
        'beforeClose.fb onDeactivate.fb': function (t, e) {
          var n = e && e.SlideShow;
          n && n.stop();
        },
      }),
      e(t).on('visibilitychange', function () {
        var n = e.fancybox.getInstance(),
          i = n && n.SlideShow;
        i && i.isActive && (t.hidden ? i.clear() : i.set());
      });
  })(document, jQuery),
  (function (t, e) {
    'use strict';
    var n = (function () {
      for (
        var e = [
            [
              'requestFullscreen',
              'exitFullscreen',
              'fullscreenElement',
              'fullscreenEnabled',
              'fullscreenchange',
              'fullscreenerror',
            ],
            [
              'webkitRequestFullscreen',
              'webkitExitFullscreen',
              'webkitFullscreenElement',
              'webkitFullscreenEnabled',
              'webkitfullscreenchange',
              'webkitfullscreenerror',
            ],
            [
              'webkitRequestFullScreen',
              'webkitCancelFullScreen',
              'webkitCurrentFullScreenElement',
              'webkitCancelFullScreen',
              'webkitfullscreenchange',
              'webkitfullscreenerror',
            ],
            [
              'mozRequestFullScreen',
              'mozCancelFullScreen',
              'mozFullScreenElement',
              'mozFullScreenEnabled',
              'mozfullscreenchange',
              'mozfullscreenerror',
            ],
            [
              'msRequestFullscreen',
              'msExitFullscreen',
              'msFullscreenElement',
              'msFullscreenEnabled',
              'MSFullscreenChange',
              'MSFullscreenError',
            ],
          ],
          n = {},
          i = 0;
        i < e.length;
        i++
      ) {
        var o = e[i];
        if (o && o[1] in t) {
          for (var r = 0; r < o.length; r++) n[e[0][r]] = o[r];
          return n;
        }
      }
      return !1;
    })();
    if (n) {
      var i = {
        request: function (e) {
          (e = e || t.documentElement),
            e[n.requestFullscreen](e.ALLOW_KEYBOARD_INPUT);
        },
        exit: function () {
          t[n.exitFullscreen]();
        },
        toggle: function (e) {
          (e = e || t.documentElement),
            this.isFullscreen() ? this.exit() : this.request(e);
        },
        isFullscreen: function () {
          return Boolean(t[n.fullscreenElement]);
        },
        enabled: function () {
          return Boolean(t[n.fullscreenEnabled]);
        },
      };
      e.extend(!0, e.fancybox.defaults, {
        btnTpl: {
          fullScreen:
            '<button data-fancybox-fullscreen class="fancybox-button fancybox-button--fsenter" title="{{FULL_SCREEN}}"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M7 14H5v5h5v-2H7v-3zm-2-4h2V7h3V5H5v5zm12 7h-3v2h5v-5h-2v3zM14 5v2h3v3h2V5h-5z"/></svg><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M5 16h3v3h2v-5H5zm3-8H5v2h5V5H8zm6 11h2v-3h3v-2h-5zm2-11V5h-2v5h5V8z"/></svg></button>',
        },
        fullScreen: { autoStart: !1 },
      }),
        e(t).on(n.fullscreenchange, function () {
          var t = i.isFullscreen(),
            n = e.fancybox.getInstance();
          n &&
            (n.current &&
              'image' === n.current.type &&
              n.isAnimating &&
              ((n.isAnimating = !1),
              n.update(!0, !0, 0),
              n.isComplete || n.complete()),
            n.trigger('onFullscreenChange', t),
            n.$refs.container.toggleClass('fancybox-is-fullscreen', t),
            n.$refs.toolbar
              .find('[data-fancybox-fullscreen]')
              .toggleClass('fancybox-button--fsenter', !t)
              .toggleClass('fancybox-button--fsexit', t));
        });
    }
    e(t).on({
      'onInit.fb': function (t, e) {
        var o;
        if (!n)
          return void e.$refs.toolbar
            .find('[data-fancybox-fullscreen]')
            .remove();
        e && e.group[e.currIndex].opts.fullScreen
          ? ((o = e.$refs.container),
            o.on(
              'click.fb-fullscreen',
              '[data-fancybox-fullscreen]',
              function (t) {
                t.stopPropagation(), t.preventDefault(), i.toggle();
              }
            ),
            e.opts.fullScreen &&
              !0 === e.opts.fullScreen.autoStart &&
              i.request(),
            (e.FullScreen = i))
          : e && e.$refs.toolbar.find('[data-fancybox-fullscreen]').hide();
      },
      'afterKeydown.fb': function (t, e, n, i, o) {
        e &&
          e.FullScreen &&
          70 === o &&
          (i.preventDefault(), e.FullScreen.toggle());
      },
      'beforeClose.fb': function (t, e) {
        e &&
          e.FullScreen &&
          e.$refs.container.hasClass('fancybox-is-fullscreen') &&
          i.exit();
      },
    });
  })(document, jQuery),
  (function (t, e) {
    'use strict';
    var n = 'fancybox-thumbs',
      i = n + '-active';
    e.fancybox.defaults = e.extend(
      !0,
      {
        btnTpl: {
          thumbs:
            '<button data-fancybox-thumbs class="fancybox-button fancybox-button--thumbs" title="{{THUMBS}}"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M14.59 14.59h3.76v3.76h-3.76v-3.76zm-4.47 0h3.76v3.76h-3.76v-3.76zm-4.47 0h3.76v3.76H5.65v-3.76zm8.94-4.47h3.76v3.76h-3.76v-3.76zm-4.47 0h3.76v3.76h-3.76v-3.76zm-4.47 0h3.76v3.76H5.65v-3.76zm8.94-4.47h3.76v3.76h-3.76V5.65zm-4.47 0h3.76v3.76h-3.76V5.65zm-4.47 0h3.76v3.76H5.65V5.65z"/></svg></button>',
        },
        thumbs: {
          autoStart: !1,
          hideOnClose: !0,
          parentEl: '.fancybox-container',
          axis: 'y',
        },
      },
      e.fancybox.defaults
    );
    var o = function (t) {
      this.init(t);
    };
    e.extend(o.prototype, {
      $button: null,
      $grid: null,
      $list: null,
      isVisible: !1,
      isActive: !1,
      init: function (t) {
        var e = this,
          n = t.group,
          i = 0;
        (e.instance = t),
          (e.opts = n[t.currIndex].opts.thumbs),
          (t.Thumbs = e),
          (e.$button = t.$refs.toolbar.find('[data-fancybox-thumbs]'));
        for (
          var o = 0, r = n.length;
          o < r && (n[o].thumb && i++, !(i > 1));
          o++
        );
        i > 1 && e.opts
          ? (e.$button.removeAttr('style').on('click', function () {
              e.toggle();
            }),
            (e.isActive = !0))
          : e.$button.hide();
      },
      create: function () {
        var t,
          i = this,
          o = i.instance,
          r = i.opts.parentEl,
          s = [];
        i.$grid ||
          ((i.$grid = e(
            '<div class="' + n + ' ' + n + '-' + i.opts.axis + '"></div>'
          ).appendTo(o.$refs.container.find(r).addBack().filter(r))),
          i.$grid.on('click', 'a', function () {
            o.jumpTo(e(this).attr('data-index'));
          })),
          i.$list ||
            (i.$list = e('<div class="' + n + '__list">').appendTo(i.$grid)),
          e.each(o.group, function (e, n) {
            (t = n.thumb),
              t || 'image' !== n.type || (t = n.src),
              s.push(
                '<a href="javascript:;" tabindex="0" data-index="' +
                  e +
                  '"' +
                  (t && t.length
                    ? ' style="background-image:url(' + t + ')"'
                    : 'class="fancybox-thumbs-missing"') +
                  '></a>'
              );
          }),
          (i.$list[0].innerHTML = s.join('')),
          'x' === i.opts.axis &&
            i.$list.width(
              parseInt(i.$grid.css('padding-right'), 10) +
                o.group.length * i.$list.children().eq(0).outerWidth(!0)
            );
      },
      focus: function (t) {
        var e,
          n,
          o = this,
          r = o.$list,
          s = o.$grid;
        o.instance.current &&
          ((e = r
            .children()
            .removeClass(i)
            .filter('[data-index="' + o.instance.current.index + '"]')
            .addClass(i)),
          (n = e.position()),
          'y' === o.opts.axis &&
          (n.top < 0 || n.top > r.height() - e.outerHeight())
            ? r.stop().animate({ scrollTop: r.scrollTop() + n.top }, t)
            : 'x' === o.opts.axis &&
              (n.left < s.scrollLeft() ||
                n.left > s.scrollLeft() + (s.width() - e.outerWidth())) &&
              r.parent().stop().animate({ scrollLeft: n.left }, t));
      },
      update: function () {
        var t = this;
        t.instance.$refs.container.toggleClass(
          'fancybox-show-thumbs',
          this.isVisible
        ),
          t.isVisible
            ? (t.$grid || t.create(),
              t.instance.trigger('onThumbsShow'),
              t.focus(0))
            : t.$grid && t.instance.trigger('onThumbsHide'),
          t.instance.update();
      },
      hide: function () {
        (this.isVisible = !1), this.update();
      },
      show: function () {
        (this.isVisible = !0), this.update();
      },
      toggle: function () {
        (this.isVisible = !this.isVisible), this.update();
      },
    }),
      e(t).on({
        'onInit.fb': function (t, e) {
          var n;
          e &&
            !e.Thumbs &&
            ((n = new o(e)), n.isActive && !0 === n.opts.autoStart && n.show());
        },
        'beforeShow.fb': function (t, e, n, i) {
          var o = e && e.Thumbs;
          o && o.isVisible && o.focus(i ? 0 : 250);
        },
        'afterKeydown.fb': function (t, e, n, i, o) {
          var r = e && e.Thumbs;
          r && r.isActive && 71 === o && (i.preventDefault(), r.toggle());
        },
        'beforeClose.fb': function (t, e) {
          var n = e && e.Thumbs;
          n && n.isVisible && !1 !== n.opts.hideOnClose && n.$grid.hide();
        },
      });
  })(document, jQuery),
  (function (t, e) {
    'use strict';
    function n(t) {
      var e = {
        '&': '&amp;',
        '<': '&lt;',
        '>': '&gt;',
        '"': '&quot;',
        "'": '&#39;',
        '/': '&#x2F;',
        '`': '&#x60;',
        '=': '&#x3D;',
      };
      return String(t).replace(/[&<>"'`=\/]/g, function (t) {
        return e[t];
      });
    }
    e.extend(!0, e.fancybox.defaults, {
      btnTpl: {
        share:
          '<button data-fancybox-share class="fancybox-button fancybox-button--share" title="{{SHARE}}"><svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M2.55 19c1.4-8.4 9.1-9.8 11.9-9.8V5l7 7-7 6.3v-3.5c-2.8 0-10.5 2.1-11.9 4.2z"/></svg></button>',
      },
      share: {
        url: function (t, e) {
          return (
            (!t.currentHash &&
              'inline' !== e.type &&
              'html' !== e.type &&
              (e.origSrc || e.src)) ||
            window.location
          );
        },
        tpl: '<div class="fancybox-share"><h1>{{SHARE}}</h1><p><a class="fancybox-share__button fancybox-share__button--fb" href="https://www.facebook.com/sharer/sharer.php?u={{url}}"><svg viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg"><path d="m287 456v-299c0-21 6-35 35-35h38v-63c-7-1-29-3-55-3-54 0-91 33-91 94v306m143-254h-205v72h196" /></svg><span>Facebook</span></a><a class="fancybox-share__button fancybox-share__button--tw" href="https://twitter.com/intent/tweet?url={{url}}&text={{descr}}"><svg viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg"><path d="m456 133c-14 7-31 11-47 13 17-10 30-27 37-46-15 10-34 16-52 20-61-62-157-7-141 75-68-3-129-35-169-85-22 37-11 86 26 109-13 0-26-4-37-9 0 39 28 72 65 80-12 3-25 4-37 2 10 33 41 57 77 57-42 30-77 38-122 34 170 111 378-32 359-208 16-11 30-25 41-42z" /></svg><span>Twitter</span></a><a class="fancybox-share__button fancybox-share__button--pt" href="https://www.pinterest.com/pin/create/button/?url={{url}}&description={{descr}}&media={{media}}"><svg viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg"><path d="m265 56c-109 0-164 78-164 144 0 39 15 74 47 87 5 2 10 0 12-5l4-19c2-6 1-8-3-13-9-11-15-25-15-45 0-58 43-110 113-110 62 0 96 38 96 88 0 67-30 122-73 122-24 0-42-19-36-44 6-29 20-60 20-81 0-19-10-35-31-35-25 0-44 26-44 60 0 21 7 36 7 36l-30 125c-8 37-1 83 0 87 0 3 4 4 5 2 2-3 32-39 42-75l16-64c8 16 31 29 56 29 74 0 124-67 124-157 0-69-58-132-146-132z" fill="#fff"/></svg><span>Pinterest</span></a></p><p><input class="fancybox-share__input" type="text" value="{{url_raw}}" onclick="select()" /></p></div>',
      },
    }),
      e(t).on('click', '[data-fancybox-share]', function () {
        var t,
          i,
          o = e.fancybox.getInstance(),
          r = o.current || null;
        r &&
          ('function' === e.type(r.opts.share.url) &&
            (t = r.opts.share.url.apply(r, [o, r])),
          (i = r.opts.share.tpl
            .replace(
              /\{\{media\}\}/g,
              'image' === r.type ? encodeURIComponent(r.src) : ''
            )
            .replace(/\{\{url\}\}/g, encodeURIComponent(t))
            .replace(/\{\{url_raw\}\}/g, n(t))
            .replace(
              /\{\{descr\}\}/g,
              o.$caption ? encodeURIComponent(o.$caption.text()) : ''
            )),
          e.fancybox.open({
            src: o.translate(o, i),
            type: 'html',
            opts: {
              touch: !1,
              animationEffect: !1,
              afterLoad: function (t, e) {
                o.$refs.container.one('beforeClose.fb', function () {
                  t.close(null, 0);
                }),
                  e.$content.find('.fancybox-share__button').click(function () {
                    return (
                      window.open(this.href, 'Share', 'width=550, height=450'),
                      !1
                    );
                  });
              },
              mobile: { autoFocus: !1 },
            },
          }));
      });
  })(document, jQuery),
  (function (t, e, n) {
    'use strict';
    function i() {
      var e = t.location.hash.substr(1),
        n = e.split('-'),
        i =
          n.length > 1 && /^\+?\d+$/.test(n[n.length - 1])
            ? parseInt(n.pop(-1), 10) || 1
            : 1,
        o = n.join('-');
      return { hash: e, index: i < 1 ? 1 : i, gallery: o };
    }
    function o(t) {
      '' !== t.gallery &&
        n("[data-fancybox='" + n.escapeSelector(t.gallery) + "']")
          .eq(t.index - 1)
          .focus()
          .trigger('click.fb-start');
    }
    function r(t) {
      var e, n;
      return (
        !!t &&
        ((e = t.current ? t.current.opts : t.opts),
        '' !==
          (n =
            e.hash ||
            (e.$orig
              ? e.$orig.data('fancybox') || e.$orig.data('fancybox-trigger')
              : '')) && n)
      );
    }
    n.escapeSelector ||
      (n.escapeSelector = function (t) {
        return (t + '').replace(
          /([\0-\x1f\x7f]|^-?\d)|^-$|[^\x80-\uFFFF\w-]/g,
          function (t, e) {
            return e
              ? '\0' === t
                ? '�'
                : t.slice(0, -1) +
                  '\\' +
                  t.charCodeAt(t.length - 1).toString(16) +
                  ' '
              : '\\' + t;
          }
        );
      }),
      n(function () {
        !1 !== n.fancybox.defaults.hash &&
          (n(e).on({
            'onInit.fb': function (t, e) {
              var n, o;
              !1 !== e.group[e.currIndex].opts.hash &&
                ((n = i()),
                (o = r(e)) &&
                  n.gallery &&
                  o == n.gallery &&
                  (e.currIndex = n.index - 1));
            },
            'beforeShow.fb': function (n, i, o, s) {
              var a;
              o &&
                !1 !== o.opts.hash &&
                (a = r(i)) &&
                ((i.currentHash =
                  a + (i.group.length > 1 ? '-' + (o.index + 1) : '')),
                t.location.hash !== '#' + i.currentHash &&
                  (s && !i.origHash && (i.origHash = t.location.hash),
                  i.hashTimer && clearTimeout(i.hashTimer),
                  (i.hashTimer = setTimeout(function () {
                    'replaceState' in t.history
                      ? (t.history[s ? 'pushState' : 'replaceState'](
                          {},
                          e.title,
                          t.location.pathname +
                            t.location.search +
                            '#' +
                            i.currentHash
                        ),
                        s && (i.hasCreatedHistory = !0))
                      : (t.location.hash = i.currentHash),
                      (i.hashTimer = null);
                  }, 300))));
            },
            'beforeClose.fb': function (n, i, o) {
              o &&
                !1 !== o.opts.hash &&
                (clearTimeout(i.hashTimer),
                i.currentHash && i.hasCreatedHistory
                  ? t.history.back()
                  : i.currentHash &&
                    ('replaceState' in t.history
                      ? t.history.replaceState(
                          {},
                          e.title,
                          t.location.pathname +
                            t.location.search +
                            (i.origHash || '')
                        )
                      : (t.location.hash = i.origHash)),
                (i.currentHash = null));
            },
          }),
          n(t).on('hashchange.fb', function () {
            var t = i(),
              e = null;
            n.each(n('.fancybox-container').get().reverse(), function (t, i) {
              var o = n(i).data('FancyBox');
              if (o && o.currentHash) return (e = o), !1;
            }),
              e
                ? e.currentHash === t.gallery + '-' + t.index ||
                  (1 === t.index && e.currentHash == t.gallery) ||
                  ((e.currentHash = null), e.close())
                : '' !== t.gallery && o(t);
          }),
          setTimeout(function () {
            n.fancybox.getInstance() || o(i());
          }, 50));
      });
  })(window, document, jQuery),
  (function (t, e) {
    'use strict';
    var n = new Date().getTime();
    e(t).on({
      'onInit.fb': function (t, e, i) {
        e.$refs.stage.on(
          'mousewheel DOMMouseScroll wheel MozMousePixelScroll',
          function (t) {
            var i = e.current,
              o = new Date().getTime();
            e.group.length < 2 ||
              !1 === i.opts.wheel ||
              ('auto' === i.opts.wheel && 'image' !== i.type) ||
              (t.preventDefault(),
              t.stopPropagation(),
              i.$slide.hasClass('fancybox-animated') ||
                ((t = t.originalEvent || t),
                o - n < 250 ||
                  ((n = o),
                  e[
                    (-t.deltaY || -t.deltaX || t.wheelDelta || -t.detail) < 0
                      ? 'next'
                      : 'previous'
                  ]())));
          }
        );
      },
    });
  })(document, jQuery),
  (function (t, e, n) {
    'function' == typeof define && define.amd
      ? define(['jquery'], t)
      : 'object' == typeof exports && 'undefined' == typeof Meteor
      ? (module.exports = t(require('jquery')))
      : t(e || n);
  })(
    function (t) {
      'use strict';
      var e = function (e, n, i) {
        var o = {
          invalid: [],
          getCaret: function () {
            try {
              var t,
                n = 0,
                i = e.get(0),
                r = document.selection,
                s = i.selectionStart;
              return (
                r && -1 === navigator.appVersion.indexOf('MSIE 10')
                  ? ((t = r.createRange()),
                    t.moveStart('character', -o.val().length),
                    (n = t.text.length))
                  : (s || '0' === s) && (n = s),
                n
              );
            } catch (t) {}
          },
          setCaret: function (t) {
            try {
              if (e.is(':focus')) {
                var n,
                  i = e.get(0);
                i.setSelectionRange
                  ? i.setSelectionRange(t, t)
                  : ((n = i.createTextRange()),
                    n.collapse(!0),
                    n.moveEnd('character', t),
                    n.moveStart('character', t),
                    n.select());
              }
            } catch (t) {}
          },
          events: function () {
            e.on('keydown.mask', function (t) {
              e.data('mask-keycode', t.keyCode || t.which),
                e.data('mask-previus-value', e.val()),
                e.data('mask-previus-caret-pos', o.getCaret()),
                (o.maskDigitPosMapOld = o.maskDigitPosMap);
            })
              .on(
                t.jMaskGlobals.useInput ? 'input.mask' : 'keyup.mask',
                o.behaviour
              )
              .on('paste.mask drop.mask', function () {
                setTimeout(function () {
                  e.keydown().keyup();
                }, 100);
              })
              .on('change.mask', function () {
                e.data('changed', !0);
              })
              .on('blur.mask', function () {
                a === o.val() || e.data('changed') || e.trigger('change'),
                  e.data('changed', !1);
              })
              .on('blur.mask', function () {
                a = o.val();
              })
              .on('focus.mask', function (e) {
                !0 === i.selectOnFocus && t(e.target).select();
              })
              .on('focusout.mask', function () {
                i.clearIfNotMatch && !r.test(o.val()) && o.val('');
              });
          },
          getRegexMask: function () {
            for (var t, e, i, o, r, a, l = [], c = 0; c < n.length; c++)
              (t = s.translation[n.charAt(c)]),
                t
                  ? ((e = t.pattern.toString().replace(/.{1}$|^.{1}/g, '')),
                    (i = t.optional),
                    (o = t.recursive),
                    o
                      ? (l.push(n.charAt(c)),
                        (r = { digit: n.charAt(c), pattern: e }))
                      : l.push(i || o ? e + '?' : e))
                  : l.push(
                      n.charAt(c).replace(/[-\/\\^$*+?.()|[\]{}]/g, '\\$&')
                    );
            return (
              (a = l.join('')),
              r &&
                (a = a
                  .replace(
                    new RegExp('(' + r.digit + '(.*' + r.digit + ')?)'),
                    '($1)?'
                  )
                  .replace(new RegExp(r.digit, 'g'), r.pattern)),
              new RegExp(a)
            );
          },
          destroyEvents: function () {
            e.off(
              [
                'input',
                'keydown',
                'keyup',
                'paste',
                'drop',
                'blur',
                'focusout',
                '',
              ].join('.mask ')
            );
          },
          val: function (t) {
            var n,
              i = e.is('input'),
              o = i ? 'val' : 'text';
            return (
              arguments.length > 0
                ? (e[o]() !== t && e[o](t), (n = e))
                : (n = e[o]()),
              n
            );
          },
          calculateCaretPosition: function (t) {
            var n = o.getMasked(),
              i = o.getCaret();
            if (t !== n) {
              var r = e.data('mask-previus-caret-pos') || 0,
                s = n.length,
                a = t.length,
                l = 0,
                c = 0,
                u = 0,
                d = 0,
                p = 0;
              for (p = i; p < s && o.maskDigitPosMap[p]; p++) c++;
              for (p = i - 1; p >= 0 && o.maskDigitPosMap[p]; p--) l++;
              for (p = i - 1; p >= 0; p--) o.maskDigitPosMap[p] && u++;
              for (p = r - 1; p >= 0; p--) o.maskDigitPosMapOld[p] && d++;
              if (i > a) i = 10 * s;
              else if (r >= i && r !== a) {
                if (!o.maskDigitPosMapOld[i]) {
                  var h = i;
                  (i -= d - u), (i -= l), o.maskDigitPosMap[i] && (i = h);
                }
              } else i > r && ((i += u - d), (i += c));
            }
            return i;
          },
          behaviour: function (n) {
            (n = n || window.event), (o.invalid = []);
            var i = e.data('mask-keycode');
            if (-1 === t.inArray(i, s.byPassKeys)) {
              var r = o.getMasked(),
                a = o.getCaret(),
                l = e.data('mask-previus-value') || '';
              return (
                setTimeout(function () {
                  o.setCaret(o.calculateCaretPosition(l));
                }, t.jMaskGlobals.keyStrokeCompensation),
                o.val(r),
                o.setCaret(a),
                o.callbacks(n)
              );
            }
          },
          getMasked: function (t, e) {
            var r,
              a,
              l = [],
              c = void 0 === e ? o.val() : e + '',
              u = 0,
              d = n.length,
              p = 0,
              h = c.length,
              f = 1,
              g = 'push',
              m = -1,
              v = 0,
              y = [];
            i.reverse
              ? ((g = 'unshift'),
                (f = -1),
                (r = 0),
                (u = d - 1),
                (p = h - 1),
                (a = function () {
                  return u > -1 && p > -1;
                }))
              : ((r = d - 1),
                (a = function () {
                  return u < d && p < h;
                }));
            for (var b; a(); ) {
              var x = n.charAt(u),
                w = c.charAt(p),
                C = s.translation[x];
              C
                ? (w.match(C.pattern)
                    ? (l[g](w),
                      C.recursive &&
                        (-1 === m ? (m = u) : u === r && u !== m && (u = m - f),
                        r === m && (u -= f)),
                      (u += f))
                    : w === b
                    ? (v--, (b = void 0))
                    : C.optional
                    ? ((u += f), (p -= f))
                    : C.fallback
                    ? (l[g](C.fallback), (u += f), (p -= f))
                    : o.invalid.push({ p: p, v: w, e: C.pattern }),
                  (p += f))
                : (t || l[g](x),
                  w === x
                    ? (y.push(p), (p += f))
                    : ((b = x), y.push(p + v), v++),
                  (u += f));
            }
            var _ = n.charAt(r);
            d !== h + 1 || s.translation[_] || l.push(_);
            var T = l.join('');
            return o.mapMaskdigitPositions(T, y, h), T;
          },
          mapMaskdigitPositions: function (t, e, n) {
            var r = i.reverse ? t.length - n : 0;
            o.maskDigitPosMap = {};
            for (var s = 0; s < e.length; s++) o.maskDigitPosMap[e[s] + r] = 1;
          },
          callbacks: function (t) {
            var r = o.val(),
              s = r !== a,
              l = [r, t, e, i],
              c = function (t, e, n) {
                'function' == typeof i[t] && e && i[t].apply(this, n);
              };
            c('onChange', !0 === s, l),
              c('onKeyPress', !0 === s, l),
              c('onComplete', r.length === n.length, l),
              c('onInvalid', o.invalid.length > 0, [r, t, e, o.invalid, i]);
          },
        };
        e = t(e);
        var r,
          s = this,
          a = o.val();
        (n = 'function' == typeof n ? n(o.val(), void 0, e, i) : n),
          (s.mask = n),
          (s.options = i),
          (s.remove = function () {
            var t = o.getCaret();
            return (
              s.options.placeholder && e.removeAttr('placeholder'),
              e.data('mask-maxlength') && e.removeAttr('maxlength'),
              o.destroyEvents(),
              o.val(s.getCleanVal()),
              o.setCaret(t),
              e
            );
          }),
          (s.getCleanVal = function () {
            return o.getMasked(!0);
          }),
          (s.getMaskedVal = function (t) {
            return o.getMasked(!1, t);
          }),
          (s.init = function (a) {
            if (
              ((a = a || !1),
              (i = i || {}),
              (s.clearIfNotMatch = t.jMaskGlobals.clearIfNotMatch),
              (s.byPassKeys = t.jMaskGlobals.byPassKeys),
              (s.translation = t.extend(
                {},
                t.jMaskGlobals.translation,
                i.translation
              )),
              (s = t.extend(!0, {}, s, i)),
              (r = o.getRegexMask()),
              a)
            )
              o.events(), o.val(o.getMasked());
            else {
              i.placeholder && e.attr('placeholder', i.placeholder),
                e.data('mask') && e.attr('autocomplete', 'off');
              for (var l = 0, c = !0; l < n.length; l++) {
                var u = s.translation[n.charAt(l)];
                if (u && u.recursive) {
                  c = !1;
                  break;
                }
              }
              c && e.attr('maxlength', n.length).data('mask-maxlength', !0),
                o.destroyEvents(),
                o.events();
              var d = o.getCaret();
              o.val(o.getMasked()), o.setCaret(d);
            }
          }),
          s.init(!e.is('input'));
      };
      t.maskWatchers = {};
      var n = function () {
          var n = t(this),
            o = {},
            r = 'data-mask-',
            s = n.attr('data-mask');
          if (
            (n.attr(r + 'reverse') && (o.reverse = !0),
            n.attr(r + 'clearifnotmatch') && (o.clearIfNotMatch = !0),
            'true' === n.attr(r + 'selectonfocus') && (o.selectOnFocus = !0),
            i(n, s, o))
          )
            return n.data('mask', new e(this, s, o));
        },
        i = function (e, n, i) {
          i = i || {};
          var o = t(e).data('mask'),
            r = JSON.stringify,
            s = t(e).val() || t(e).text();
          try {
            return (
              'function' == typeof n && (n = n(s)),
              'object' != typeof o || r(o.options) !== r(i) || o.mask !== n
            );
          } catch (t) {}
        },
        o = function (t) {
          var e,
            n = document.createElement('div');
          return (
            (t = 'on' + t),
            (e = t in n),
            e ||
              (n.setAttribute(t, 'return;'), (e = 'function' == typeof n[t])),
            (n = null),
            e
          );
        };
      (t.fn.mask = function (n, o) {
        o = o || {};
        var r = this.selector,
          s = t.jMaskGlobals,
          a = s.watchInterval,
          l = o.watchInputs || s.watchInputs,
          c = function () {
            if (i(this, n, o)) return t(this).data('mask', new e(this, n, o));
          };
        return (
          t(this).each(c),
          r &&
            '' !== r &&
            l &&
            (clearInterval(t.maskWatchers[r]),
            (t.maskWatchers[r] = setInterval(function () {
              t(document).find(r).each(c);
            }, a))),
          this
        );
      }),
        (t.fn.masked = function (t) {
          return this.data('mask').getMaskedVal(t);
        }),
        (t.fn.unmask = function () {
          return (
            clearInterval(t.maskWatchers[this.selector]),
            delete t.maskWatchers[this.selector],
            this.each(function () {
              var e = t(this).data('mask');
              e && e.remove().removeData('mask');
            })
          );
        }),
        (t.fn.cleanVal = function () {
          return this.data('mask').getCleanVal();
        }),
        (t.applyDataMask = function (e) {
          (e = e || t.jMaskGlobals.maskElements),
            (e instanceof t ? e : t(e))
              .filter(t.jMaskGlobals.dataMaskAttr)
              .each(n);
        });
      var r = {
        maskElements: 'input,td,span,div',
        dataMaskAttr: '*[data-mask]',
        dataMask: !0,
        watchInterval: 300,
        watchInputs: !0,
        keyStrokeCompensation: 10,
        useInput:
          !/Chrome\/[2-4][0-9]|SamsungBrowser/.test(
            window.navigator.userAgent
          ) && o('input'),
        watchDataMask: !1,
        byPassKeys: [9, 16, 17, 18, 36, 37, 38, 39, 40, 91],
        translation: {
          0: { pattern: /\d/ },
          9: { pattern: /\d/, optional: !0 },
          '#': { pattern: /\d/, recursive: !0 },
          A: { pattern: /[a-zA-Z0-9]/ },
          S: { pattern: /[a-zA-Z]/ },
        },
      };
      (t.jMaskGlobals = t.jMaskGlobals || {}),
        (r = t.jMaskGlobals = t.extend(!0, {}, r, t.jMaskGlobals)),
        r.dataMask && t.applyDataMask(),
        setInterval(function () {
          t.jMaskGlobals.watchDataMask && t.applyDataMask();
        }, r.watchInterval);
    },
    window.jQuery,
    window.Zepto
  ),
  function () {
    var t, e, n, i, o, r, s, a, l, c;
    (e = window.device),
      (t = {}),
      (window.device = t),
      (i = window.document.documentElement),
      (c = window.navigator.userAgent.toLowerCase()),
      (t.ios = function () {
        return t.iphone() || t.ipod() || t.ipad();
      }),
      (t.iphone = function () {
        return !t.windows() && o('iphone');
      }),
      (t.ipod = function () {
        return o('ipod');
      }),
      (t.ipad = function () {
        return o('ipad');
      }),
      (t.android = function () {
        return !t.windows() && o('android');
      }),
      (t.androidPhone = function () {
        return t.android() && o('mobile');
      }),
      (t.androidTablet = function () {
        return t.android() && !o('mobile');
      }),
      (t.blackberry = function () {
        return o('blackberry') || o('bb10') || o('rim');
      }),
      (t.blackberryPhone = function () {
        return t.blackberry() && !o('tablet');
      }),
      (t.blackberryTablet = function () {
        return t.blackberry() && o('tablet');
      }),
      (t.windows = function () {
        return o('windows');
      }),
      (t.windowsPhone = function () {
        return t.windows() && o('phone');
      }),
      (t.windowsTablet = function () {
        return t.windows() && o('touch') && !t.windowsPhone();
      }),
      (t.fxos = function () {
        return (o('(mobile;') || o('(tablet;')) && o('; rv:');
      }),
      (t.fxosPhone = function () {
        return t.fxos() && o('mobile');
      }),
      (t.fxosTablet = function () {
        return t.fxos() && o('tablet');
      }),
      (t.meego = function () {
        return o('meego');
      }),
      (t.cordova = function () {
        return window.cordova && 'file:' === location.protocol;
      }),
      (t.nodeWebkit = function () {
        return 'object' == typeof window.process;
      }),
      (t.mobile = function () {
        return (
          t.androidPhone() ||
          t.iphone() ||
          t.ipod() ||
          t.windowsPhone() ||
          t.blackberryPhone() ||
          t.fxosPhone() ||
          t.meego()
        );
      }),
      (t.tablet = function () {
        return (
          t.ipad() ||
          t.androidTablet() ||
          t.blackberryTablet() ||
          t.windowsTablet() ||
          t.fxosTablet()
        );
      }),
      (t.desktop = function () {
        return !t.tablet() && !t.mobile();
      }),
      (t.television = function () {
        var t;
        for (
          television = [
            'googletv',
            'viera',
            'smarttv',
            'internet.tv',
            'netcast',
            'nettv',
            'appletv',
            'boxee',
            'kylo',
            'roku',
            'dlnadoc',
            'roku',
            'pov_tv',
            'hbbtv',
            'ce-html',
          ],
            t = 0;
          t < television.length;

        ) {
          if (o(television[t])) return !0;
          t++;
        }
        return !1;
      }),
      (t.portrait = function () {
        return window.innerHeight / window.innerWidth > 1;
      }),
      (t.landscape = function () {
        return window.innerHeight / window.innerWidth < 1;
      }),
      (t.noConflict = function () {
        return (window.device = e), this;
      }),
      (o = function (t) {
        return -1 !== c.indexOf(t);
      }),
      (s = function (t) {
        var e;
        return (e = new RegExp(t, 'i')), i.className.match(e);
      }),
      (n = function (t) {
        var e = null;
        s(t) ||
          ((e = i.className.replace(/^\s+|\s+$/g, '')),
          (i.className = e + ' ' + t));
      }),
      (l = function (t) {
        s(t) && (i.className = i.className.replace(' ' + t, ''));
      }),
      t.ios()
        ? t.ipad()
          ? n('ios ipad tablet')
          : t.iphone()
          ? n('ios iphone mobile')
          : t.ipod() && n('ios ipod mobile')
        : t.android()
        ? n(t.androidTablet() ? 'android tablet' : 'android mobile')
        : t.blackberry()
        ? n(t.blackberryTablet() ? 'blackberry tablet' : 'blackberry mobile')
        : t.windows()
        ? n(
            t.windowsTablet()
              ? 'windows tablet'
              : t.windowsPhone()
              ? 'windows mobile'
              : 'desktop'
          )
        : t.fxos()
        ? n(t.fxosTablet() ? 'fxos tablet' : 'fxos mobile')
        : t.meego()
        ? n('meego mobile')
        : t.nodeWebkit()
        ? n('node-webkit')
        : t.television()
        ? n('television')
        : t.desktop() && n('desktop'),
      t.cordova() && n('cordova'),
      (r = function () {
        t.landscape()
          ? (l('portrait'), n('landscape'))
          : (l('landscape'), n('portrait'));
      }),
      (a = Object.prototype.hasOwnProperty.call(window, 'onorientationchange')
        ? 'orientationchange'
        : 'resize'),
      window.addEventListener
        ? window.addEventListener(a, r, !1)
        : window.attachEvent
        ? window.attachEvent(a, r)
        : (window[a] = r),
      r(),
      'function' == typeof define && 'object' == typeof define.amd && define.amd
        ? define(function () {
            return t;
          })
        : 'undefined' != typeof module && module.exports
        ? (module.exports = t)
        : (window.device = t);
  }.call(this),
  (function (t) {
    'function' == typeof define && define.amd
      ? define(['jquery'], t)
      : t(
          'object' == typeof exports
            ? require('jquery')
            : window.jQuery || window.Zepto
        );
  })(function (t) {
    var e,
      n,
      i,
      o,
      r,
      s,
      a = 'Close',
      l = 'BeforeClose',
      c = 'MarkupParse',
      u = 'Open',
      d = 'Change',
      p = 'mfp',
      h = '.' + p,
      f = 'mfp-ready',
      g = 'mfp-removing',
      m = 'mfp-prevent-close',
      v = function () {},
      y = !!window.jQuery,
      b = t(window),
      x = function (t, n) {
        e.ev.on(p + t + h, n);
      },
      w = function (e, n, i, o) {
        var r = document.createElement('div');
        return (
          (r.className = 'mfp-' + e),
          i && (r.innerHTML = i),
          o ? n && n.appendChild(r) : ((r = t(r)), n && r.appendTo(n)),
          r
        );
      },
      C = function (n, i) {
        e.ev.triggerHandler(p + n, i),
          e.st.callbacks &&
            ((n = n.charAt(0).toLowerCase() + n.slice(1)),
            e.st.callbacks[n] &&
              e.st.callbacks[n].apply(e, t.isArray(i) ? i : [i]));
      },
      _ = function (n) {
        return (
          (n === s && e.currTemplate.closeBtn) ||
            ((e.currTemplate.closeBtn = t(
              e.st.closeMarkup.replace('%title%', e.st.tClose)
            )),
            (s = n)),
          e.currTemplate.closeBtn
        );
      },
      T = function () {
        t.magnificPopup.instance ||
          ((e = new v()), e.init(), (t.magnificPopup.instance = e));
      },
      k = function () {
        var t = document.createElement('p').style,
          e = ['ms', 'O', 'Moz', 'Webkit'];
        if (void 0 !== t.transition) return !0;
        for (; e.length; ) if (e.pop() + 'Transition' in t) return !0;
        return !1;
      };
    (v.prototype = {
      constructor: v,
      init: function () {
        var n = navigator.appVersion;
        (e.isLowIE = e.isIE8 = document.all && !document.addEventListener),
          (e.isAndroid = /android/gi.test(n)),
          (e.isIOS = /iphone|ipad|ipod/gi.test(n)),
          (e.supportsTransition = k()),
          (e.probablyMobile =
            e.isAndroid ||
            e.isIOS ||
            /(Opera Mini)|Kindle|webOS|BlackBerry|(Opera Mobi)|(Windows Phone)|IEMobile/i.test(
              navigator.userAgent
            )),
          (i = t(document)),
          (e.popupsCache = {});
      },
      open: function (n) {
        var o;
        if (!1 === n.isObj) {
          (e.items = n.items.toArray()), (e.index = 0);
          var s,
            a = n.items;
          for (o = 0; o < a.length; o++)
            if (((s = a[o]), s.parsed && (s = s.el[0]), s === n.el[0])) {
              e.index = o;
              break;
            }
        } else
          (e.items = t.isArray(n.items) ? n.items : [n.items]),
            (e.index = n.index || 0);
        if (e.isOpen) return void e.updateItemHTML();
        (e.types = []),
          (r = ''),
          n.mainEl && n.mainEl.length ? (e.ev = n.mainEl.eq(0)) : (e.ev = i),
          n.key
            ? (e.popupsCache[n.key] || (e.popupsCache[n.key] = {}),
              (e.currTemplate = e.popupsCache[n.key]))
            : (e.currTemplate = {}),
          (e.st = t.extend(!0, {}, t.magnificPopup.defaults, n)),
          (e.fixedContentPos =
            'auto' === e.st.fixedContentPos
              ? !e.probablyMobile
              : e.st.fixedContentPos),
          e.st.modal &&
            ((e.st.closeOnContentClick = !1),
            (e.st.closeOnBgClick = !1),
            (e.st.showCloseBtn = !1),
            (e.st.enableEscapeKey = !1)),
          e.bgOverlay ||
            ((e.bgOverlay = w('bg').on('click' + h, function () {
              e.close();
            })),
            (e.wrap = w('wrap')
              .attr('tabindex', -1)
              .on('click' + h, function (t) {
                e._checkIfClose(t.target) && e.close();
              })),
            (e.container = w('container', e.wrap))),
          (e.contentContainer = w('content')),
          e.st.preloader &&
            (e.preloader = w('preloader', e.container, e.st.tLoading));
        var l = t.magnificPopup.modules;
        for (o = 0; o < l.length; o++) {
          var d = l[o];
          (d = d.charAt(0).toUpperCase() + d.slice(1)), e['init' + d].call(e);
        }
        C('BeforeOpen'),
          e.st.showCloseBtn &&
            (e.st.closeBtnInside
              ? (x(c, function (t, e, n, i) {
                  n.close_replaceWith = _(i.type);
                }),
                (r += ' mfp-close-btn-in'))
              : e.wrap.append(_())),
          e.st.alignTop && (r += ' mfp-align-top'),
          e.fixedContentPos
            ? e.wrap.css({
                overflow: e.st.overflowY,
                overflowX: 'hidden',
                overflowY: e.st.overflowY,
              })
            : e.wrap.css({ top: b.scrollTop(), position: 'absolute' }),
          (!1 === e.st.fixedBgPos ||
            ('auto' === e.st.fixedBgPos && !e.fixedContentPos)) &&
            e.bgOverlay.css({ height: i.height(), position: 'absolute' }),
          e.st.enableEscapeKey &&
            i.on('keyup' + h, function (t) {
              27 === t.keyCode && e.close();
            }),
          b.on('resize' + h, function () {
            e.updateSize();
          }),
          e.st.closeOnContentClick || (r += ' mfp-auto-cursor'),
          r && e.wrap.addClass(r);
        var p = (e.wH = b.height()),
          g = {};
        if (e.fixedContentPos && e._hasScrollBar(p)) {
          var m = e._getScrollbarSize();
          m && (g.marginRight = m);
        }
        e.fixedContentPos &&
          (e.isIE7
            ? t('body, html').css('overflow', 'hidden')
            : (g.overflow = 'hidden'));
        var v = e.st.mainClass;
        return (
          e.isIE7 && (v += ' mfp-ie7'),
          v && e._addClassToMFP(v),
          e.updateItemHTML(),
          C('BuildControls'),
          t('html').css(g),
          e.bgOverlay.add(e.wrap).prependTo(e.st.prependTo || t(document.body)),
          (e._lastFocusedEl = document.activeElement),
          setTimeout(function () {
            e.content
              ? (e._addClassToMFP(f), e._setFocus())
              : e.bgOverlay.addClass(f),
              i.on('focusin' + h, e._onFocusIn);
          }, 16),
          (e.isOpen = !0),
          e.updateSize(p),
          C(u),
          n
        );
      },
      close: function () {
        e.isOpen &&
          (C(l),
          (e.isOpen = !1),
          e.st.removalDelay && !e.isLowIE && e.supportsTransition
            ? (e._addClassToMFP(g),
              setTimeout(function () {
                e._close();
              }, e.st.removalDelay))
            : e._close());
      },
      _close: function () {
        C(a);
        var n = g + ' ' + f + ' ';
        if (
          (e.bgOverlay.detach(),
          e.wrap.detach(),
          e.container.empty(),
          e.st.mainClass && (n += e.st.mainClass + ' '),
          e._removeClassFromMFP(n),
          e.fixedContentPos)
        ) {
          var o = { marginRight: '' };
          e.isIE7 ? t('body, html').css('overflow', '') : (o.overflow = ''),
            t('html').css(o);
        }
        i.off('keyup' + h + ' focusin' + h),
          e.ev.off(h),
          e.wrap.attr('class', 'mfp-wrap').removeAttr('style'),
          e.bgOverlay.attr('class', 'mfp-bg'),
          e.container.attr('class', 'mfp-container'),
          !e.st.showCloseBtn ||
            (e.st.closeBtnInside && !0 !== e.currTemplate[e.currItem.type]) ||
            (e.currTemplate.closeBtn && e.currTemplate.closeBtn.detach()),
          e.st.autoFocusLast && e._lastFocusedEl && t(e._lastFocusedEl).focus(),
          (e.currItem = null),
          (e.content = null),
          (e.currTemplate = null),
          (e.prevHeight = 0),
          C('AfterClose');
      },
      updateSize: function (t) {
        if (e.isIOS) {
          var n = document.documentElement.clientWidth / window.innerWidth,
            i = window.innerHeight * n;
          e.wrap.css('height', i), (e.wH = i);
        } else e.wH = t || b.height();
        e.fixedContentPos || e.wrap.css('height', e.wH), C('Resize');
      },
      updateItemHTML: function () {
        var n = e.items[e.index];
        e.contentContainer.detach(),
          e.content && e.content.detach(),
          n.parsed || (n = e.parseEl(e.index));
        var i = n.type;
        if (
          (C('BeforeChange', [e.currItem ? e.currItem.type : '', i]),
          (e.currItem = n),
          !e.currTemplate[i])
        ) {
          var r = !!e.st[i] && e.st[i].markup;
          C('FirstMarkupParse', r), (e.currTemplate[i] = !r || t(r));
        }
        o && o !== n.type && e.container.removeClass('mfp-' + o + '-holder');
        var s = e['get' + i.charAt(0).toUpperCase() + i.slice(1)](
          n,
          e.currTemplate[i]
        );
        e.appendContent(s, i),
          (n.preloaded = !0),
          C(d, n),
          (o = n.type),
          e.container.prepend(e.contentContainer),
          C('AfterChange');
      },
      appendContent: function (t, n) {
        (e.content = t),
          t
            ? e.st.showCloseBtn &&
              e.st.closeBtnInside &&
              !0 === e.currTemplate[n]
              ? e.content.find('.mfp-close').length || e.content.append(_())
              : (e.content = t)
            : (e.content = ''),
          C('BeforeAppend'),
          e.container.addClass('mfp-' + n + '-holder'),
          e.contentContainer.append(e.content);
      },
      parseEl: function (n) {
        var i,
          o = e.items[n];
        if (
          (o.tagName
            ? (o = { el: t(o) })
            : ((i = o.type), (o = { data: o, src: o.src })),
          o.el)
        ) {
          for (var r = e.types, s = 0; s < r.length; s++)
            if (o.el.hasClass('mfp-' + r[s])) {
              i = r[s];
              break;
            }
          (o.src = o.el.attr('data-mfp-src')),
            o.src || (o.src = o.el.attr('href'));
        }
        return (
          (o.type = i || e.st.type || 'inline'),
          (o.index = n),
          (o.parsed = !0),
          (e.items[n] = o),
          C('ElementParse', o),
          e.items[n]
        );
      },
      addGroup: function (t, n) {
        var i = function (i) {
          (i.mfpEl = this), e._openClick(i, t, n);
        };
        n || (n = {});
        var o = 'click.magnificPopup';
        (n.mainEl = t),
          n.items
            ? ((n.isObj = !0), t.off(o).on(o, i))
            : ((n.isObj = !1),
              n.delegate
                ? t.off(o).on(o, n.delegate, i)
                : ((n.items = t), t.off(o).on(o, i)));
      },
      _openClick: function (n, i, o) {
        if (
          (void 0 !== o.midClick
            ? o.midClick
            : t.magnificPopup.defaults.midClick) ||
          !(2 === n.which || n.ctrlKey || n.metaKey || n.altKey || n.shiftKey)
        ) {
          var r =
            void 0 !== o.disableOn
              ? o.disableOn
              : t.magnificPopup.defaults.disableOn;
          if (r)
            if (t.isFunction(r)) {
              if (!r.call(e)) return !0;
            } else if (b.width() < r) return !0;
          n.type && (n.preventDefault(), e.isOpen && n.stopPropagation()),
            (o.el = t(n.mfpEl)),
            o.delegate && (o.items = i.find(o.delegate)),
            e.open(o);
        }
      },
      updateStatus: function (t, i) {
        if (e.preloader) {
          n !== t && e.container.removeClass('mfp-s-' + n),
            i || 'loading' !== t || (i = e.st.tLoading);
          var o = { status: t, text: i };
          C('UpdateStatus', o),
            (t = o.status),
            (i = o.text),
            e.preloader.html(i),
            e.preloader.find('a').on('click', function (t) {
              t.stopImmediatePropagation();
            }),
            e.container.addClass('mfp-s-' + t),
            (n = t);
        }
      },
      _checkIfClose: function (n) {
        if (!t(n).hasClass(m)) {
          var i = e.st.closeOnContentClick,
            o = e.st.closeOnBgClick;
          if (i && o) return !0;
          if (
            !e.content ||
            t(n).hasClass('mfp-close') ||
            (e.preloader && n === e.preloader[0])
          )
            return !0;
          if (n === e.content[0] || t.contains(e.content[0], n)) {
            if (i) return !0;
          } else if (o && t.contains(document, n)) return !0;
          return !1;
        }
      },
      _addClassToMFP: function (t) {
        e.bgOverlay.addClass(t), e.wrap.addClass(t);
      },
      _removeClassFromMFP: function (t) {
        this.bgOverlay.removeClass(t), e.wrap.removeClass(t);
      },
      _hasScrollBar: function (t) {
        return (
          (e.isIE7 ? i.height() : document.body.scrollHeight) >
          (t || b.height())
        );
      },
      _setFocus: function () {
        (e.st.focus ? e.content.find(e.st.focus).eq(0) : e.wrap).focus();
      },
      _onFocusIn: function (n) {
        return n.target === e.wrap[0] || t.contains(e.wrap[0], n.target)
          ? void 0
          : (e._setFocus(), !1);
      },
      _parseMarkup: function (e, n, i) {
        var o;
        i.data && (n = t.extend(i.data, n)),
          C(c, [e, n, i]),
          t.each(n, function (n, i) {
            if (void 0 === i || !1 === i) return !0;
            if (((o = n.split('_')), o.length > 1)) {
              var r = e.find(h + '-' + o[0]);
              if (r.length > 0) {
                var s = o[1];
                'replaceWith' === s
                  ? r[0] !== i[0] && r.replaceWith(i)
                  : 'img' === s
                  ? r.is('img')
                    ? r.attr('src', i)
                    : r.replaceWith(
                        t('<img>').attr('src', i).attr('class', r.attr('class'))
                      )
                  : r.attr(o[1], i);
              }
            } else e.find(h + '-' + n).html(i);
          });
      },
      _getScrollbarSize: function () {
        if (void 0 === e.scrollbarSize) {
          var t = document.createElement('div');
          (t.style.cssText =
            'width: 99px; height: 99px; overflow: scroll; position: absolute; top: -9999px;'),
            document.body.appendChild(t),
            (e.scrollbarSize = t.offsetWidth - t.clientWidth),
            document.body.removeChild(t);
        }
        return e.scrollbarSize;
      },
    }),
      (t.magnificPopup = {
        instance: null,
        proto: v.prototype,
        modules: [],
        open: function (e, n) {
          return (
            T(),
            (e = e ? t.extend(!0, {}, e) : {}),
            (e.isObj = !0),
            (e.index = n || 0),
            this.instance.open(e)
          );
        },
        close: function () {
          return t.magnificPopup.instance && t.magnificPopup.instance.close();
        },
        registerModule: function (e, n) {
          n.options && (t.magnificPopup.defaults[e] = n.options),
            t.extend(this.proto, n.proto),
            this.modules.push(e);
        },
        defaults: {
          disableOn: 0,
          key: null,
          midClick: !1,
          mainClass: '',
          preloader: !0,
          focus: '',
          closeOnContentClick: !1,
          closeOnBgClick: !0,
          closeBtnInside: !0,
          showCloseBtn: !0,
          enableEscapeKey: !0,
          modal: !1,
          alignTop: !1,
          removalDelay: 0,
          prependTo: null,
          fixedContentPos: 'auto',
          fixedBgPos: 'auto',
          overflowY: 'auto',
          closeMarkup:
            '<button title="%title%" type="button" class="mfp-close">&#215;</button>',
          tClose: 'Close (Esc)',
          tLoading: 'Loading...',
          autoFocusLast: !0,
        },
      }),
      (t.fn.magnificPopup = function (n) {
        T();
        var i = t(this);
        if ('string' == typeof n)
          if ('open' === n) {
            var o,
              r = y ? i.data('magnificPopup') : i[0].magnificPopup,
              s = parseInt(arguments[1], 10) || 0;
            r.items
              ? (o = r.items[s])
              : ((o = i),
                r.delegate && (o = o.find(r.delegate)),
                (o = o.eq(s))),
              e._openClick({ mfpEl: o }, i, r);
          } else
            e.isOpen && e[n].apply(e, Array.prototype.slice.call(arguments, 1));
        else
          (n = t.extend(!0, {}, n)),
            y ? i.data('magnificPopup', n) : (i[0].magnificPopup = n),
            e.addGroup(i, n);
        return i;
      });
    var S,
      $,
      E,
      P = 'inline',
      A = function () {
        E && ($.after(E.addClass(S)).detach(), (E = null));
      };
    t.magnificPopup.registerModule(P, {
      options: {
        hiddenClass: 'hide',
        markup: '',
        tNotFound: 'Content not found',
      },
      proto: {
        initInline: function () {
          e.types.push(P),
            x(a + '.' + P, function () {
              A();
            });
        },
        getInline: function (n, i) {
          if ((A(), n.src)) {
            var o = e.st.inline,
              r = t(n.src);
            if (r.length) {
              var s = r[0].parentNode;
              s &&
                s.tagName &&
                ($ || ((S = o.hiddenClass), ($ = w(S)), (S = 'mfp-' + S)),
                (E = r.after($).detach().removeClass(S))),
                e.updateStatus('ready');
            } else e.updateStatus('error', o.tNotFound), (r = t('<div>'));
            return (n.inlineElement = r), r;
          }
          return e.updateStatus('ready'), e._parseMarkup(i, {}, n), i;
        },
      },
    });
    var I,
      M = 'ajax',
      L = function () {
        I && t(document.body).removeClass(I);
      },
      D = function () {
        L(), e.req && e.req.abort();
      };
    t.magnificPopup.registerModule(M, {
      options: {
        settings: null,
        cursor: 'mfp-ajax-cur',
        tError: '<a href="%url%">The content</a> could not be loaded.',
      },
      proto: {
        initAjax: function () {
          e.types.push(M),
            (I = e.st.ajax.cursor),
            x(a + '.' + M, D),
            x('BeforeChange.' + M, D);
        },
        getAjax: function (n) {
          I && t(document.body).addClass(I), e.updateStatus('loading');
          var i = t.extend(
            {
              url: n.src,
              success: function (i, o, r) {
                var s = { data: i, xhr: r };
                C('ParseAjax', s),
                  e.appendContent(t(s.data), M),
                  (n.finished = !0),
                  L(),
                  e._setFocus(),
                  setTimeout(function () {
                    e.wrap.addClass(f);
                  }, 16),
                  e.updateStatus('ready'),
                  C('AjaxContentAdded');
              },
              error: function () {
                L(),
                  (n.finished = n.loadError = !0),
                  e.updateStatus(
                    'error',
                    e.st.ajax.tError.replace('%url%', n.src)
                  );
              },
            },
            e.st.ajax.settings
          );
          return (e.req = t.ajax(i)), '';
        },
      },
    });
    var j,
      z = function (n) {
        if (n.data && void 0 !== n.data.title) return n.data.title;
        var i = e.st.image.titleSrc;
        if (i) {
          if (t.isFunction(i)) return i.call(e, n);
          if (n.el) return n.el.attr(i) || '';
        }
        return '';
      };
    t.magnificPopup.registerModule('image', {
      options: {
        markup:
          '<div class="mfp-figure"><div class="mfp-close"></div><figure><div class="mfp-img"></div><figcaption><div class="mfp-bottom-bar"><div class="mfp-title"></div><div class="mfp-counter"></div></div></figcaption></figure></div>',
        cursor: 'mfp-zoom-out-cur',
        titleSrc: 'title',
        verticalFit: !0,
        tError: '<a href="%url%">The image</a> could not be loaded.',
      },
      proto: {
        initImage: function () {
          var n = e.st.image,
            i = '.image';
          e.types.push('image'),
            x(u + i, function () {
              'image' === e.currItem.type &&
                n.cursor &&
                t(document.body).addClass(n.cursor);
            }),
            x(a + i, function () {
              n.cursor && t(document.body).removeClass(n.cursor),
                b.off('resize' + h);
            }),
            x('Resize' + i, e.resizeImage),
            e.isLowIE && x('AfterChange', e.resizeImage);
        },
        resizeImage: function () {
          var t = e.currItem;
          if (t && t.img && e.st.image.verticalFit) {
            var n = 0;
            e.isLowIE &&
              (n =
                parseInt(t.img.css('padding-top'), 10) +
                parseInt(t.img.css('padding-bottom'), 10)),
              t.img.css('max-height', e.wH - n);
          }
        },
        _onImageHasSize: function (t) {
          t.img &&
            ((t.hasSize = !0),
            j && clearInterval(j),
            (t.isCheckingImgSize = !1),
            C('ImageHasSize', t),
            t.imgHidden &&
              (e.content && e.content.removeClass('mfp-loading'),
              (t.imgHidden = !1)));
        },
        findImageSize: function (t) {
          var n = 0,
            i = t.img[0],
            o = function (r) {
              j && clearInterval(j),
                (j = setInterval(function () {
                  return i.naturalWidth > 0
                    ? void e._onImageHasSize(t)
                    : (n > 200 && clearInterval(j),
                      n++,
                      void (3 === n
                        ? o(10)
                        : 40 === n
                        ? o(50)
                        : 100 === n && o(500)));
                }, r));
            };
          o(1);
        },
        getImage: function (n, i) {
          var o = 0,
            r = function () {
              n &&
                (n.img[0].complete
                  ? (n.img.off('.mfploader'),
                    n === e.currItem &&
                      (e._onImageHasSize(n), e.updateStatus('ready')),
                    (n.hasSize = !0),
                    (n.loaded = !0),
                    C('ImageLoadComplete'))
                  : (o++, 200 > o ? setTimeout(r, 100) : s()));
            },
            s = function () {
              n &&
                (n.img.off('.mfploader'),
                n === e.currItem &&
                  (e._onImageHasSize(n),
                  e.updateStatus('error', a.tError.replace('%url%', n.src))),
                (n.hasSize = !0),
                (n.loaded = !0),
                (n.loadError = !0));
            },
            a = e.st.image,
            l = i.find('.mfp-img');
          if (l.length) {
            var c = document.createElement('img');
            (c.className = 'mfp-img'),
              n.el &&
                n.el.find('img').length &&
                (c.alt = n.el.find('img').attr('alt')),
              (n.img = t(c).on('load.mfploader', r).on('error.mfploader', s)),
              (c.src = n.src),
              l.is('img') && (n.img = n.img.clone()),
              (c = n.img[0]),
              c.naturalWidth > 0
                ? (n.hasSize = !0)
                : c.width || (n.hasSize = !1);
          }
          return (
            e._parseMarkup(i, { title: z(n), img_replaceWith: n.img }, n),
            e.resizeImage(),
            n.hasSize
              ? (j && clearInterval(j),
                n.loadError
                  ? (i.addClass('mfp-loading'),
                    e.updateStatus('error', a.tError.replace('%url%', n.src)))
                  : (i.removeClass('mfp-loading'), e.updateStatus('ready')),
                i)
              : (e.updateStatus('loading'),
                (n.loading = !0),
                n.hasSize ||
                  ((n.imgHidden = !0),
                  i.addClass('mfp-loading'),
                  e.findImageSize(n)),
                i)
          );
        },
      },
    });
    var O,
      H = function () {
        return (
          void 0 === O &&
            (O = void 0 !== document.createElement('p').style.MozTransform),
          O
        );
      };
    t.magnificPopup.registerModule('zoom', {
      options: {
        enabled: !1,
        easing: 'ease-in-out',
        duration: 300,
        opener: function (t) {
          return t.is('img') ? t : t.find('img');
        },
      },
      proto: {
        initZoom: function () {
          var t,
            n = e.st.zoom,
            i = '.zoom';
          if (n.enabled && e.supportsTransition) {
            var o,
              r,
              s = n.duration,
              c = function (t) {
                var e = t
                    .clone()
                    .removeAttr('style')
                    .removeAttr('class')
                    .addClass('mfp-animated-image'),
                  i = 'all ' + n.duration / 1e3 + 's ' + n.easing,
                  o = {
                    position: 'fixed',
                    zIndex: 9999,
                    left: 0,
                    top: 0,
                    '-webkit-backface-visibility': 'hidden',
                  },
                  r = 'transition';
                return (
                  (o['-webkit-' + r] =
                    o['-moz-' + r] =
                    o['-o-' + r] =
                    o[r] =
                      i),
                  e.css(o),
                  e
                );
              },
              u = function () {
                e.content.css('visibility', 'visible');
              };
            x('BuildControls' + i, function () {
              if (e._allowZoom()) {
                if (
                  (clearTimeout(o),
                  e.content.css('visibility', 'hidden'),
                  !(t = e._getItemToZoom()))
                )
                  return void u();
                (r = c(t)),
                  r.css(e._getOffset()),
                  e.wrap.append(r),
                  (o = setTimeout(function () {
                    r.css(e._getOffset(!0)),
                      (o = setTimeout(function () {
                        u(),
                          setTimeout(function () {
                            r.remove(), (t = r = null), C('ZoomAnimationEnded');
                          }, 16);
                      }, s));
                  }, 16));
              }
            }),
              x(l + i, function () {
                if (e._allowZoom()) {
                  if ((clearTimeout(o), (e.st.removalDelay = s), !t)) {
                    if (!(t = e._getItemToZoom())) return;
                    r = c(t);
                  }
                  r.css(e._getOffset(!0)),
                    e.wrap.append(r),
                    e.content.css('visibility', 'hidden'),
                    setTimeout(function () {
                      r.css(e._getOffset());
                    }, 16);
                }
              }),
              x(a + i, function () {
                e._allowZoom() && (u(), r && r.remove(), (t = null));
              });
          }
        },
        _allowZoom: function () {
          return 'image' === e.currItem.type;
        },
        _getItemToZoom: function () {
          return !!e.currItem.hasSize && e.currItem.img;
        },
        _getOffset: function (n) {
          var i;
          i = n
            ? e.currItem.img
            : e.st.zoom.opener(e.currItem.el || e.currItem);
          var o = i.offset(),
            r = parseInt(i.css('padding-top'), 10),
            s = parseInt(i.css('padding-bottom'), 10);
          o.top -= t(window).scrollTop() - r;
          var a = {
            width: i.width(),
            height: (y ? i.innerHeight() : i[0].offsetHeight) - s - r,
          };
          return (
            H()
              ? (a['-moz-transform'] = a.transform =
                  'translate(' + o.left + 'px,' + o.top + 'px)')
              : ((a.left = o.left), (a.top = o.top)),
            a
          );
        },
      },
    });
    var N = 'iframe',
      F = '//about:blank',
      q = function (t) {
        if (e.currTemplate[N]) {
          var n = e.currTemplate[N].find('iframe');
          n.length &&
            (t || (n[0].src = F),
            e.isIE8 && n.css('display', t ? 'block' : 'none'));
        }
      };
    t.magnificPopup.registerModule(N, {
      options: {
        markup:
          '<div class="mfp-iframe-scaler"><div class="mfp-close"></div><iframe class="mfp-iframe" src="//about:blank" frameborder="0" allowfullscreen></iframe></div>',
        srcAction: 'iframe_src',
        patterns: {
          youtube: {
            index: 'youtube.com',
            id: 'v=',
            src: '//www.youtube.com/embed/%id%?autoplay=1',
          },
          vimeo: {
            index: 'vimeo.com/',
            id: '/',
            src: '//player.vimeo.com/video/%id%?autoplay=1',
          },
          gmaps: { index: '//maps.google.', src: '%id%&output=embed' },
        },
      },
      proto: {
        initIframe: function () {
          e.types.push(N),
            x('BeforeChange', function (t, e, n) {
              e !== n && (e === N ? q() : n === N && q(!0));
            }),
            x(a + '.' + N, function () {
              q();
            });
        },
        getIframe: function (n, i) {
          var o = n.src,
            r = e.st.iframe;
          t.each(r.patterns, function () {
            return o.indexOf(this.index) > -1
              ? (this.id &&
                  (o =
                    'string' == typeof this.id
                      ? o.substr(
                          o.lastIndexOf(this.id) + this.id.length,
                          o.length
                        )
                      : this.id.call(this, o)),
                (o = this.src.replace('%id%', o)),
                !1)
              : void 0;
          });
          var s = {};
          return (
            r.srcAction && (s[r.srcAction] = o),
            e._parseMarkup(i, s, n),
            e.updateStatus('ready'),
            i
          );
        },
      },
    });
    var R = function (t) {
        var n = e.items.length;
        return t > n - 1 ? t - n : 0 > t ? n + t : t;
      },
      B = function (t, e, n) {
        return t.replace(/%curr%/gi, e + 1).replace(/%total%/gi, n);
      };
    t.magnificPopup.registerModule('gallery', {
      options: {
        enabled: !1,
        arrowMarkup:
          '<button title="%title%" type="button" class="mfp-arrow mfp-arrow-%dir%"></button>',
        preload: [0, 2],
        navigateByImgClick: !0,
        arrows: !0,
        tPrev: 'Previous (Left arrow key)',
        tNext: 'Next (Right arrow key)',
        tCounter: '%curr% of %total%',
      },
      proto: {
        initGallery: function () {
          var n = e.st.gallery,
            o = '.mfp-gallery';
          return (
            (e.direction = !0),
            !(!n || !n.enabled) &&
              ((r += ' mfp-gallery'),
              x(u + o, function () {
                n.navigateByImgClick &&
                  e.wrap.on('click' + o, '.mfp-img', function () {
                    return e.items.length > 1 ? (e.next(), !1) : void 0;
                  }),
                  i.on('keydown' + o, function (t) {
                    37 === t.keyCode ? e.prev() : 39 === t.keyCode && e.next();
                  });
              }),
              x('UpdateStatus' + o, function (t, n) {
                n.text &&
                  (n.text = B(n.text, e.currItem.index, e.items.length));
              }),
              x(c + o, function (t, i, o, r) {
                var s = e.items.length;
                o.counter = s > 1 ? B(n.tCounter, r.index, s) : '';
              }),
              x('BuildControls' + o, function () {
                if (e.items.length > 1 && n.arrows && !e.arrowLeft) {
                  var i = n.arrowMarkup,
                    o = (e.arrowLeft = t(
                      i.replace(/%title%/gi, n.tPrev).replace(/%dir%/gi, 'left')
                    ).addClass(m)),
                    r = (e.arrowRight = t(
                      i
                        .replace(/%title%/gi, n.tNext)
                        .replace(/%dir%/gi, 'right')
                    ).addClass(m));
                  o.click(function () {
                    e.prev();
                  }),
                    r.click(function () {
                      e.next();
                    }),
                    e.container.append(o.add(r));
                }
              }),
              x(d + o, function () {
                e._preloadTimeout && clearTimeout(e._preloadTimeout),
                  (e._preloadTimeout = setTimeout(function () {
                    e.preloadNearbyImages(), (e._preloadTimeout = null);
                  }, 16));
              }),
              void x(a + o, function () {
                i.off(o),
                  e.wrap.off('click' + o),
                  (e.arrowRight = e.arrowLeft = null);
              }))
          );
        },
        next: function () {
          (e.direction = !0), (e.index = R(e.index + 1)), e.updateItemHTML();
        },
        prev: function () {
          (e.direction = !1), (e.index = R(e.index - 1)), e.updateItemHTML();
        },
        goTo: function (t) {
          (e.direction = t >= e.index), (e.index = t), e.updateItemHTML();
        },
        preloadNearbyImages: function () {
          var t,
            n = e.st.gallery.preload,
            i = Math.min(n[0], e.items.length),
            o = Math.min(n[1], e.items.length);
          for (t = 1; t <= (e.direction ? o : i); t++)
            e._preloadItem(e.index + t);
          for (t = 1; t <= (e.direction ? i : o); t++)
            e._preloadItem(e.index - t);
        },
        _preloadItem: function (n) {
          if (((n = R(n)), !e.items[n].preloaded)) {
            var i = e.items[n];
            i.parsed || (i = e.parseEl(n)),
              C('LazyLoad', i),
              'image' === i.type &&
                (i.img = t('<img class="mfp-img" />')
                  .on('load.mfploader', function () {
                    i.hasSize = !0;
                  })
                  .on('error.mfploader', function () {
                    (i.hasSize = !0), (i.loadError = !0), C('LazyLoadError', i);
                  })
                  .attr('src', i.src)),
              (i.preloaded = !0);
          }
        },
      },
    });
    var W = 'retina';
    t.magnificPopup.registerModule(W, {
      options: {
        replaceSrc: function (t) {
          return t.src.replace(/\.\w+$/, function (t) {
            return '@2x' + t;
          });
        },
        ratio: 1,
      },
      proto: {
        initRetina: function () {
          if (window.devicePixelRatio > 1) {
            var t = e.st.retina,
              n = t.ratio;
            (n = isNaN(n) ? n() : n) > 1 &&
              (x('ImageHasSize.' + W, function (t, e) {
                e.img.css({
                  'max-width': e.img[0].naturalWidth / n,
                  width: '100%',
                });
              }),
              x('ElementParse.' + W, function (e, i) {
                i.src = t.replaceSrc(i, n);
              }));
          }
        },
      },
    }),
      T();
  });
